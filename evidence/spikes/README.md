# Spike Directories

Use `SPIKE-NNN/` directories. Each Spike should contain a README plus only the code, configurations, results, and official-run evidence needed to preserve its exact scope. The next identifier is `TODO`.

