# 2026 年高教社杯数学建模竞赛 B 题

## 无线电干扰源的快速自动定位与清除

本文只做题目拆解，不进入具体建模和算法实现。

题面和两份附件已与官网发布的赛题包逐文件核对一致。本文中的“官方事实”均来自题面、附件 1 和附件 2；“分析推导”是对任务结构的整理；“待定假设”和“待澄清问题”不能直接当作官方规则。

## 一、整道题的主线

四个问题构成一条递进链：

1. **问题 1：**已有多次方向观测时，定位区域有多大？
2. **问题 2：**只有一次观测时，第二个检测点应该选在哪里？
3. **问题 3：**所有干扰源都是全向源时，如何搜索、定位并清除全部目标？
4. **问题 4：**存在定向源时，如何处理观测盲区并完成全部清除？

问题 1、2提供单源几何定位基础；问题 3、4要求把搜索、定位、移动、清除和结束判断组织成完整的交互策略。

## 二、官方事实边界

| 项目 | 题面明确内容 | 直接影响 |
|---|---|---|
| 空间范围 | 目标区域是半径 1800 米的圆，圆心为原点；东为 (x) 正向，北为 (y) 正向 | 干扰源在圆内，机器狗可以提交圆外位置 |
| 数量与频道 | 问题 3、4中干扰源总数为 10—16 个；频道来自 1—20，每个频道最多一个源 | 源数量和频道集合均未知 |
| 有效接收半径 | 每个源的有效接收半径在 1000—1500 米之间，具体值不返回 | 不能假定所有源具有同一已知半径 |
| 全向源 | 覆盖角度 360° | 在有效接收距离内通常可检测 |
| 定向源 | 有效覆盖范围为定向方向两侧各 90°，共 180°，含边界 | 背向区域可能收不到信号 |
| 示向度 | 从检测点指向源的方位角；东为 0°，逆时针为正，范围 [0°,360°) | 返回的是方向，不是距离或坐标 |
| 方向误差 | 示向度与真实方向误差在 [-1°,1°] 内 | 单次方向不能视为精确射线 |
| 重复检测 | 同一地点的环境误差固定，重复检测不会改变读数误差 | 不能默认原地重复测量就能平均消除误差 |
| 检测结果 | `direction`、`near`、`no_signal`；只有 `direction` 返回示向度 | `no_signal` 不等于附近没有目标 |
| 近距离 | 距离不超过 5 米且处于信号覆盖范围时返回 `near` | 无方向信息，但可直接清除 |
| 清除条件 | 清除点与指定频道源距离不超过 20 米即可成功 | 清除不受定向源朝向影响 |
| 运动 | 机器狗沿直线移动，速度 5 米/秒；移动时不能检测 | 移动、检测必须分开计时 |
| 初始状态 | 从 (0,0) 出发，测向机初始频道为 1 | 第一条测量指令的频道切换费用取决于是否仍为 1 |

## 三、问题 1：交会定位区域直径

### 1. 题目要求

已知若干检测点坐标，以及某干扰源在这些检测点处的示向度：

- 给出交会定位法得到的多边形定位区域直径的计算算法；
- 判断以该直径为直径的圆是否能够覆盖该定位区域。

### 2. 输入与输出

**输入：**

- 检测点坐标；
- 各检测点对应的示向度；
- 示向度误差范围 ±1°。

**输出：**

- 定位区域的构造方法；
- 定位区域直径的计算方法；
- 覆盖圆问题的判断和证明或反例。

### 3. 子问题

1. 将每次带误差的示向度转化为一个允许方向角区间。
2. 求所有方向带形成的公共可行区域。
3. 判断公共区域是否为空、有界或退化。
4. 计算区域内任意两点间距离的最大值。
5. 判断“直径为区域直径的圆”在何种意义下能否覆盖整个区域。

### 4. 关键难点

- 观测不是无误差直线，而是带角宽的方向带。
- 多次方向可能近似平行，导致区域很长、退化或无界。
- 角度跨越 0°/360° 时必须保持方向连续性。
- “区域直径”和“从某个清除点覆盖整个区域所需的最大距离”不是同一个量。

### 5. 待澄清边界

题目说的是“多边形定位区域”。需要说明是否只研究方向交会形成的多边形，还是同时叠加目标圆域、有效接收半径等先验。叠加后边界可能包含圆弧，不能与纯多边形混用。

本问没有给出具体数值实例，要求的是通用算法和几何论证。

## 四、问题 2：第二检测点的选择

### 1. 题目要求

已知一个全向干扰源在第一个检测点处测得的示向度：

- 给出第二个检测点的选择策略；
- 给出第二个检测点的候选区域；
- 使第二次观测获得较好的定位效果。

### 2. 输入与输出

**输入：**

- 第一个检测点坐标；
- 第一次测得的示向度；
- 已知的目标区域和设备规则。

**未知：**

- 干扰源准确位置；
- 干扰源与第一检测点的准确距离；
- 有效接收半径；
- 第二次观测结果。

**输出：**

- 第二检测点的选择原则；
- 第二检测点候选区域的描述；
- 候选区域有利于定位的原因。

### 3. 子问题

1. 根据第一次方向观测描述源的可能位置。
2. 定义“较好定位效果”的评价指标。
3. 保证第二点仍有机会接收到该全向源信号。
4. 在定位精度、最坏情况和移动耗时之间做取舍。

### 4. 关键难点

- 第二点必须根据当前信息选择，不能使用真实源坐标。
- 交会几何上有利的位置，未必能收到信号。
- 第一次观测不能提供距离，因此不能直接指定一个“最佳距离”。
- 必须区分“源可能出现的区域”和“机器狗可前往检测的候选区域”。

### 5. 非官方部分

“较好”没有由题目唯一规定。可以选择定位区域直径、最坏情况误差、成功交会概率、移动代价等作为评价指标，但必须在后续方案中明确说明，不能写成官方要求。

## 五、问题 3：全向源的搜索、定位与清除

### 1. 题目要求

目标区域内全部为全向干扰源，数量在 10—16 个之间但未知。机器狗从原点、频道 1 出发，要求：

- 在确保所有源被清除的前提下完成任务；
- 完成时间尽可能短；
- 设计自动搜索、定位、清除策略和算法；
- 先进行不限次数的演练测试，再完成三次正式测试；
- 计算演练统计量，填写正式测试表格并导出日志。

### 2. 交互中可获得的信息

机器狗不能直接读取源的数量、位置、频道集合或有效接收半径，只能通过接口逐步获得：

- `measure` 返回的检测结果和示向度；
- `clear` 返回成功或未发现；
- 当前机器狗位置；
- 当前测向机频道；
- 已执行动作和累计时间。

### 3. 必须解决的子问题

| 子问题 | 要回答的内容 | 结果依赖 |
|---|---|---|
| 发现目标 | 去哪些位置、检测哪些频道才能发现未知目标 | 搜索覆盖和频道调度 |
| 缩小位置范围 | 发现某频道后还需要哪些观测 | 问题 1、问题 2 |
| 实施清除 | 何时移动到清除点、何时调用 `/clear` | 定位不确定性和20米清除半径 |
| 安排行动顺序 | 继续定位当前目标还是转向其他频道 | 移动、检测、切换成本 |
| 判断任务完成 | 数量未知时如何确认没有遗漏 | 搜索覆盖证明和清除记录 |
| 测试与提交 | 如何统计演练并记录正式结果 | 模拟器规则和日志要求 |

### 4. 时间组成

| 动作 | 虚拟耗时 |
|---|---:|
| 移动 | 直线距离 ÷ 5 |
| `/measure` 检测 | 5 秒 |
| 测向机频道切换 | 1 秒 |
| `/clear` 未发现目标 | 3 秒，加移动时间 |
| `/clear` 成功 | 5 秒，加移动时间 |
| `/enter`、`/exit` | 不增加虚拟时间 |

特别注意：`/clear` 的频道参数只是指定清除目标，不会切换测向机频道，也不会产生频道切换耗时。

### 5. 关键难点

1. **找全与结束判断：**清除若干目标不代表已经完成，因为总数未知；不能把一次 `no_signal` 直接解释为该频道不存在目标。
2. **定位与效率权衡：**多检测一次可能缩小位置范围，但会增加检测、移动和换频道时间。
3. **信息逐步揭示：**这是在线决策问题，不是已知全部坐标后的静态路径规划。

### 6. 统计和正式测试要求

演练统计量：

\[
\text{清除干扰源个数的比例}
=\frac{\text{被清除干扰源个数}}{\text{干扰源总数}}
\]

\[
\text{平均定位清除时间}
=\frac{\text{定位清除总时间}}{\text{被清除干扰源个数}}
\]

正式测试表格需要填写：

- 测试案例编码；
- 清除干扰源个数；
- 平均定位清除时间；
- 程序运行时间。

问题 3有三次正式测试。正式测试不显示干扰源总数，因此不能自行补出未经观测或未经官方反馈的清除比例。

## 六、问题 4：全向源与定向源混合

### 1. 题目要求

目标区域中既有全向源，也有定向源；总数仍为 10—16 个，但以下信息未知：

- 定向源数量；
- 哪些频道对应定向源；
- 定向源的发射方向。

需要在此条件下重新设计检测与清除策略和算法，并完成演练及三次正式测试。

### 2. 可以沿用的部分

- 坐标系和目标圆域；
- 频道互不相同；
- 示向度的定义及 ±1°误差；
- 机器狗移动和动作耗时；
- 20米内清除规则；
- 正式测试和日志要求。

### 3. 必须重新处理的部分

- 搜索覆盖不能只依赖全向源的接收性质。
- `no_signal` 可能由距离过远、频道无源或定向源背向造成。
- 不能根据一次未检测到信号就排除一个区域。
- 结束条件需要能解释定向源造成的观测盲区。

### 4. 关键事实

- 示向度表示从检测点指向源的位置方向，不表示源的发射朝向。
- 定向源即使无法被某个检测点接收，仍可在距离不超过20米时被清除。
- 题目没有要求必须识别每个源的类型和朝向；是否推断这些信息取决于后续策略。

## 七、四问之间的依赖关系

| 前置内容 | 支撑后续 | 依赖原因 |
|---|---|---|
| 坐标、角度、误差规则 | 问题 1、问题 2 | 决定观测如何转化为位置约束 |
| 问题 1的区域评估 | 问题 2 | 为第二检测点的效果评价提供依据 |
| 问题 1、问题 2 | 问题 3的单源定位 | 判断是否需要继续观测及下一点位置 |
| 目标发现、定位和清除反馈 | 问题 3的行动安排 | 决定搜索与清除顺序 |
| 搜索覆盖证明、清除记录 | 问题 3的结束判断 | 同时证明“已清除”和“无遗漏” |
| 问题 3的完整流程 | 问题 4 | 保留基本流程，重新处理定向盲区 |
| 演练测试 | 问题 3、问题 4正式测试 | 正式机会有限，必须先验证策略 |

## 八、官方事实、建模假设与待澄清问题

### 1. 官方事实

- 目标区域半径为1800米，源均在圆内。
- 源数量为10—16个，频道为1—20，每频道最多一个源。
- 有效接收半径为1000—1500米，具体值不返回。
- 全向源覆盖360°，定向源覆盖180°。
- 示向度误差范围为±1°。
- 机器狗速度为5米/秒；检测5秒；频道切换1秒；清除失败3秒、成功5秒。
- 清除半径20米，清除不受定向朝向影响。
- 问题3、问题4各有三次正式测试。

### 2. 后续需要自行定义的建模内容

- 问题 2 中“较好定位效果”的具体评价指标；
- 搜索点的布置方式；
- 频道检测顺序；
- 由方向误差构造定位区域的方法；
- 多目标之间的访问和清除顺序；
- 数量未知时的结束判据；
- 演练中用于测试的源位置、接收半径和定向方向分布。

### 3. 不能擅自加入的假设

以下内容题面没有保证，若使用必须显式标注为额外假设：

- 示向度误差服从均匀分布或正态分布；
- 不同地点的误差相互独立；
- 所有源的接收半径相同；
- 源位置均匀分布；
- 定向源朝向均匀分布；
- `no_signal` 等价于附近没有源；
- 机器狗不能走出目标圆域。

### 4. 待澄清问题

- 问题 1 的定位区域是否只指方向交会多边形，还是要叠加圆域和接收距离限制？
- 示向度保留两位小数后，边界误差是否仍按严格 ±1°处理？
- 一次测试清除0个源时，平均定位清除时间如何报告？
- 正式测试只公布清除个数而不公布总数时，论文是否只按表1填写而不计算比例？题面现有文字支持后者。

## 九、测试和接口边界

- 测试前需要在线登录、联网并完成服务器时间校验。
- 模拟器接口默认地址为 `http://127.0.0.1:2026`。
- 机器狗必须先调用 `/enter`，结束时主动调用 `/exit`。
- `/measure` 和 `/clear` 必须串行发送，不能并发发送不同新动作。
- 每个新动作使用新的 `request_id`；网络重试时复用完全相同的请求和原 ID。
- 必须同时检查 HTTP 状态和 `accepted` 字段。
- `/measure` 的频道会更新测向机当前频道；`/clear` 不会。
- `/enter` 返回的 `remaining_real_duration_s` 是本局实际剩余现实时间，不能固定假定总有1200秒。
- 测试窗口为25分钟；成功 `/enter` 后程序运行最长20分钟，以较早截止者为准。
- 虚拟世界活动时长上限为100小时；坐标分量绝对值不得超过2000000米。
- 正式测试结束后要导出三份日志，问题3明确要求不要修改文件名；问题4同样要求导出日志。

## 十、最容易犯的错误

1. 把 `no_signal` 当成“该频道没有源”。
2. 把单次示向度当成无误差真实方向。
3. 把 `near` 当成检测失败而继续远距离搜索。
4. 给 `/clear` 错算频道切换时间。
5. 只优化平均时间，却没有证明全部源都能被清除。
6. 把虚拟任务时间和现实程序运行时间混为一谈。
7. 在正式测试中使用不可见的目标总数计算未经支持的指标。
8. 把演练阶段的测试真值带入正式测试结论。

## 十一、后续建模前应先完成的工作

1. 明确问题 1 的定位区域定义和退化情况处理。
2. 为问题 2确定“较好”的量化指标。
3. 证明问题 3的搜索覆盖和结束条件。
4. 为问题 4单独设计定向源盲区下的再搜索机制。
5. 先在模拟器演练中记录完整动作序列、响应和耗时，再进行正式测试。

