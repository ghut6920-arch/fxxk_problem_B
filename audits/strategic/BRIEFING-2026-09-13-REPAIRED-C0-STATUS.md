# 事实简报：修复版 C0 基准现状（供外部 Strategist）

记录日期：2026-09-13。记录人：Technical Lead。  
只陈述固定版本、数字与已有审查结论。不提出新的 KEEP/MODIFY/REJECT，不选型，不关闭 RT-002，不授权正式 `/enter`。

请与下列文件一并阅读：

- `audits/strategic/BRIEFING-2026-09-13-FACTS.md`（夹具、P1-A、D-004、WI-017 旧演练）
- `audits/strategic/BRIEFING-2026-09-13-SNAKE-AND-RT005.md`（蛇形缺陷与 RT-005）
- `audits/strategic/SR-002.md`、`DECISIONS.md` D-007

写入前本地 `main` HEAD：`8f50f8335b51f1cb909252baa9c533bba6666e7b`（未全部 push）。

---

## 1. 本轮用户授权（D-007）做了什么、没做什么

用户授权：验证**修复版 C0**，并在其过关后有限研究 225→150 清除与 Q4 81→49 扫描。  
明确**不**自动重开 C1、C2、正式场次。  
SR-002 的 C1/C2/正式资格 REJECT 仍在。D-007 只把 150/49 从「本窗口预算 0」里划出为**后置研究**，且须先完成修复版 C0 验证。

阶段 1（WI-020）已执行。阶段 2 数学（EXP-005 + RT-006）已 KEEP。**150/49 代码尚未签发、未实现。**

---

## 2. 修复版 C0（标签 B）固定对象

| 项 | 值 |
|---|---|
| 蛇形修复结果 | `d675831695d705ea8c4ffcb74fe8c83277847daf` |
| `scan.py` blob | `32dd26f7fccf8b2d50f2af349251848cea1c39fc`（WI-020/022 均未改） |
| 网格/清除 | P3=9，P4=81，清除中心 225；局部 `(i,j)` 蛇形再旋转 |
| 阶段 1 结果提交 | `7f4dfba66a1467da865e411c2aa03bbec2203843` |
| RT-005 | `08bf26a2ae0e3873ad742ff1c977b860b16973e8`：旧序 MAJOR（不可把 `224·22` 套在 `a48c77c`）；修复后本范围无 MAJOR/CRITICAL 残留 |
| `src/candidate/` | 仍不在 `main` |

距离论证：局部 20 m 步长 + 刚体变换保距 + 1 m 提交界。有限角度测试只核验实现。

---

## 3. 阶段 1 离线（WI-020）

在 `7f4dfba` 上复跑：`tests/p1a`、`tests/candidate`、`tests/p1b`、`tests/p1a_run`（孤立 `run_suite()` 为 `P1A_PROPERTIES_PASS`，`checks_failed=0`）。  
新增 mock Q3/Q4 全流程、N=16 动作计数、未知接受/截止失败即停。清除内部最大步长 20.000 m。成本含原点→首测点、扫描、清除内部、跨频道。

残留：`tests/p1a_run` 单测 10 s 帽在负载下可能把 G11 标成 UNRESOLVED；不视为 B 缺陷，未抬帽。

---

## 4. 阶段 1 实机演练（修复版 B，各 1 场）

均 practice，`robot_id=202619007369`，无正式 `/enter`，自适应动作 0，未知接受 0。

| | K | N（操作者最终口径） | 类型 | Tv (s) | 墙钟 (s) | 进场剩余真实时间 (s) |
|---|---:|---:|---|---:|---:|---:|
| Q3 | 14 | **14** | 14 全向 | 16537.6 | ~10.9 | 1189 |
| Q4 | 16 | **16** | 5 全向 + 11 定向 | 37326.2 | ~32.9 | 1167 |

操作者一度把 Q3 的 N 报成 16（与 Q4 混淆）。后更正为 14。同场模拟器 sidecar `result.json`（`practice-p3-3558640547382552954-Q4UM-RKFT-SRPG-MQCQ`）：`jammer_count=14`，全向 14。与 K=14 一致。

**两场均为 K=N 清完。** 用户提供的 `.jlog` 载荷加密，未解密出 `g`/`R_c`。

不可与 `a48c77c` 旧两场（当时 K=N=12）做因果对比，不能把差值记成蛇形修复收益。这些场次**不是** SR-002 / 设计 §10 的 3 场资格包。

---

## 5. WI-022（在 N 更正之前做的诊断）

结果 `7cbeda3723e43d40623e0d0eaff5b634fb2ae36a`。未改 `src/candidate/`。

当时按错误的 N=16 解释「14/16 + COMPLETE」。日志事实仍成立：180 次 measure、每频道 9 个 P3、六路全 `no_signal` 后按方案写无源再 COMPLETE。P3 覆盖 `700√2<1000` 仍成立。

N 更正后：**该场不是漏检。** 诊断保留为反事实控制流说明，不描述实际 K/N。O-10：COMPLETE 以 `g∈D`、`R_c∈[1000,1500]` 为前提；未把 COMPLETE 改成依赖事后 N。

---

## 6. 150/49 数学（未实现）

`experiments/EXP-005/COVER_VARIANT_SPEC.md`。RT-006（`5a707f5e3ba331b39fa399b2ee3b6eb3260932d2`）**KEEP** 候选 A（150 清除）与 B（Q4 的 49 点扫描）作为**数学**。实现约束（若将来写代码）：20×30 单元而非 20×20；保留圆外 `P_4'` 点；解析上界 62946.57 s / 3380 请求不是成绩。KEEP **不是** 实现 WI，不是选型。

---

## 7. 尚未做的

- 150 点清除、49 点扫描的 **程序**
- 12 世界 × 多臂合成比较（阶段 3）
- 冻结版本每题 ≥3 场合格演练与压力（阶段 5 / RT-002）
- 模拟器来源独立哈希（D-006 仍是用户保证）
- C1 实现；正式 `/enter`
- 将 `src/candidate/` 合入 `main` / 远程 push 最新审查提交
- 独立挑战「合法 Q3 不可能漏」的论文句（当前无漏可证）

---

## 8. 本文不裁定、交给顾问的问题

1. 修复版 B（9/81/225，已两场 K=N 演练）是否作为本轮默认比赛程序。  
2. 是否投入 150/49 **实现与同案例比较**（数学 KEEP 已有；代码没有；17:30 北京时间新测试开始截止仍在规则中）。  
3. 是否进入阶段 5 资格证据，或先写答卷。  
4. C1 是否仍按 SR-002 关闭（阶段 6 的七条尚未满足：无同世界优化对照，无主动测量必要性证据）。  
5. 答卷如何引用：蛇形修复、两场 K=N、WI-022 反事实、RT-006 KEEP。

---

## 9. 路径索引

| 文件 | 相对路径 |
|---|---|
| 本简报 | `audits/strategic/BRIEFING-2026-09-13-REPAIRED-C0-STATUS.md` |
| 前两份简报 | `BRIEFING-2026-09-13-FACTS.md`，`BRIEFING-2026-09-13-SNAKE-AND-RT005.md` |
| RT-005 / RT-006 | `audits/redteam/RT-005.md`，`RT-006.md` |
| TR-020–025 | `audits/technical/` |
| EXP-005 规格 | `experiments/EXP-005/COVER_VARIANT_SPEC.md` |
