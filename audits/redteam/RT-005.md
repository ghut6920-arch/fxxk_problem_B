# RT-005 — WI-018 clear-rectangle snake (old vs repair)

- Date: `2026-09-13`
- Reviewer Role: Independent Red Team
- Reviewed Target and Version: old `a48c77cc0d46a81b96aa56f54197b8837c4c735c`; repair `d675831695d705ea8c4ffcb74fe8c83277847daf` (WI-018 result; parent `4d24d09e4985fe7aef4da0eb7d1efaaf53dd5a11`). Review Execution Start `c7279cf21c9cfd9e410ffb1406eb42701f268aa1` is **not** a review target.
- Finding Summary: on `a48c77c`, `clear_rectangle_centres` snakes by **global y after rotation**, so the emitted sequence violates plan §5.2 adjacent ≤22 m while the 225-point **set** remains correct. Independent 45° counterexample: 150/224 steps >22 m, max **72.11102550927974** m \(=\sqrt{40^2+60^2}\), path **5782.9045** m. Repair `d675831` snakes local `(i,j)` then rotates: same set, max **20.00000000000016** m, 0 steps >22 m, path **4480** m. Highest severity applies to the **old** binary’s path-cost claims. Repair leaves **no MAJOR/CRITICAL residual in this scope**.
- Highest Finding Severity: `MAJOR` (old sequence / \(224\cdot22\) / \(V_{\rm src}\) misuse). Not model-family. Repair: not applicable for MAJOR/CRITICAL inside this WI.

This record does **not** close `RT-002`. It does not write `P1A_PROPERTIES_*`, does not reverse practice `K=N=12`, and is not formal qualification.

## Independent Scope

File the already independently recomputed challenge of two **fixed** commits under WI-019. Read those trees with `git show` / extracted blobs. Do not check out Executor branches. Do not edit `src/`.

In scope: emitted 225-centre **order** vs plan §5.2–5.3; set cover vs path cost; G16/test capture; same-channel consecutive `/clear` distances in stored EXP-003 logs; whether `d675831` restores the 22 m sequence premise without shrinking the grid or adding C1.

Out of scope: new live rehearsal; rewriting historical PASS labels; merge/push; C1; closing `RT-002`.

## Attack Methods and Evidence

- Verified objects: both targets, Comparison Base `f06fd5915c9ec325eca55011c848718da7cc01e5` (ancestor of HEAD), `HEAD:work/WI-019.md`.
- Reimplemented two orders from the plan formula only: (1) rotate then `snake_order` by global `y`; (2) local-index snake (`j` even: `i` increasing; `j` odd: `i` decreasing) then \(S+xt+yt_\perp\). Compared both to `git show` of `scan.py` at each commit.
- Distances: Euclidean consecutive steps; bound 22 m \(=20+1+1\).
- Logs: `evidence/experiments/EXP-003/q3_practice_requests.json` and `q4_practice_requests.json` at `a48c77c`. Same-channel consecutive `/clear` with `http_status=200`, `accepted=true`, `attempts=1`, unique `request_id` and `seq`. Channel changes excluded (11 hops = 12 sources).
- G16 at `a48c77c` vs `d675831` `tests/p1a_run/checks.py`; new `tests/candidate/test_clear_sequence.py` on the repair tree.
- TR-019/TR-020 were read last and not used as numeric authority.

## Findings and Counterexamples

### RT5-F1 — Global-y snake after rotation breaks the 22 m sequence (old `a48c77c`)

- Severity: `MAJOR` for applying plan §5.3 \(224\cdot22\) / \(V_{\rm src}\le3662.6\) to this binary’s **emitted clear order**. Cover of \(G(S,\theta)\) is not in dispute. Not model-family.
- Minimal counterexample: `S=(0,0)`, `θ=π/4`, centres \(S+(10+20i)t+(-20+20j)t_\perp\), \(i=0..74\), \(j=0,1,2\).

| Order | n | steps >22 m | max adjacent (m) | path (m) |
|---|---:|---:|---:|---:|
| Independent global-y snake = `a48c77c` `clear_rectangle_centres` | 225 | **150**/224 | **72.11102550927974** | **5782.9045** |
| Independent local `(i,j)` then rotate = `d675831` | 225 | **0** | **20.00000000000016** | **4480.0** \(=224\times20\) |

Sets equal (225 distinct rounded points). First five global-y steps: `20`, `20√2≈28.284271`, `20`, `28.284271`, `28.284271`. Worst step index 215, displacement ≈`(70.711, 14.142)`, not a 20 m cell side.

Strengthening (not required to prove the defect): global-y at **θ=90°** still has max **44.72136** m (1 step >22); **θ=0.5°** max **1480.135** m (2 steps >22); **θ=123.456°** 222/224 steps >22. **θ=0** coincides with local snake (max 20 m). Translation `S=(1000,-250)`, θ=45°: global-y max **56.568542** m, still 150 steps >22; local still 20 m.

- Expected: snake in the rectangle, then rigid motion; adjacent submitted ≤22 m; \(V_{\rm src}\) uses \(224\cdot22\).
- Actual at `a48c77c`: snake in global `y` of already rotated points.
- Affected claim: path-cost instantiation of §5.3 on this sequence. **Not** 225-set cover; **not** practice `K=N=12`.
- Required remediation: local-index snake then rotate (done at `d675831` under WI-018). Do not apply \(224\cdot22\) to unrepaired logs.

### RT5-F2 — G16 / θ=0 tests could not catch the defect

- Severity: `MAJOR` as a **test-gap** on `a48c77c` (same defect family as F1). Not a second independent oracle bug.
- Evidence: old `g16` checks set equality, a fixture pair `[10,0]–[30,0]`, and the constant 22, with fixture `theta_deg=0` where the two orders coincide. Old `tests/candidate/test_scan.py` also uses θ=0. Passing those tests is not sequence correctness after rotation.
- Expected: at least one non-axis heading walking **all** consecutive emitted distances.
- Actual: repair adds `tests/candidate/test_clear_sequence.py` (legacy global-y at 45° expects 150 long steps and \(\sqrt{40^2+60^2}\); repaired sequence ≤20 m) and G16 harness walks 45°/90°/123.456°. Independently, those assertions match the recomputed numbers.
- Affected claim: WI-016 P1-A G16 PASS remains valid for **set** cover and θ=0 adjacency; it is **not** a check of rotated snake or of \(224\cdot22\) on `a48c77c`.

### Practice logs (same-channel consecutive `/clear`) — supporting evidence, not a second finding

Independent filter: `/clear`, HTTP 200, `accepted=true`, `attempts=1`; unique `request_id` and `seq`; consecutive pair counted only if **channel equal**. Unaccepted: 0. Channel-change skips: 11 (12 sources).

| Log at `a48c77c` | requests | `/clear` | same-ch segs | >22 m | max (m) |
|---|---:|---:|---:|---:|---:|
| `q3_practice_requests.json` | 2027 (1+180+1845+1) | 1845 | **1833** | **1235** | **382.0994634908561** (seq 1582→1583, ch 16) |
| `q4_practice_requests.json` | 3319 (1+1620+1697+1) | 1697 | **1685** | **885** | **501.59744815937813** (seq 2460→2461, ch 10) |

Same-channel reconstructed move time \(\sum d/5\): Q3 **24778.96** s; Q4 **20656.13** s. All accepted measure+clear position hops: Q3 **31476.92** s / exit vt **38510.900536**; Q4 **36272.40** s / **51898.355196**. These vt values remain below 63000/81000 s. That does **not** restore \(224\cdot22\).

### Repair `d675831` — long steps gone in this scope

- `local_core_snake` is a **permutation of the 225 cores** `(i,j)` only; then the existing formula. Not a new C0 primitive; not 225→150; not 81→49; not C1.
- Independent: θ ∈ {45, 30, 60, 123.456, −37, 0.5, 90, 180, 270} and a translation, max step 20 m, 0 over 22 m, path 4480 m; set equals the formula set and the old global-y **set**.
- θ=0 order still matches catalog snake (G16 still meaningful).
- Residual inside this WI: **no MAJOR/CRITICAL**. No new live rehearsal, so wall-clock/`K` under the new order is **not** claimed. The 10000 m inter-rectangle connect term in \(V_{\rm src}\) was not re-proved here.

## Reproduction Instructions

Extract `src/candidate/scan.py` from each commit (do not check out Executor). Build centres from the formula; order A = global-`y` snake; order B = local `(i,j)` snake then rotate. Compare to `clear_rectangle_centres`. At `S=(0,0)`, `θ=π/4`, A must match `a48c77c` (150 steps >22 m, max \(\sqrt{40^2+60^2}\)); B must match `d675831` (224×20 m). Parse EXP-003 JSON as above for same-channel `/clear`.

Do not treat unittest pass as the proof; the 45° table is the proof.

## Steelman

1. 225-point cover of \(G(S,θ)\) never depended on visit order.
2. Practice `K=N=12` (operator UI) is a completeness observation, not a 22 m path theorem.
3. Observed practice virtual times stayed under the rounded 63000/81000 s envelopes.
4. Repair restores the sequence the \(224\cdot22\) term assumed, without changing the set.

## Strongest Counter-Argument

1. A proof that uses \(224\cdot22\) on `a48c77c`’s actual `/clear` sequence is false.
2. G16 at θ=0 is the exact heading where the bug is invisible.
3. Two quiet practices under budget do not certify the 22 m guarantee for arbitrary heading.
4. Repair of this sequence is not a live-log re-score and not formal readiness.

## Required Handoff

Technical Lead: retrieve `audits/redteam/RT-005.md`. On `a48c77c`, do not cite \(V_{\rm src}\) / \(224\cdot22\) for the unrepaired order. On `d675831`, the sequence premise of §5.2 is restored in the checked rotations; do not merge/push/C1/simulator from this record. `RT-002` stays open. Strategist: not required for this implementation-order defect.

## Participation and Review Limits

- Reviewer Prior Participation: authored `audits/redteam/RT-004.md` (WI-015 candidate `e4ee8fe`, which is an ancestor of `a48c77c` and contained the global-y snake). **Did not** author `src/candidate/scan.py` under WI-018 (`deepseek-flash`). WI-019 allows this filing; prior RT-004 is disclosed.
- Independence and Coverage Limitations: no new rehearsal; log move-time sums depend on the stated filter (same-channel `/clear` vs all positioned hops). Numbers in this record were recomputed from formula and JSON, not copied from TR-019.

## Follow-up and Closure

- Decision / Repair Owner and Review Reference: Technical Lead for using `d675831` vs unrepaired logs. Implementation repair already exists (WI-018 / TR-020). This is the independent challenge record.
- Reproduction or Repair Evidence and Version: defect `a48c77cc0d46a81b96aa56f54197b8837c4c735c`; repair `d675831695d705ea8c4ffcb74fe8c83277847daf`.
- Red Team Recheck Result: first filing. Old defect **holds**. Repair **removes** the long steps in the checked headings; set unchanged; no grid shrink.
- Remaining Findings or Disagreements: RT5-F1/F2 on the old binary. Repair: no MAJOR/CRITICAL residual in this scope. `RT-002` F1 remains open.
- Strategist Closure Decision / Evidence / Affected Version Reference: not applicable (no CRITICAL in this record). Do not close RT5-F1 by KEEP of a still-wrong `a48c77c` sequence; the finding is limited to that version’s path-cost claims.
- Reopen Conditions: any later `clear_rectangle_centres` that again orders by global coordinates after rotation; 225-set change; tests that only check θ=0 or the constant 22; citing \(224\cdot22\) on unrepaired logs; treating this repair as C1, formal qualification, or `RT-002` closure.

## Answers required by WI-019

- **A.** The long-step defect **holds** at `a48c77c`.
- **B.** Set cover and practice `K=N=12` **stand**. \(224\cdot22\) / \(V_{\rm src}\) on the unrepaired sequence, and “G16 proved rotated order,” **must not** be used.
- **C.** `d675831` **solves this item** on the checked rotations/translations; no new live run; other \(V_{\rm src}\) terms not re-proved.
- **D.** Technical Lead archives this record and bounds claim language. External Strategist is **not** required for this repair.

This record does not close `RT-002`. It does not select a model.
