# RT-007 — WI-023 cover-variant implementation `5390c53`

- Date: `2026-09-13`
- Reviewer Role: Independent Red Team
- Reviewed Target and Version: `5390c53a3a81d6c1b27f9b043f271382cdf065ed` (parent `6a8e904be1fc345b33f175e6d30750218d3b28a6`). Review Execution Start `d5df6c2701aa15bab4610c7e6ec50bcb7331140e` is **not** a review target.
- Finding Summary: CLEAR150 is 20×30 with a local snake and a single 30 m wrap; SCAN49 keeps the 28 exterior lattice points; BASE `plan=None` still uses frozen `scan.py` blob `32dd26f7fccf8b2d50f2af349251848cea1c39fc`; COMBINED is SCAN49 scan + CLEAR150 clears. One **MAJOR** provenance gap: committed `worlds.json` SHA-256 is **not** the hash recorded in the report and `compare_tracks.json`. Screens may be cited as **research** evidence only with that caveat; they are not a selection.
- Highest Finding Severity: `MAJOR` (world-file hash mismatch). Geometry of the four tags: no refuting finding in this scope.

This record does **not** close `RT-002`. It does not select a model. No `src/` edits. No 36-track rerun. No `tests/p1a_run` discover.

## Independent Scope

WI-025 challenge of the **implementation** at `5390c53`: CLEAR150 tiling and order; SCAN49 exterior retention; BASE default path; whether 36 tracks feed hidden truth to the policy; screens vs selection. Trees read with `git show` / extracted blobs. Executor worktree not checked out.

Out of scope: rerunning RT-005 snake identities except as cited; rerunning RT-006 dense disk/circle cover; live sim; C1; Q2.

## Attack Methods and Evidence

- `git rev-parse 5390c53:src/candidate/scan.py` vs `d675831:src/candidate/scan.py`.
- Loaded extracted `src/candidate/variants.py` and `model.py`; emitted CLEAR150/SCAN49/COMBINED sequences; `C0Runner(env=None).plan`.
- SHA-256 of `git show 5390c53:evidence/experiments/EXP-005/worlds.json` bytes; compared to `compare_tracks.json` `worlds_sha256` and COVER_VARIANTS.md. Checked N, types, \(R_c\), \(\|g\|\le1800\) on the 12 worlds. Did **not** replay 36 tracks.
- Inspected `scripts/run_cover_compare.py` `WorldEnv` vs `C0Runner` for truth flow.
- Author report and `test_the_exterior_ring_is_not_needed_for_in_disk_coverage` read last.

## Findings and Counterexamples

### RT7-F1 — Frozen worlds hash does not match the committed file

- Severity: `MAJOR` for **provenance of the 36-track table**, not for CLEAR150/SCAN49 geometry.
- Evidence: independent SHA-256 of the commit blob `5390c53:evidence/experiments/EXP-005/worlds.json` is

  `f00b1718f64de2dae3bff38187805f6e75c2697cf4cad4d80362f588b0e573f6`

  (`len=26082`, LF only). The same digest is obtained from `json.dumps(parsed, indent=1, sort_keys=False)`. COVER_VARIANTS.md and `compare_tracks.json` record

  `c4911cea0841983a39a0c57a991acaa77b5ae228dc47aa4b07cfd4fefb8c5d99`

  which was **not** reproduced from the committed bytes (including indent-2, `sort_keys`, trailing newline, UTF-8 BOM).
- Expected: the hash in the report and track file equals the committed worlds bytes, so the 36 rows are bound to that file.
- Actual: two different digests. The table cannot be independently tied to the frozen file at this commit.
- Affected claim: “worlds frozen before the run; SHA-256 c4911cea…”. Geometry claims in §4 of the report are unaffected.
- Required remediation: re-hash the committed blob into the report and `compare_tracks.json`, or replace worlds.json with the exact bytes that hash to `c4911cea…` and say which. Do not treat the 36-track screens as bound evidence until that match exists.

### RT7-F2 — SCAN49 unit test invites dropping the exterior ring

- Severity: `MINOR`.
- Evidence: `scan49_points()` emits all 49 prescribed points, including 28 with \(\|p\|>1800\) (max \(2100\sqrt{2}\approx2969.85\); corners \((\pm2100,\pm2100)\) present). Independent. RT-006: deleting exterior points fails directional cover at \(g=(1800,0)\), \(\phi=0\).
- `test_the_exterior_ring_is_not_needed_for_in_disk_coverage` only measures **nearest-lattice distance** on a 25 m grid (omni-style) and states dropping the ring leaves no hole. That is not the closed half-plane claim.
- Expected: tests that fail if any of the 28 exterior points is missing; do not certify a 5×5 filter.
- Actual: generator keeps the ring (conforming). The omni-distance comment is misleading.
- Affected claim: none of the emitted SCAN49 set. Risk is a later “optimisation” that filters \(\|p\|>1800\).
- Required remediation: delete or rewrite that test so it cannot pass a filtered 25-point lattice as SCAN49.

### Named WI-025 checks (no extra finding)

**BASE default path unchanged.** `5390c53:src/candidate/scan.py` blob `32dd26f7fccf8b2d50f2af349251848cea1c39fc` equals `d675831:src/candidate/scan.py`. `C0Runner(env=None).plan is None`; `_clear_plan(..., "direction")` equals frozen `scan.clear_plan` (225 centres); `_budget("Q3")` equals `c0_budget_certificate("Q3")` = 63000. `plan_for("BASE").clear_centres` matches `scan.clear_rectangle_centres` at 0°/45°/123.456°.

**CLEAR150 is 20×30, not 20×20.** Set \(\{(10+20i,y): i=0..74,\, y\in\{-15,15\}\}\), 150 distinct. Local snake then rotate: step index 74 is **30 m**, `(1490,-15)→(1490,15)`; 148 steps of 20 m; path 2990 m. \(\sqrt{325}+1\approx19.027756<20\). Headings 0, 45, 90, 123.456, −37, 0.5°: max step ≤30 m (float), exactly one 30 m wrap, 148×20 m. 20×20 squares at \(y=\pm15\) would not produce a 30 m wrap or cover \(|y|\in[25,30]\).

**SCAN49 retains exterior points.** 49 = \(\{700(i,j): i,j=-3..3\}\); 28 with \(\|p\|>1800\); 980 measures, 979 switches; Q3 still 9 points. COMBINED: Q4 scan 49, clears 150, request bound 3380, budget 62946.57 s.

**CLEAR150 scan stays 81; SCAN49 clears stay 225.** COMBINED is only those two switches.

**Worlds legal (content).** 12 worlds, seed 20260913, 3 dev + 3 holdout per question. All \(N\in[10,16]\); Q3 all omni; Q4 mixed types; \(R_c\in[1000,1500]\); \(\max\|g\|\approx1767.2\le1800\); pairwise separation >120 m (no illegal pair in this file). `compare_tracks.json` has 36 rows. Content legality does **not** repair RT7-F1.

**Truth not passed to policy (code inspection, not a 36-track replay).** `C0Runner` consumes `observation` / `theta_hat` / `success` only. `WorldEnv` uses `MockSimulator` internally for physics; `truth_count()` / `cleared_channels()` run **after** the track for the table. Hidden `g` is not a `C0Runner` constructor argument. This is the intended env/policy split. Not a live-sim claim.

**Screens are not a selection.** COVER_VARIANTS.md already labels holdout median-save as screens and says not a model selection. With RT7-F1, even research use of those save percentages must note the unbound hash. Do not promote COMBINED from this table.

## Reproduction Instructions

```
git rev-parse 5390c53a3a81d6c1b27f9b043f271382cdf065ed:src/candidate/scan.py
# 32dd26f7fccf8b2d50f2af349251848cea1c39fc

git show 5390c53:.../worlds.json | python -c "import hashlib,sys; print(hashlib.sha256(sys.stdin.buffer.read()).hexdigest())"
# f00b1718f64de2dae3bff38187805f6e75c2697cf4cad4d80362f588b0e573f6
```

Extract `src/candidate` from `5390c53`. `clear150_centres((0,0),0)[74:76]` is `(1490,-15)`, `(1490,15)`; distance 30. `scan49_points()` contains `(\pm2100,\pm2100)`. Do not `unittest discover -s tests/p1a_run`. Do not replay 36 tracks to confirm this record.

## Steelman

1. Variant geometry matches the RT-006 KEEP identities on the emitted sequences checked here.
2. Default runner without `plan=` is the frozen 9/81/225 path.
3. Author text already says screens are not selection.
4. World **contents** in the commit are legal C0 worlds.

## Strongest Counter-Argument

1. A 36-track table whose recorded worlds hash is not the committed file is not reproducible evidence.
2. An omni nearest-point test must not be read as permission to drop SCAN49 exterior points (RT-006 directional counterexample).
3. Same-model D-004 screens remain non-independent evaluation.
4. `COVER_VARIANTS_READY` does not make COMBINED the default and does not close `RT-002`.

## Required Handoff

Technical Lead: geometry of CLEAR150/SCAN49/COMBINED/BASE default may be used as **implementation** evidence at `5390c53`. Rebind or regenerate the worlds hash before citing the 36-track screens. Do not select a tag. Do not close `RT-002`. Do not merge/push from this record. Strategist: not required for this implementation challenge.

Research use of the screens: allowed only as a same-model screening sample, and only after RT7-F1 is repaired or explicitly scoped as “table not bound to committed worlds.json”.

## Participation and Review Limits

- Reviewer Prior Participation: authored RT-004, RT-005, RT-006. Did **not** author WI-023 / `variants.py`. WI-025 allows this filing.
- Independence and Coverage Limitations: no 36-track replay; no `p1a_run`; no dense circle cover (RT-006 already has the directional exterior-drop counterexample). Hash check used `git show` bytes only.

## Follow-up and Closure

- Decision / Repair Owner and Review Reference: Technical Lead for RT7-F1 (hash bind) and RT7-F2 (test wording).
- Reproduction or Repair Evidence and Version: `5390c53a3a81d6c1b27f9b043f271382cdf065ed`.
- Red Team Recheck Result: first filing for WI-023 code.
- Remaining Findings or Disagreements: RT7-F1 MAJOR; RT7-F2 MINOR. Named geometry checks passed. `RT-002` F1 remains open.
- Strategist Closure Decision / Evidence / Affected Version Reference: not applicable (no CRITICAL; not model-family).
- Reopen Conditions: CLEAR150 emits 20×20 squares or a global-y snake; SCAN49 drops \(\|p\|>1800\); `scan.py` blob changes while still claiming BASE frozen; `C0Runner` default `plan` is not None; worlds hash still disagrees after an alleged freeze; 36-track saves treated as selection or independent P1-A; `RT-002` closed from these screens.

This record does not close `RT-002`. It does not select a model.
