# RT-008 — WI-024 Q2 wrap-up independent challenge

- Date: `2026-09-13`
- Reviewer Role: Independent Red Team
- Reviewed Target and Version: WI-024 result `882ba6fe3fd84459c64e2121e872a51cee35304a` on `feat/WI-024-q2-wrapup` (parent / WI-024 Execution Start `20a1b6146317115ca3dfee81e9d53e531bfa7997`; WI-024 Comparison Base `7cbeda3723e43d40623e0d0eaff5b634fb2ae36a`). This review's Execution Start `d9547cfc81de1827f5d4806f56e4eaa161ffea10` is **not** a review target.
- Finding Summary: one MAJOR residual on bound-ranking language (cross-resolution pool rank reversal of \(a=250\) vs \(a=500\); leftover “优于” / “三种分辨率下排序均确定”). C_in transcription, sampling-vs-proof labelling, pool-vs-lateral honesty, G14 omni/directional split, \(\bar J_2\)-not-error keys, isolated timing vs the 10 s cap, and version identity of the production table were independently confirmed. Highest severity applies to **claim wording**, not to the inner-domain identity.
- Highest Finding Severity: `MAJOR`

This record does **not** close `RT-002`. It does **not** write `paper/CLAIMS.md`. It is not model selection, not C1, not 150/49, and not a merge or push.

## Independent Scope

Challenge whether the **fixed** Q2 wrap-up at `882ba6fe` is a bounded, evidence-backed independent answer. In scope: first feasible region \(A_1\); \(C_{\rm in}\) (inequality, three-disk, sampled \(C_{\rm in}\subseteq C_{\rm sig}\)); near / direction / `no_signal`; \(\bar J_2\) as an upper bound vs realised error / relative-improvement misuse; pool vs lateral vs forward under one first observation; outer-relaxation sold as localisation advantage; timeouts / UNRESOLVED / isolated select vs 10 s; paper over-claims; code / config / results / chapter version identity.

Out of scope: editing `src/`; live simulator; `tests/p1a_run`; raising the 10 s cap; 150/49; C1; registering CLAIMS; closing `RT-002`; treating TR-029 `PASS` as correctness.

Method: `git cat-file -e` on Execution Start, review target, and Comparison Base. Extract blobs from `882ba6fe` into `%TEMP%/rt008-882ba6fe` (`git show` / `git cat-file blob`). Independently re-derive \(C_{\rm in}\) algebra from plan §4.1 **before** treating author tests as authority. Re-run `certifiable_candidates` on the extracted tree (`PYTHONPATH=src`, CPython 3.12.3). Do not check out `B题-q2` for writing. Do not use worktree `B题-redteam`.

## Git (this review)

| Item | Value |
|---|---|
| Worktree | `E:\pycharm\projects\pythonProject18\题目\B题-redteam-q2` |
| Branch | `review/RV-008-q2-wrapup` |
| HEAD / Execution Start | `d9547cfc81de1827f5d4806f56e4eaa161ffea10` |
| Comparison Base (WI-026) | `27bc08f25872d604768ccf09cfac58cb6560e488` (ancestor of HEAD) |
| Review target | `882ba6fe3fd84459c64e2121e872a51cee35304a` (**not** HEAD; not an ancestor of HEAD; on `feat/WI-024-q2-wrapup`) |
| WI-026 at HEAD | present |
| Worktree at start | clean |

Diff `20a1b61..882ba6fe` is exactly the eight WI-024 paths. No `scan.py`, `variants.py`, `tests/p1a_run`, 150/49, C1, or `paper/CLAIMS.md` edits.

## Attack Methods and Evidence

- Algebra: three closed disks of radius 1000 at \(S\) and \(S+1000\,t(\hat\theta\pm\delta)\) vs \(\|q\|\le1000\) and \(\|q\|^2\le 2000(a\cos\delta-|b|\sin\delta)\), using `perp` = left normal. Plan §4.1 \(C_{\rm in}\subseteq C_{\rm sig}\) argument (wedge min of \(q^\top e\), convexity of \(\|q-de\|^2\) in \(d\)).
- \(A_1\) outer: `OuterState` starts on \([-1800,1800]^2\); `keeps(direction)` drops \(d_{\min}>1500\) or \(d_{\max}\le 5\) or a failed wedge half-plane; `intersects_disk` drops cells wholly outside \(D\). Independent `build_a1_outer` at 4096/8 and 256/8.
- Covering intervals: 360 cells of \(1^\circ\), half-width \(1.5^\circ\); true \(\delta=1^\circ\) wedge is contained at the cell edge (boundary equality).
- Independent `certifiable_candidates` at 24×256 and one production 360×4096 call on the extracted tree. Compared to `q2_wrapup.json` bit-for-bit on bounds.
- Feedback: `second_feedback_status`; `successor_near` / `_dir` / `_no`; G14 directional back-side vs omni `CONFLICT`.
- Paper / wrap-up wording vs plan §4.3.4–4.3.6 (min upper bound is a selection rule, not a \(J_2\) ranking certificate; relative improvement needs \(\underline r(A_1)\), not an \(A_1\) upper bound).
- SHA-256 of raw git blobs vs `environment.file_sha256` (CRLF working-tree hashes).
- TR-029 was read last and not used as numeric authority.

Environment: Python 3.12.3, extracted tree `PYTHONPATH=src`. No simulator, no `p1a_run`.

## Findings and Counterexamples

### RT8-F1 — Bound order is not a certified \(J_2\) ranking; paper overstates rank stability

- Severity: `MAJOR` for the Q2 **chapter wording / comparator interpretation**. Not a \(C_{\rm in}\) identity failure. Not model-family.
- Evidence (independent, extracted `882ba6fe`):

| Resolution | \(a=250,\|b\|=500\) \(\bar J_2\) (m) | \(a=500,\|b\|=500\) (lateral ∈ pool) | \(a=750,\|b\|=500\) | forward \((500,0)\) |
|---|---:|---:|---:|---:|
| 24 × 256 (this review) | **247.6956790379326** | 261.67210405211813 | **164.14618401462312** | 514.0512204309631 |
| 360 × 4096 (this review) | 150.3109569827247 | **115.96234572049671** | **88.93905919223569** | 640.1913547550647 |

The \(a=250\) vs \(a=500\) bound-rank **reverses** across the two published discretisations. Selected family remains \(a=750\) (tie-break \(b=-500\)). Author JSON matches these numbers exactly.

Plan §4.3.4 selects \(\min \bar J_2\). \(\bar J_2\) is an **upper bound** on \(J_2\). A smaller upper bound does not certify a smaller \(J_2\). Plan §4.3.6 forbids using an \(A_1\) **upper** radius as a relative-improvement denominator; `relative_quality_certificate` exists in `q2.py` and is unused in the table. The published −23.3% (115.9623 → 88.9391) is a ratio of two upper bounds.

`paper/02_Q2.md` §2.3: “三种分辨率下排序均确定（重复运行排序哈希一致）”. There are **two** discretisations, not three. The parenthetical is repeat-run hash stability (`e9fee23ec248a949` / `e050d931e250485b`), which this review does not dispute. As written, the sentence is false or misleading about cross-resolution rank. The wrap-up test snapshot reports only “pool best / lateral / forward” and **omits** the test-resolution fact that \(a=250\) beats lateral \(a=500\).

§2.7: “优于池内成员侧移 115.9623 米 … 这些是上界意义下的比较”. The qualifier is present; “优于” still reads as a localisation advantage. Combined with the undisclosed 250↔500 reversal, a reader of the production table alone can take 88.9 vs 115.9 as a geometric fact.

- Expected: every comparator sentence is “smaller certified upper bound under frozen caps”; cross-resolution rank changes are reported; relative \(J_2\) improvement is either absent or uses \(\underline r(A_1)\).
- Actual: production table and “upper bound, not error” keys are honest; the rank reversal and “三种分辨率下排序均确定” are not.
- Affected claim: Q2 chapter §2.3 / §2.7 and wrap-up §8 as a **true-quality** or **stable-rank** statement. Does **not** reopen \(C_{\rm in}\subseteq C_{\rm sig}\) or the covering-wedge upper-bound argument.
- Required remediation: retract or rewrite the 三种分辨率 sentence; publish the 250↔500 reversal; keep “优于” only as 上界比较; do not register CLAIMS that \((750,-500)\) has smaller \(J_2\) than lateral. Code change is not required for this finding.

### RT8-F2 — Guard-branch tests over-claim what they check

- Severity: `MINOR`.
- Evidence: `tests/candidate/test_q2.py::test_empty_a1_returns_conflict_not_a_zero_score` calls `certifiable_candidates(..., intervals=4, max_leaves=4)` and accepts `OK`, `CONFLICT`, or `QUALITY_UNCERTIFIED`. Independent call: `status=OK`, `a1_leaves=4`, `selected=(500,0)` (forward). The empty-\(A_1\) `CONFLICT` branch in `certifiable_candidates` is not exercised. `test_ranking_is_by_bound_then_move_then_a_then_b` only asserts that the bound component is sorted, not the full \((\bar J_2, \mathrm{move}, a, b)\) lex order. Wrap-up / paper claim both guards have “direct tests”.
- Affected claim: test completeness of CONFLICT / lex ranking, not the production table (lex order on the eight records is correct in code and in this review's ranking).
- Required remediation: a call that actually empties `a1.leaves`, and a ranking assertion that includes move/\(a\)/\(b\). Not a wrap-up blocker by itself.

### RT8-F3 — Evidence JSON hashes are CRLF working-tree hashes at a dirty parent HEAD

- Severity: `MINOR` provenance.
- Evidence: `q2_wrapup.json` `environment.head_at_generation = 20a1b614…`, `worktree_dirty_at_generation = true`. `file_sha256` for `q2.py` is `38b634302fe56aa7…`. Raw git blob `882ba6fe:src/candidate/q2.py` SHA-256 is `da811511ca2de86e…` (LF). Replacing LF with CRLF **matches** the JSON hashes for `q2.py`, `cells.py`, `geo.py`, `observe.py`. `cells.py` / `geo.py` / `observe.py` blobs are unchanged from the parent. Independent production bounds match the JSON, so the generator ran the wrap-up `q2.py`, not a different algorithm.
- Affected claim: “SHA-256 of the modules used” as a git-blob pin. Not the numeric table.
- Required remediation: record git blob ids or hash `git cat-file blob`; generate from a clean HEAD that contains the code.

## Named challenges (independent results)

Target `882ba6fe3fd84459c64e2121e872a51cee35304a`. Extracted tree; one production select wall 7.531 s on this machine (unreplicated; does **not** adjudicate OPEN-3).

### 1. First feasible region \(A_1\) — **matches plan §4.1 as an outer approximation**

`build_a1_outer` applies one direction feedback at \(S\) then refines. Filter implements \(D\cap W_1\cap\{5<r\le 1500\}\) conservatively (cells that only partially meet a constraint are kept).

| Check | Independent result |
|---|---|
| 4096/8 leaves, bbox, \(\bar r\) | 316; `[-14.0625, -28.125, 1504.6875, 28.125]`; 759.8956548434793 m |
| G09 worlds retained | `(5+ε_d,0)`, `(1000,0)`, `(1500,0)` all true |
| G10 \(S=(800,0)\), \(\hat\theta=\pm 1^\circ\), \(g=(1800,0)\) | retained (256 leaves) |
| \(y\)-extent vs \(\tan 1^\circ\cdot 1500\approx 26.18\) | bbox \(\lvert y\rvert=28.125\) is a coarse-cell outer |

### 2. \(C_{\rm in}\) — **identity holds; sampling is not a proof; three-disk is a transcription**

Algebra: with left-normal \(t_\perp\), \(\|q-1000 t(\hat\theta\pm\delta)\|\le 1000\) iff \(\|q\|^2\le 2000(a\cos\delta\pm b\sin\delta)\), hence iff \(\|q\|^2\le 2000(a\cos\delta-\lvert b\rvert\sin\delta)\), plus \(\|q\|\le 1000\). Plan §4.1 containment argument is valid (\(q^\top e\ge m\); \(\|p-g\|^2\) convex in \(d\); endpoints \(\le R_*(g)^2\)).

All six pool points, lateral \((500,500)\), and forward \((500,0)\) satisfy **both** transcriptions. Forward is not in the pool. Evaluator `c_in_member` is the same inequality; wrap-up and paper §2.0.7 disclose same-source agreement. Sampled 512200-pair check is labelled “not a proof”. Equality \(C_{\rm in}=C_{\rm sig}\) is not claimed.

### 3. near / direction / `no_signal` — **complete enough for Q2 omni; G14 not applied**

- `keeps(direction)` uses 5–1500 and the forward wedge; `keeps(near)` is \(d_{\min}\le 5\) (closed). G13 labels `near, near, direction` at \(5-\varepsilon_d, 5, 5+\varepsilon_d\) are the catalog contract.
- Omni `no_signal` at a certified \(C_{\rm in}\) point → `CONFLICT`. Directional `no_signal` → `OK`, `c_in_applies=false` (G14). Positive feedback outside \(C_{\rm in}\) is not a conflict.
- \(\bar J_2\) maximises **direction-interval** box radii only. Plan §4.2 sets \(j=0\) on near, so omitting near cannot raise the max. Forward near outer \(\bar r\approx 15.72\) m \(\ll 640\). True `no_signal` is empty on \(C_{\rm in}\); omitting it from \(J_2\) is the plan definition.
- Supporting (not a second finding): finite `successor_no` is nonempty for 6/8 candidates. Independent: at \((250,-500)\), no_signal outer \(\bar r=205.8367664292813\) m **exceeds** that point's direction \(\bar J_2=150.31\) m; at the selected \((750,-500)\) the no_signal outer is empty. Wrap-up already labels this “unproven empty”, not a conflict. `test_no_signal_successor_is_not_empty_inside_c_in` asserts the outer artefact; the name is easy to misread.

### 4–5. \(\bar J_2\) as quality — **not sold as realised error; leftover superiority language is F1**

Result dict has no `error` / `actual` / `precision` / `optimal` / `clear` key. Every published bound \(\gg 20\) m; no two-measure clear claim. Exact-angle filter can exceed the certified interval max **only for forward** (JSON 710.47 vs 640.19); pool exact-angle maxima are 13–21 m **smaller**. Dropping a cell only when the whole cell fails a half-plane is still sound; extra kept cells need not be monotone in wedge width. Sampling of 386 grid points is not a continuous proof; the covering-wedge inclusion is the argument. OPEN-2 is a modelling change, not a silent tuning.

Illegal relative improvement via \(A_1\) **upper** radius 759.9 m is **not** used. The remaining hazard is F1 (two upper bounds compared as if they ranked \(J_2\)).

### 6. Three methods, same first observation / evaluation / resource — **yes**

One `build_a1_outer`, one interval count, one leaf cap. Lateral is a second tag on a pool point (duplicate evaluation, same numbers). Forward is the only extra geometry.

### 7. Outer relaxation vs “real” localisation advantage — **see F1**

Selected \(a=750\) is stable at both published caps. Intermediate pool order is not. Do not promote 88.9 vs 115.9 m as a localisation theorem.

### 8. Timeouts / UNRESOLVED / isolated vs 10 s — **disclosed; not a geometry fail**

JSON isolated production walls 9.965 / 10.389 / 10.088 s; test 0.595–0.615 s. Machine, CPU load 3/3/6%, python.exe 1/1/2, WI-023 not observed. Cap **not** raised; `p1a_run` **not** run. G09/G11 10 s is budget/implementation of \(O(6\cdot 360\cdot M)\), as WI-026 required. OPEN-1 / OPEN-3 remain UNRESOLVED. This review's single 7.531 s production call is not a timing study.

### 9. Paper over-claims — **global opt / two-measure / independent eval / universal superiority not claimed; F1 wording remains**

§2.0 lists the required non-claims. Evaluator independence is disclosed as absent. “闭合” in §2.7 is stronger than OPEN-1/2/3; that is a local tone issue under F1, not a hidden global-optimum claim.

### 10. Version identity — **numeric table matches this commit; JSON pin is F3**

Independent production and test bounds match `q2_wrapup.json` and `paper/02_Q2.md` / wrap-up tables. Chapter is labelled 草稿, not CLAIMS. Provenance pin is F3.

## Reproduction Instructions

From a Red Team worktree, do not check out `feat/WI-024-q2-wrapup` for writing:

```text
git cat-file -e 882ba6fe3fd84459c64e2121e872a51cee35304a^{commit}
git cat-file blob 882ba6fe:src/candidate/q2.py   # extract the eight WI-024 paths plus cells/geo
PYTHONPATH=<extract>/src python -c "... certifiable_candidates(S,0, intervals=24 then 360) ..."
```

Confirm: six pool + lateral + forward inside both \(C_{\rm in}\) transcriptions; lateral ∈ pool; forward ∉ pool; 250 vs 500 rank reversal; omni `no_signal` at forward is `CONFLICT`; directional is not; production bounds 88.93905919223569 / 115.96234572049671 / 640.1913547550647. Do not treat unittest green as the proof.

## Steelman

1. \(A_1\), \(C_{\rm in}\), and the covering-wedge \(\bar J_2\) construction follow plan §4 with conservative cells.
2. Non-claims in §2.0 are the right list; three-disk vs inequality is labelled a transcription; sampling is labelled not a proof.
3. Pool-vs-lateral is disclosed as within-pool; movement trade-off is disclosed; \(\pm b\) symmetry is disclosed.
4. G14 is not given the omni receive guarantee.
5. Isolated ~10 s vs the harness cap is recorded as budget, cap not raised.
6. No global optimum, no two-measure clear, no CLAIMS registration, no 150/49 / C1 / `p1a_run` edits.

## Strongest Counter-Argument

1. Minimising an upper bound does not minimise \(J_2\); the published 250↔500 reversal is a concrete demonstration that bound order is discretisation-dependent.
2. “三种分辨率下排序均确定” is not true of discretisations; the test snapshot hides \(a=250\) beating lateral at 24×256.
3. “优于侧移” can still be read as quality even after “上界意义下”.
4. Exact-angle vs interval non-monotonicity means the reported \(\bar J_2\) is one conservative number among several, not a tight envelope.
5. Empty-\(A_1\) CONFLICT is untested; JSON SHA pins CRLF checkouts at a dirty parent HEAD.
6. None of this makes \(\bar J_2\) a realised error, and none of it falsifies \(C_{\rm in}\subseteq C_{\rm sig}\).

## What may be retained

- \(A_1\) outer of the first direction, with G09/G10 retention.
- \(C_{\rm in}\) as a sufficient inner domain, two transcriptions, six certified pool points, forward certified and not in the pool, lateral ∈ pool.
- Plan §4.1 proof of \(C_{\rm in}\subseteq C_{\rm sig}\); sampled grid as support only.
- Successor definitions; omni `no_signal` at certified \(C_{\rm in}\) is `CONFLICT`; G14 directional exclusion.
- \(\bar J_2\) as a covering-interval box-radius **upper bound** under frozen 360×4096 (and the 24×256 snapshot), selection rule min bound then move then \(a\) then \(b\), `QUALITY_UNCERTIFIED` / `CONFLICT` sentinels in code.
- Honest local comparison **as a bound table**, including “pool cannot lose to lateral by construction”.
- Isolated `certifiable_candidates` timing as a budget fact; 10 s cap not a geometry oracle.
- Draft status of `paper/02_Q2.md`; no CLAIMS registration.

## What must be narrowed

- “三种分辨率下排序均确定” → three **repeats**, not three resolutions; report the 250↔500 reversal.
- “优于侧移” / −23.3% → smaller \(\bar J_2\) under named caps, not smaller \(J_2\), not a certified relative improvement.
- “数学与实现上闭合” → finite answer with OPEN-1/2/3 still open.
- Do not cite three-disk agreement or 512200 sampled pairs as the proof of \(C_{\rm in}\subseteq C_{\rm sig}\).
- Do not cite isolated 9.97–10.39 s as a stable `p1a_run` PASS.

## Does this block Q2 chapter close-out?

**Yes, as currently worded, for a citable independent answer.** F1 requires a paper / wrap-up wording repair (and an explicit rank-reversal sentence) before the chapter is treated as closed. It does **not** invalidate the inner-domain mathematics or require a different selected point under the frozen rule. It is not CRITICAL and does not block unrelated Q3/Q4 C0 work. Do not register `paper/CLAIMS.md`. Do not close `RT-002`.

## Required Handoff

Technical Lead: record the review of `882ba6fe`; repair F1 wording on a Q2 worktree if the chapter is to be cited; F2/F3 are local. External Strategist: not required to keep Q2 as a bounded finite answer; required if someone wants to treat \(\bar J_2\) as realised error, claim \(J_2(750)<J_2(\mathrm{lateral})\), open OPEN-2 as a model change, or promote the table into CLAIMS. User: only if chapter close-out is declared despite F1. Do not close `RT-002`.

## Participation and Review Limits

- Reviewer Prior Participation: this conversation did **not** author WI-024, `src/candidate/q2.py`, Q2 tests, EXP-006, or `paper/02_Q2.md` (WI-024 author: Codex / `deepseek-flash`). It did not implement the wrap-up repair. Prior Red Team records on this worktree lineage — RT-004 (C0 candidate / `cells.py`), RT-005 (snake), RT-006 (EXP-005 150/49 math) — are disclosed as required by WI-026; they are different targets. RT-004 inspected `cells.py`, which Q2 imports; this review re-read `cells.py` from `882ba6fe` and re-derived the drop rule. A new conversation does not create independence; absence of Q2 authorship is the independence fact.
- Independence and Coverage Limitations: production \(\bar J_2\) independently recomputed once (not three timed repeats). Containment of true feasible points was not re-gridded beyond the author 386-point lemma (algebra of the covering wedge was re-derived). No live harness. Sampling is not a continuous proof.

## Follow-up and Closure

- Decision / Repair Owner and Review Reference: Technical Lead for F1 wording and F2/F3 hygiene; Strategist only if F1 is escalated into a \(J_2\)-ranking or OPEN-2 model change.
- Reproduction or Repair Evidence and Version: this record vs `882ba6fe3fd84459c64e2121e872a51cee35304a`. Recheck a repaired paper/wrap-up commit; do not silently amend `882ba6fe`.
- Red Team Recheck Result: first filing.
- Remaining Findings or Disagreements: F1 open; F2–F3 open as MINOR. No CRITICAL.
- Strategist Closure Decision / Evidence / Affected Version Reference: not applicable (no CRITICAL). F1 is not closed by TR-029 `PASS`.
- Reopen Conditions: registering CLAIMS that \(\bar J_2\) is error or that \((750,-500)\) is \(J_2\)-better than lateral; claiming \(C_{\rm in}=C_{\rm sig}\) from sampling or three-disk agreement; treating isolated ~10 s as harness PASS or raising the 10 s cap without a recorded decision; applying Q2 omni receive to G14 directional `no_signal`; using an \(A_1\) **upper** radius as a relative-improvement denominator; closing `RT-002` from this review.

This record does not close `RT-002`. It does not select a model. It does not authorize `src/` work. It does not push.
