# TR-028 — WI-023 cover-variant implementation and screens

- Date: 2026-09-13
- Reviewer Role: Technical Lead
- Reviewed Commit: `5390c53a3a81d6c1b27f9b043f271382cdf065ed`
- Disposition: **PASS** of implementation + offline 36-track screens. **Not a selection.** BASE remains default. Not live, not C1, not formal.

## Git

Parent = Execution Start `6a8e904be1fc345b33f175e6d30750218d3b28a6`. Comparison Base `7cbeda` is an ancestor. `scan.py` still `32dd26f7…`. New `variants.py`; `model.py` additive `plan=` default None. Paths match WI-023. Result **not** on `main`, not pushed.

## Screens (pre-registered, not moved)

36/36 COMPLETE, 0 false-complete, wall ≪ 40 s. Holdout median save: CLEAR150 **0.93 % FAIL**; SCAN49 **22.00 % PASS**; COMBINED **25.09 % PASS**. Worst Tv/BASE **1.001**.

Q3 only has CLEAR150: holdout saves 12.0 / 9.1 / 0.1 % → median **9.1 % FAIL**. Q4 SCAN49/COMBINED pass.

**Technical recommendation (screens only):** keep **BASE** as default. For a later named freeze, Q3 stays BASE; Q4 may use SCAN49 or COMBINED (COMBINED slightly larger median save; SCAN49 simpler). Do **not** adopt CLEAR150 alone. This is not model selection.

## C1 note

Remaining cost is failed clears + intra-clear + inter-channel, not unused scan after SCAN49. C1 is **not** opened: no real-time qualification, no extra-measure experiment, SR-002 still closed. Verdict: **暂不需要** this round; failed-clear walk is a possible later fusion target, not a C1 WI.

## Residuals

WI-022 `expectedFailure` still in `tests/c0_baseline`. P1-A 10 s cap still load-sensitive (this run G09/G11 ~7.5 s). Offline mock ≠ HTTP.

## Progress briefing

- Established: four tags exist; 36 synthetic tracks; screens as above.
- Unverified independently: Red Team of `5390c53` (20×30, exterior 49, BASE default path).
- Red Team: **now**, target `5390c53`. Not RT-006 math, not old snake, not Q2.
- Strategist: **optional after RT** if adopting COMBINED/SCAN49 as the contest Q4 program (resource/config, not family). Not required to archive this PASS.
- Next: WI-025 Red Team. Live Phase 5 only after that and a named freeze. ~6.3 h to 17:30 new-test start. Q2 (WI-024) stays on `B题-q2`.

No push.
