# TR-027 — Q2 wrap-up inventory and WI-024 issuance

- Date: 2026-09-13
- Reviewer Role: Technical Lead
- Disposition: inventory only; **WI-024 issued**. Independent worktree `B题-q2`. Does not touch WI-023. Not C1.

## Executor occupancy

`B题-executor` is on `feat/WI-023-cover-variants` at `6a8e904` with **uncommitted** `src/candidate/model.py` and untracked `variants.py`. Q2 **must not** use that worktree.

## Q2 completion table (before WI-024)

| Item | Status |
|---|---|
| A1, C_in, 6-pool, lateral (500,500), forward (500,0) | Implemented `src/candidate/q2.py`; unit tests `tests/candidate/test_q2.py` (reduced 24×256) |
| C_in ⊆ C_sig proof | Plan §4.1 text; numeric membership tests; **no** dedicated paper write-up |
| Successor A2 near/dir/no | Implemented; some observe tests |
| Finite solver 360×1° , r_bar, J2 upper bound, tie-break | Implemented at production caps; **reduced** in candidate unit tests |
| G09–G14 local compare vs pool/lateral/forward | `tests/p1a_run/checks.py` + fixtures; **not** a standalone Q2 report |
| G09/G11 10 s cap | Harness runs **production** 360×4096 **plus** evaluator. Isolated ~9.2–9.5 s; under load G11 10.4 s → UNRESOLVED. **Checker wall-clock**, not a geometry fail. Do not raise cap. Need **isolated select-time** vs harness time. |
| Paper Q2 chapter / CLAIMS | Missing (`paper/CLAIMS.md` empty) |
| Claim: not globally optimal; bounds are upper bounds | Code comments; not a user-facing Q2 answer |

## G09/G11 root cause (no math change)

`p1a_run` G09/G11 call `q2.certifiable_candidates(..., intervals=360, max_leaves=4096)`. O(6·360·M) with M≤4096 plus evaluator. 10 s is a **harness** budget. WI-024 must **not** edit `tests/p1a_run` (WI-016/023 surface). Record isolated timings in `tests/q2/`. Optional later harness WI.

## Progress briefing

- Established: Q2 solver exists on BASE tree `7cbeda`.
- Missing: wrap-up report, isolated timing, honest G09–G14 table, paper draft.
- Red Team: after WI-024 result (Q2 only).
- Strategist: not to start WI-024.
- Next: new Executor conversation on `B题-q2`. WI-023 continues on `B题-executor` without Q2 files.

No push. No live Q3/Q4. No 150/49 edits in this WI.
