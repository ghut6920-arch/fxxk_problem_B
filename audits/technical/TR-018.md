# TR-018 — WI-017 P1-B adapter and C0 practice

- Date: 2026-09-13
- Reviewer Role: Technical Lead
- Work Item: `work/WI-017.md`
- Reviewed Commit: `a48c77cc0d46a81b96aa56f54197b8837c4c735c`
- Disposition: **PASS** for P1-B mock + two operator-confirmed **practice** observations. Conclusion `P1B_PASS_PRACTICE_OBSERVED` accepted under D-006/SR-002. Not formal qualification, not C1, not `RT-002` closure, not independent evaluation, not a push.

## Scope

Parent / Execution Start `a4def4e4f834034fbfd1ee19940069695ada85d5`. Comparison Base `761c0f7788ed773d2a132b92caaa9e1378b22c88` is an ancestor. Paths are only `src/protocol/`, `tests/p1b/`, `scripts/run_c0_practice.py`, `evidence/experiments/EXP-003/`. `src/candidate/`, evaluator, fixtures unchanged.

User later stated practice attempts are unlimited. This WI still ran **one Q3 and one Q4**, matching the WI cap. Extra practices are not required for this PASS. Unlimited practice does **not** close RT-002 F1 (formal readiness still needs repeated **stressed** evidence).

## Evidence

- Python 3.12.3, Executor worktree: `tests/p1b` 60 OK 3.57 s; `tests/p1a` 50 OK 0.102 s. No `evaluator` import under `src/protocol/`.
- Live logs independently summarized: Q3 2027 requests (`/enter` 1, `/measure` 180, `/clear` 1845, `/exit` 1), all HTTP 200 / `accepted=true`, attempts=1; enter `remaining_real_duration_s=650`, exit `virtual_time_s=38510.900536`. Q4 3319 requests (1 / 1620 / 1697 / 1), same 200/accepted pattern; enter remaining 1200, exit vt 51898.355196. Wall ~25.8 s / ~48 s on loopback.
- Report wording: origin user-guaranteed; mode operator-declared (interface has no mode field); `N` not from UI; adaptive actions 0; unknown-accept 0. `--confirm-practice` gate exists; `--mode formal` refused.

## Findings

No FIX inside WI-017. Residuals:

1. `COMPLETE` / `K=12` is the candidate certificate plus successful clears. True `N` was not read from the rehearsal UI. Do not write “cleared all official sources.”
2. Live path never exercised retry / 500 / unknown-accept; those exist only in mock T11–T12.
3. Loopback ~13 ms/request and large remaining real time **do not** certify a 20-minute networked formal slot (RT-002 F1).
4. D-004 same-model; D-006 origin waiver. Request JSON logs are large (~1.7 / 2.7 MB) but in-scope.

## Modeling (no C1)

On this machine and loopback, C0 finished both practices with large real-time margin and virtual time ≪ 360000 s. That is **not** a reason to open C1. It is a reason **not** to change C0 scan/clear geometry from these two quiet sessions. If the UI later shows `K < N`, treat as implementation/coverage follow-up, not model-family failure, until a theoretical counterexample exists.

## Progress briefing

- Red Team: **not required to accept this WI**. Named later gate if promoting these numbers into the paper beyond “practice observation”: `a48c77c` (mode/N/unknown-accept). Formal readiness still needs stressed repeats (RT-002), not more quiet loopback copies.
- Strategist: **not needed**. SR-002 C1/formal REJECT stands.
- TL adjustments: none to mathematics. Record practice-unlimited as a user fact; do not treat it as formal-slot abundance.
- Next: user may check the simulator UI for revealed `N` after those two exits; optional more **stressed** practice later; no formal test; no C1 WI.

## Follow-up

WI-017 **COMPLETE**. Candidate/protocol remain unmerged, unpushed.

## Operator UI reveal (2026-09-13)

The user reported the rehearsal completion UI for **these two sessions**:

| Session | Revealed `N` | Types |
|---|---|---|
| Q3 practice (`a48c77c` log, `K=12`) | 12 | 12 omnidirectional |
| Q4 practice (`a48c77c` log, `K=12`) | 12 | 2 omnidirectional, 10 directional |

`K=N=12` on both. Q4 mixed types satisfy `1 ≤ N_dir ≤ N−1`. This is **operator-reported UI truth**, not independently read from the simulator process. It supports the candidate `COMPLETE` certificate **for these two practice cases only**. It does not prove all official cases, close `RT-002`, or authorize a formal slot. C1 stays closed.
