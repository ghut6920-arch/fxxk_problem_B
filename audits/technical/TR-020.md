# TR-020 — WI-018 C0 local clear-rectangle snake repair

- Date: 2026-09-13
- Reviewer Role: Technical Lead
- Work Item: `work/WI-018.md`
- Reviewed Commit: `d675831695d705ea8c4ffcb74fe8c83277847daf`
- Disposition: **PASS** for restoring local-index snake then rotate. Implementation only. Not C1, not live re-rehearsal, not selection.

## Git

Parent / Execution Start `4d24d09e4985fe7aef4da0eb7d1efaaf53dd5a11`. Comparison Base `a48c77cc0d46a81b96aa56f54197b8837c4c735c` is an ancestor. Four authorized paths only. Worktree clean at the result.

## Evidence

Independent: `tests/candidate` 214 OK 6.6 s; `tests/p1a` 50 OK; `tests/p1a_run` 14 OK 34.3 s. At `S=(0,0)`, `θ=45°`: 225 points, max consecutive **20.000** m, 0 steps >22 m, path **4480** m; **same set** as global-y snake, **different sequence**. `local_core_snake()` is 225 cores, even rows increasing `i`, odd decreasing.

G16 harness now walks consecutive distances at 45°/90°/123.456°. Author ran the new tests against archived `a48c77c` candidate tree: 8 fail + 1 error; repaired 19 OK. That archive check is author-side; Red Team must still independently confirm both commits.

## `local_core_snake`

Public helper added in authorized `scan.py`. It only permutes `(i,j)` before the existing formula. Accepted as in-scope test/repair surface, **not** a change of 225-set, spacing, or C0 mathematics. Callers of `clear_rectangle_centres` need not use it. Residual: frozen candidate public API grew by one function; do not treat it as a new planning primitive.

## Boundaries

Practice `K=N=12` and P1-A set-cover PASS are not rewritten. `V_src` `224·22` now applies to **this** sequence, not to unrepaired logs. No 81→49 / 225→150.

## Progress briefing

- Established: repair restores plan §5.2 order on the 45° counterexample.
- Unverified independently: author’s `git archive a48c77c` failure table (Red Team).
- Red Team: **now**, targets `a48c77c` (long steps) and `d675831` (gone; set unchanged; G16 still passes; no point-count change). Repair author must not do it.
- Strategist: not for this repair.
- Next: user sends the Red Team prompt. No merge/push/C1/simulator from this PASS.

## Follow-up

WI-018 **COMPLETE** pending Red Team record. Candidate remains unmerged.
