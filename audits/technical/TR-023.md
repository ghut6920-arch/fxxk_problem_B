# TR-023 — WI-020 repaired C0 (B) baseline

- Date: 2026-09-13
- Reviewer Role: Technical Lead
- Reviewed Commit: `7f4dfba66a1467da865e411c2aa03bbec2203843`
- Disposition: **PASS** for Phase 1 gate. Conclusion `B_BASELINE_READY` accepted with residuals. Not Phase 5, not 150/49 implementation, not C1, not formal.

## Git

Parent = Execution Start `4e5e0af7e7b67acab7bef242a07b5f16ee2c746a`. `scan.py` blob still `32dd26f7fccf8b2d50f2af349251848cea1c39fc`. Paths only WI-020-authorized (`src/protocol/session.py`, tests/c0_baseline, scripts, EXP-004). No `src/candidate/` edits.

Independent: `tests/c0_baseline` 46 OK, 5.8 s (Python 3.12.3).

## Gate vs WI-020

| Gate | Result |
|---|---|
| Mock Q3/Q4 full flow | COMPLETE; N=16 action counts 3780/5220 |
| Fail-closed unknown-accept / deadline | no certificate |
| False-complete / truth-exclusion / unsafe-cancel | 0; adaptive 0 |
| Itemized costs including origin→first | present; mock residuals <1e-6 s |
| Intra-clear on B | mock and live max step **20.000000** m (≤22) |
| Live | 2 practices, no third; operator `--confirm-session-problem`; no formal |
| Pairing vs `a48c77c` | explicitly not claimed |

Live: Q3 K=14, Tv=16537.6 s, wall ~10.9 s, remaining 1189 s; Q4 K=16, Tv=37326.2 s, wall ~32.9 s, remaining 1167 s. **N/types not operator-UI reported** — COMPLETE is the candidate certificate, not UI truth.

## Residuals (not FIX of B)

1. `tests/p1a_run` 10 s/item cap is load-sensitive (G09/G11 ~9.2–9.5 s isolated; under load G11 10.4 s → UNRESOLVED). Isolated `run_suite()` still `P1A_PROPERTIES_PASS`, `checks_failed=0`. **Do not raise the cap to obtain a green command.** Optional later harness WI. Not a candidate defect.
2. Same `robot_id` `202619007369` reused after per-session confirm flags — process residual, not a geometry fail.
3. Live N unknown until the user reports the UI.

## Dynamic adjustment

- **No** 150/49 code. **No** C1. **No** cap change. **No** pairing old/new practices as snake gain.
- Phase 1 gate **met** for starting **Phase 2 math+RT only**.
- Clock at this review ~04:03 Beijing; ~13.4 h until 17:30 new-test **start** cutoff. Keep the Phase 0 rule: if Phase 2+3 would consume the live-start reserve, drop optimization and qualify B only.

## Progress briefing

- Established: B at `d675831`/`7f4dfba` has mock e2e + two practices with 20 m intra-clear steps and itemized costs.
- Unverified: UI `N`; stressed/repeated rehearsals; 150/49 cover proofs.
- Red Team: **now for Phase 2** — challenge the written 150/49 **math spec**, not another snake table, not this live log as Phase 5.
- Strategist: **not** to start Phase 2 specs. Needed only if RT rejects the 150/49 geometry or time forces dropping Phase 3.
- Next: WI-021 Red Team on EXP-005 spec. Executor does **not** implement 150/49 until that RT returns.

## Follow-up

WI-020 **COMPLETE**. Phase 5 does not inherit these two lives as the 3-session pack.
