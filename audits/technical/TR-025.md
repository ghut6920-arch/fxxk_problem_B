# TR-025 — WI-022 Q3 14/16 miss diagnosis

- Date: 2026-09-13
- Reviewer Role: Technical Lead
- Reviewed Commit: `7cbeda3723e43d40623e0d0eaff5b634fb2ae36a`
- Disposition: **PASS** of the diagnosis (`Q3_MISS_EXPLAINED_OPEN`). **No candidate patch.** Not model-family. Not 150/49 code. Not `RT-002` closure.

## Git

Parent = Execution Start `01a3163e0170a42f9684288b6e3e482c551dfdf6`. Paths: `tests/c0_baseline/replay.py`, `test_q3_miss.py`, `evidence/experiments/EXP-004/Q3_MISS.md` only. `scan.py` blob still `32dd26f7fccf8b2d50f2af349251848cea1c39fc`. Independent: `tests/c0_baseline` 59 OK (1 expectedFailure), 9.7 s.

## Independent checks (not copied from the report)

- Frozen log: 180 `/measure`, COMPLETE, K=14, `no_source=[1,5,6,10,16,20]`.
- P3 cover on a 20 m grid of `D=B(0,1800)`: max distance to nearest lattice point **989.9495 m** `=700√2 < 1000`. Official `R_c∈[1000,1500]`, `g∈D` (`problem/RULES.md`).
- Therefore a **legal** omni source in D must produce a positive at some P3 point. Full 9-point silence on a channel is **incompatible with a legal source on that channel**.
- Plan §6 no-source after full cover + `quantity_conflict` with |F|=14 in [10,16] ⇒ COMPLETE is **faithful**. The live event is a **false absence certificate relative to UI N**, not a truncated scan and not a P3 hole.

The report’s favoured E2 (two `R_c<1000`) vs E1 (`|g|≳2227`) is **not settled**. Interface does not return `R_c`. Located sources from bearings lie in D; that does not locate the two silent channels. Do not treat E2 as proven.

## Decisions on the three handoff questions

1. **Operator radii.** Official interface does **not** return receive radius. If the practice UI happens to list them, report them; **do not block** the diagnosis on that. Radii ≥1000 and `g∈D` for a silent channel would escalate (simulator vs RULES).
2. **COMPLETE meaning.** **(a)** Adopt as documentation of the **existing** plan: COMPLETE is under `g∈D`, `R_c∈[1000,1500]`. Not a new model. **(b)** Hidden truth is not available pre-exit; do not invent a legality oracle. Formal cases are assumed generated under RULES. **(c)** Refuse COMPLETE without N — **not this round** (N is post-exit; changing the certificate is a spec change → Strategist if the user wants it).
3. **Formal risk.** On a **legal** official Q3 world, this miss **should not** occur if P3 cover and the adapter hold. Practice **can** still emit off-contract worlds; this session is one observation, not a proof the generator is illegal.

`@expectedFailure` is accepted as a recorded conservative gap, not a silent PASS of a guard.

## Dynamic adjustment

- Do **not** patch C0 geometry or add a COMPLETE guard in this round.
- Do **not** reopen C0 as model-family failure.
- Q3 14/16 no longer blocks **legal-world** use of B; it **does** block claiming “this practice cleared all UI sources.”
- 150/49 **code** still not issued here (RT-006 KEEP is math only). Phase 3 waits for explicit next authorization after this diagnosis.
- C1 still closed.

## Progress briefing

- Established: mechanism = full P3 silence → plan no-source → COMPLETE; P3 cover of legal D holds on a 20 m grid / `700√2`.
- Unverified: the two silent sources’ true `(g,R_c)`; simulator generator legality.
- Red Team: **not required to accept this WI**. Named later gate if the paper says “legal Q3 cannot miss”: target `7cbeda` + frozen log, not a new snake table.
- Strategist: **not** unless the user wants option (c).
- Next: user chooses Phase 3 (150/49 implementation under RT-006 hazards) vs packing B as legal-world candidate vs optional RT of this diagnosis.

## Follow-up

WI-022 **COMPLETE**. `src/candidate/` unchanged.
