# TR-012 — WI-014 evaluator and fixtures

- Date: 2026-09-12
- Reviewer: Codex Technical Lead (B题/main); not the evaluator author
- Disposition: **FIX**; implementation/configuration defects, with catalog interpretations escalated below
- Fixed result: `a49cc64827cc398c44a41d4094776edddb41f79f`
- Comparison Base: `f69f2c670827fc807a124945bef280fb0907775d`
- Execution Start: `e9b94a298c220421ca4fab3d70d5e607f71f7983`
- Lead review start: `4065724d5b9f7bcc7caba69f8bcaf4742544b0c9`
- Executor: `E:/pycharm/projects/pythonProject18/题目/B题-executor`, `feat/WI-014-p1a-evaluator`

## Evidence checked

Commit-object checks for result/base/start passed. Result parent equals Execution Start; Comparison Base is an ancestor. Executor HEAD equals the fixed result and status was clean before/after tests. All 34 changed files are in the three WI-authorized paths; no candidate tree exists. Committed-range diff check passed. No merge/history change was performed.

PowerShell, Python 3.12.3, PYTHONPATH=src, PYTHONDONTWRITEBYTECODE=1: `python -m unittest discover -s tests/p1a -v` passed 11 tests in 0.058 s. Independently hashed all 26 committed fixture blobs: zero manifest mismatches. Inspected all 26 numeric input records against the catalog, with plan §§2–5 and design §4.5 as predicate references. O-03 is preserved. This is evaluator self-testing, not a candidate property run.

Executor reports no installation, network, simulator or push. Artifact scope was verified; historical actions were not observed live and no remote fetch was made. Author identity is self-reported as `executor-B题`, model `deepseek-flash`; retain the independence bind and verify actual participation before candidate assignment. Git author name alone does not establish independence.

## Findings and required actions

### F1 — Closed directional boundary rejected

`predicates.py:103`: source g=(700,700), phi=90 degrees, R_c=1500, sensor=(0,700). Exact normal (0,1) gives dot product zero and distance 700, so expected `direction`; actual `no_signal`. Floating cosine residue causes the rejection. G15 only checks the opposite side of this boundary. Repair using justified analytic/certified arithmetic and focused boundary regressions; do not widen official visibility with a loose tolerance.

### F2 — NaN falsely passes numeric comparison

`selfcheck.py:473`: `compare(1.0,float('nan'))` returns `[]`. Reject nonfinite actual values when finite values are expected, with NaN/infinity regressions. Current finite fixtures do not validate this comparator.

### F3 — G07 invents a certification threshold

`halfplane.py:31,196` and G07 truth/candidate-later text reinterpret 1-degree feedback bins as geometric precision limits. Plan §3 allows uncertainty when reliable classification is unavailable; plan §4.3 feedback partitioning is not a precision limit. The wedge routine ignores sensor positions and otherwise returns BOUNDED without certification.

For frozen G07b, q=(2000,0) is feasible and q+t(1,0) remains feasible for all t>=0: bearing 0 lies inside both [-1,1] and [-0.99,1.01]. Thus an unboundedness witness exists. Remove unsupported precision claims; do not silently overwrite frozen expectations. Keep the affected reference OPEN pending the interpretation decision below. This is a review proof, not a candidate run or rejection of the model family.

### F4 — G15 replaces heading perturbation with position perturbation

All directional variants keep phi=90 and vary g_y. Catalog requests heading perturbation but defines epsilon_d in metres: an upstream dimensional ambiguity. Executor must not silently substitute tests. Preserve evidence and keep the affected item OPEN until a recorded clarification supplies the heading unit/numeric instance. No Executor catalog edit is authorized.

### F5 — Checkout changes hashed fixture bytes

Committed hashes match. However, `git cat-file --filters a49cc64827cc398c44a41d4094776edddb41f79f:tests/p1a/fixtures/G01.json` produces 21 CRLF sequences and a different SHA-256 from the manifest, reproducing the report's disclosed caveat. A scoped `tests/p1a/.gitattributes` file can protect fixture JSON bytes within existing allowed paths. Verify committed and checkout-filtered bytes; do not change global Git configuration.

### F6 — Incomplete A1 helper advertised as full membership

`predicates.py:160` checks disk/radius but no wedge, has no bearing input and ignores R_c. `in_a1([-100,0],[0,0])` returns True, though this point is outside a zero-degree first-observation wedge. It is unused in self-checks. Remove this unused helper or narrow its name/documentation to the predicate actually computed; do not expose it as a full A1 oracle.

### F7 — Duplicate-success ledger inconsistency is silently accepted

On frozen T04's two-success script, sum of delta_t is 10 but ledger_totals T is 8. K=1 correctly counts distinct channels, but both s=1 entries cannot be valid successive clears under plan §2. Explicitly flag/reject inconsistent injected feedback while retaining the single-count check. Do not present conflicting totals as valid, or expand this into P1-B retry code.

## Reproduction

In the fixed clean Executor worktree, with the environment above:

```python
from evaluator import predicates as p, halfplane as h, selfcheck as s
p.observation({'g':[700,700],'R_c':1500,'kind':'directional','phi_deg':90},[0,700])
# 'no_signal'; exact expected 'direction'
s.compare(1.0,float('nan'))  # []
p.in_a1([-100,0],[0,0])  # True
f=s.load_fixture(s.default_fixtures_dir(),'G07')['inputs']['g07b_near_collinear']
h.near_collinear_wedges_state(f)  # 'NUMERICAL_UNCERTAIN'
[p.direction_contract_ok(f['theta_hat_1_deg'],f['S1'],[2000,0]),
 p.direction_contract_ok(f['theta_hat_2_deg'],f['S2'],[2000,0])]
# [True, True]; the ray proof is in F3
events=s.load_fixture(s.default_fixtures_dir(),'T04')['inputs']['two_success_script']
sum(p.delta_t([0,0],1,e) for e in events),p.ledger_totals(events)['T']
# (10.0, 8.0)
```

Diagnostic processes exited 0 because they printed observations, not passing assertions. No evaluator or test implementation was changed by this reviewer.

## Dynamic feedback and escalation

- Red Team: recommended **now**, a bounded independent challenge of fixed result a49cc64827cc398c44a41d4094776edddb41f79f, focusing on false acceptance/rejection, oracle isolation and ledger consistency. No reviewer has been dispatched; a separate assigned review WI and independent participation are required.
- External Strategist: targeted clarification needed **now** for G07b's conditional uncertainty label versus the recession witness, and G15's heading-perturbation unit/numeric instance. Owner: external advisor/user for modeling interpretation; Technical Lead records the resolution and updates frozen artifacts/WI before affected execution. No broad route redesign is justified by evaluator bugs.
- Technical Lead: F1/F2/F5/F6/F7 are bounded repairs within existing paths; preserve mathematics and thresholds. F3/F4 remain affected holds pending clarification. No C0 WI, candidate, P1-B, RT-002 closure, merge or push.
- User briefing: self-checks pass but evaluator readiness is not accepted; repair confirmed local defects, obtain targeted catalog clarification and invite independent challenge. These results concern validation quality, not model performance.

## Follow-up and review limits

WI-014 is FIX_REQUIRED. Historical EVALUATOR_FIXTURES_READY is retained but not accepted. Preserve this report and cited history; append recheck evidence for the next fixed revision. Remaining open references must be reported as OPEN, not silently converted to readiness. This is Technical Review, not independent Red Team or Strategic Review; neither has occurred here. Repair execution must have a supplied start and a clean precheck. No formal result or P1-A property passage is established.

## Repair recheck — 2026-09-12

Fixed repair `530734ab450e597b178a4a5156329ec5b587348f` exists and is the child of repair start `a49cc64827cc398c44a41d4094776edddb41f79f`. Executor worktree remains clean on the assigned branch. All 11 changed paths are inside WI scope. PowerShell/Python 3.12.3 with PYTHONPATH=src and PYTHONDONTWRITEBYTECODE=1: required unittest rerun passed 26 tests in 0.068 s; committed-range diff check passed. Independently checked all 26 fixture hashes against both committed blobs and checkout-filtered bytes: zero mismatches.

The original F1 cardinal-boundary counterexample is covered by exact cardinal normals/rational dot products; F2 finite-versus-NaN/infinity rejection, F5 checkout-byte preservation, F6 unused-helper removal, and F7 explicit duplicate-success inconsistency disposition have corresponding code and passing regressions. This accepts the bounded repairs to the original counterexamples, not arbitrary-angle numerical certification or a general proof of oracle correctness. The report acknowledges the generic-angle floating-boundary limitation. F3/F4 retain frozen numeric expectations and explicit OPEN annotations; overall EVALUATOR_FIXTURES_OPEN is appropriate. Some report process statements (TR-012 uncommitted, Red Team not performed) are historical author-side statements, not verified current state.

User reports Red Team completed its review. Its result has not yet been located: the Red Team worktree remains on historical `e980a021447497c48d6d95a978573c44b5a55fc3`, with no new review file or commit. The prior request allowed a conversation-only report, so absence of a commit does not imply the review did not happen. Technical Lead requested the report location/text and must inspect its actual fixed target and findings before recording independent review disposition.

Current disposition: **FIX / interpretation HOLD**. Wait for external Strategist/user clarification on G07 classification and G15 heading perturbation before final acceptance or downstream candidate work. Do not issue another speculative repair for these interpretations. Receiving/reconciling the Red Team report can continue while waiting; any new independent implementation finding can be handled separately. No model-route change is justified by the current evaluator-only evidence. No merge, commit or push was performed for this recheck.


## RT-003 received and mapped — 2026-09-12

Read user-delivered `audits/redteam/RT-003.md` in Lead worktree. It reviews only `a49cc64827cc398c44a41d4094776edddb41f79f`: seven findings, highest MAJOR, no CRITICAL, no RT-002 closure. Retain the original report unchanged. Its participation statement declares no authorship of implementation/specification; this is independent challenge evidence for the original target, not a repair recheck.

| RT-003 finding | Technical Lead comparison with repair 530734ab450e597b178a4a5156329ec5b587348f |
|---|---|
| F1 boundary | Original counterexample repaired; additional phi=135, p=(0,0), g=(1,1) probe returns True as expected. Red Team repair recheck pending. |
| F2 comparator | Finite expectation vs NaN repaired; extra actual NaN remains silently ignored. Probe `compare({'a':1.0},{'a':1.0,'b':float('nan')})` returns `[]`. Outstanding bounded implementation repair. |
| F3 G07 | Interpretation OPEN; await recorded advisor/user decision. |
| F4 G15 | Interpretation OPEN; await recorded advisor/user decision. |
| F5 duplicate-success ledger | Original defect flagged in repair, as rechecked above; Red Team repair recheck pending. |
| F6 common-mode self-check | Independent counterexamples now exist, but F2 residual and F3/F4 remain. Self-checks are not independent correctness proof. Evaluator/fixture same authorship was explicitly assigned by WI-014; it is not by itself a breach of the evaluator/candidate/challenge author split. |
| F7 partial A1 helper | Removed in repair; Red Team repair recheck pending. |

Additional probes used Python 3.12.3, PYTHONPATH=src, PYTHONDONTWRITEBYTECODE=1 in the clean fixed Executor worktree; exit 0 (diagnostic output, not pass assertion). Finite expectation versus NaN produces a mismatch, phi135 visibility is True, unexpected extra NaN produces no mismatch. No code edited.

RT-003 did not reproduce the old checkout CRLF observation. Preserve both observations as environment-dependent evidence; do not rewrite either report. The repair's scoped attributes and the Lead's full committed/filtered hash checks pass. RT-003's ancillary `u_c` remark must be assessed against the existing `exists`/`cleared` input contract; no automatic API expansion is authorized.

Updated user feedback: wait for Strategist on G07/G15 and final acceptance, but not for the extra-NaN local defect. Technical Lead can specify a bounded repair to validate unexpected nonfinite fields, preserving allowed finite-extra-key semantics unless explicitly changed. Then request Red Team recheck on the final fixed repair target. Current status remains FIX_REQUIRED / interpretation HOLD. No findings are marked Red-Team-closed by this mapping; no C0 WI, merge, commit or push.


## Latest repair and advisor disposition — 2026-09-12

Verified evaluator commit a8401fc2bf53375610b29f96f1c332d09738975e exists; its three-path residual repair remains within WI scope. Required tests rerun in the clean assigned Executor worktree: Python 3.12.3, 35 tests, OK, 0.080 s; committed-range whitespace check passed. Red Team commit 6831fed64472bc97e71f2a6e985d688c971d230e exists and touches only audits/redteam/RT-003.md. Lead RT-003 hashes to the identical blob edcdc660c979344909d8118b1370f48634e73c0e. Its recheck of a8401fc reports F1/F2/F5/F7 repaired, F6 partial, G07/G15 not adjudicated. Retain that fixed-version scope; RT-002 remains open.

User supplied external Strategist resolution, recorded as SR-001 / D-002. Interpretation wait ends: G07b must certify UNBOUNDED; G15 gets an independent angular epsilon and frozen three headings. Implementation repair and independent recheck remain pending. No further Strategist input is presently needed unless SR-001 reopen conditions occur. Red Team should next recheck the new G07/G15 fixed result and residual F6. Technical Lead has updated SPEC/catalog/WI only; no candidate code, model-route change or overall acceptance.


## Final technical recheck — 1ae791b / SR-001 (2026-09-12)

- Disposition: **PASS** for WI-014 evaluator/fixture scope at fixed result `1ae791b8055690fd9d1239d0e864819865b58f6b`.
- Execution Start: `556abb3a79fbab26caf2dce6ff79dff14a86c478` (parent of the result; Comparison Base `f69f2c670827fc807a124945bef280fb0907775d` is an ancestor).
- Independent Red Team recheck: `cd60ec9e76cd1db61ef6dd848776b7bb15cdd1a6` (`audits/redteam/RT-003.md` blob `b68df6b1d6fe39af4805020557035a9c290342c6`).
- This PASS does **not** close `RT-002`, select a model, authorize `src/candidate/`, merge the result onto `main`, push, or imply `P1A_PROPERTIES_*`.

### Evidence checked

Commit objects exist. Result parent equals the supplied Execution Start. Executor worktree `E:/pycharm/projects/pythonProject18/题目/B题-executor` on `feat/WI-014-p1a-evaluator` is clean at the result. Delta versus Execution Start is exactly seven authorized paths: `src/evaluator/halfplane.py`, `src/evaluator/selfcheck.py`, `tests/p1a/fixtures/G07.json`, `G15.json`, `manifest.json`, `tests/p1a/test_evaluator_fixtures.py`, `evidence/experiments/EXP-002/EVALUATOR_AND_FIXTURES.md`. `git ls-files src/candidate` is empty. Committed-range `git diff --check` is clean.

PowerShell, Python 3.12.3, `PYTHONPATH=src`, `PYTHONDONTWRITEBYTECODE=1`, in the clean assigned Executor worktree: `python -m unittest discover -s tests/p1a -q` → 50 tests, OK, 0.098 s. That suite is a self-check fact, not independent passage and not this PASS.

Independently hashed all 26 fixture files against the committed manifest: 0 mismatches for worktree bytes, committed blobs, and `git cat-file --filters` checkout bytes. `G01.json` filtered CRLF count is 0. Scoped `tests/p1a/.gitattributes` (`fixtures/*.json -text`) remains. Manifest SHA-256 `84d594eb4fe719d8cce36a8ece7cce7b40243bf4fffbfd43e6bcdd4b2e428377` matches the Executor report.

Lead `RT-003.md` working-tree hash equals `cd60ec9:audits/redteam/RT-003.md`. Original findings on `a49cc648` and the `a8401fc` recheck are retained. SR-001 reopen conditions were checked against the frozen sensors/bearings/δ, 700 m scale, JSON headings, and nonempty P4 scan; none triggered.

### G07 — analytic ray vs represented half-planes

Two evidence kinds are distinguished and both hold.

**Analytic (bearings, no evaluator rows).** For every sampled `t ∈ {0,1,1e3,1e6,1e9,1e12}`, `q_t=(2000+t,0)` has bearing 0 from `S1=(0,0)` and from `S2=(1000,0)`. Zero lies in `[-1,1]` and `|0-0.01|=0.01≤1`. `||q||=2000>1800`, so a disk/radius cap would have excluded the certificate point.

**Represented float half-planes (exact arithmetic on those rows).** `wedge_pair_halfplanes` returns **4** Fraction rows only (two wedges × two sides; no D/radius rows). `_feasible=True`, then `_recession_nontrivial=True`, then `halfplane_state=UNBOUNDED`. `ray_certificate(rows, q=(2000,0), d=(1,0))=True` with all four feasible and recession values `>0` (margins ~17–35 and ~0.017). Negative control `θ̂₂=90°` is `BOUNDED` and the same ray is not certified. `RESOLUTION_DEG` and `_has_near_parallel_pair` are absent. Frozen `NUMERICAL_UNCERTAIN` is not reported. `g07b_finite_diameter_reported` is false because the state is not `BOUNDED`.

The certificate is a proof for the **represented** directions (`Fraction(str(cos/sin(float degrees)))`), not an algebraic proof of exact 1°/0.01° trigonometry. The analytic bearing argument is the independent unboundedness proof required by SR-001. Residual: a future instance with tiny row margins would need an interval enclosure of the directions; this frozen instance is not near that boundary.

### G15 — heading triple, mirror, JSON, P4, O-03

Independent `ε_φ=atan(1e-6/700)` equals `1.4285714285714286e-9` rad = `8.18511135901176e-8` deg. `90±ε_φ` **equals** the frozen JSON degrees `89.99999991814889` and `90.00000008185111`. JSON round-trip preserves all three and keeps the perturbations distinct from `90.0` and from each other.

Observations via `observation()` (not `expected_evaluator`):

| Check | Independent expected | Actual at `1ae791b` |
|---|---|---|
| `p_R=(1400,700)` at frozen `φ` | `direction, direction, no_signal` | same |
| `p_L=(0,700)` at `φ=90` | `direction` (closed) | `direction` |
| `p_L` full triple (derived) | `no_signal, direction, direction` | same |
| `p=(700,1400)` all three `φ` | `direction` ×3 | same |
| complete P4 visible (`near`/`direction`) | nonempty | 6 / 8 / 6 of 81; `g` not included |
| `g=(700+ε_d,700)` | separately named, no `phi_deg` | `position_perturbation_eps_d` omni-only |
| directional coincidence `(0,0)` | `O03_OPEN`; not coverage | `O03_OPEN`; `(0,0)∈P4` but absent from all three heading visible lists |

O-03 is preserved. Position (`ε_d` metres) and heading (`ε_φ`) are split.

### Prior findings at this version

Independently reproduced on `1ae791b` (not copied from RT-003):

| Item | Result |
|---|---|
| F1 / RT3-F1 `(0,700)` at `φ=90`; `φ=135/134/136` | `direction` / True/False/True; no regression |
| F2 / RT3-F2 NaN and extra non-finite keys | mismatch reported; extra finite keys still ignored |
| F5 / RT3-F5 T04 two-success | `∑ΔT=10`, `T=8`, `K=1`, `consistent=False` |
| F7 / RT3-F7 `in_a1` | absent |
| RT3-F3 / F4 | repaired under SR-001 as above |
| RT3-F6 | **partial**, standing isolation caveat (below) |

RT-003 at `cd60ec9` reports the same F3/F4 repair, F1/F2/F5/F7 hold, no new MAJOR/CRITICAL. Original findings on `a49cc648` remain historical. The author's report §13.8 `EVALUATOR_FIXTURES_OPEN` is retained; it was the correct conservative pre-review label and is not silently rewritten to `READY`. Historical `EVALUATOR_FIXTURES_READY` remains unaccepted.

### MINOR hardcoded coverage flag — bounded no-fix

`selfcheck.run_evaluator_now` returns `"coincidence_used_as_coverage_evidence": False` as a **literal**, not by inspecting any visible set. RT-003 records this as MINOR under F6.

Uses of the flag: it is an `expected_evaluator` documentation field and a self-check equality. It is **not** the coverage oracle. Actual coverage is computed by `visible_scan_points` over omni worlds and the heading triple; those lists independently exclude coincidence (`O03_OPEN` is not `near`/`direction`; heading `g=(700,700)` so `(0,0)` is not the source). This Technical Review and RT-003 both enumerated the heading P4 sets (6/8/6) and confirmed `(0,0)` and `g` are absent.

Disposition: **no Executor repair**. Pretending the flag is a dynamic check would be a false claim. A later reader must not treat the hardcoded field as proof of coverage exclusion; the proof is the independently enumerated visible sets plus the `O03_OPEN` label. If a future fixture mixes the coincidence world into a nonempty-coverage map, that map — not this flag — is the gate that must fail.

### `clear_succeeds` / T07 remarks — not in-scope defects

`clear_succeeds(source, p)` encodes plan §2 `u_c=z_c(1-q_c)` via `exists`/`cleared`, then `||p-g||≤20`. It does not read a separate `u_c` key; a planted `u_c=0` with default `exists=True` is ignored. Current call sites (G16) pass neither `u_c` nor a conflicting pair. WI-014 requires the predicate, not a redundant parameter. **No API expansion.**

T07 `evaluator_now` in the frozen catalog is `validate_world` on the legal/illegal N/`N_dir` lists. `initial_N_counts_cleared_source` aliases `validate_world(Q3,10,0)` and ignores the scene key `cleared:1`. That is catalog-faithful and tautological; recount-after-clear is `candidate_later`. **No T07 upgrade.**

### Progress, dynamic adjustments, and user briefing

- Current result and remaining uncertainty: WI-014 evaluator/fixtures at `1ae791b` meet the revised SPEC/catalog and SR-001. Residual F6 is that author-written `expected_evaluator` plus a green suite are not oracle isolation. The hardcoded O-03 flag is explanatory. No candidate exists, so there is no P1-A property result.
- Red Team: **not needed now** for this bounded acceptance. Targeted recheck of `1ae791b` is already recorded at `cd60ec9`. Named later gate: independent challenge of any future candidate, and `RT-002` still blocks only formal-readiness promotion. Exact target if reopened: a later evaluator/fixture commit, or the first candidate commit.
- External Strategist: **not needed**. SR-001/D-002 already rules G07/G15; reopen conditions are not met. No model-route question is justified by this evaluator-only evidence.
- Technical Lead adjustments: none to mathematics, thresholds, or comparison conditions. Bounded no-fix of the MINOR flag, with the reason above. No C0 WI, no merge of `1ae791b` onto `main`, no push.
- Next action, owner, and waiting work: user authorization is required before integrating the evaluator onto `main` or opening a **different-author** candidate WI. Evaluator author (`executor-B题` / model `deepseek-flash`, self-reported) remains barred from `src/candidate/`. Relabelling is not independence. Affected work that must wait: C0 implementation, P1-A property run, `RT-002` closure, model selection.
- Short user-facing briefing: latest G07/G15 repair is technically accepted as evaluator/fixtures only. F3/F4 are repaired; prior F1/F2/F5/F7 do not regress; F6 remains a standing isolation caveat. No further Executor repair and no new Strategist round. Do not start C0 from this PASS.

### Participation and review limits

Reviewer did not author `src/evaluator/` or the fixtures. This is Technical Review of a fixed commit plus the already-completed independent Red Team recheck; it is not a second Red Team pass and does not close Red Team findings by Lead assertion. Executor report process statements that F1/F2/F5/F7 “still need Red Team recheck” were true at write time; the `cd60ec9` recheck of `1ae791b` has since recorded no regression.

### Follow-up and closure

WI-014 is **COMPLETE** for the explicitly limited evaluator/fixture scope. `RT-002` remains open. `RT-003` original findings remain on the record; F6 residual is not a WI-014 blocking defect because the WI assigned one author to evaluator and fixtures, and independent derivation of G07/G15 now exists outside that author. Required remaining action is user authorization of the next WI, not another repair of `1ae791b`.
