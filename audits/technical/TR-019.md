# TR-019 — Read-only check: C0 clear-rectangle snake vs plan §5.2

- Date: 2026-09-13
- Reviewer Role: Technical Lead
- Reviewed object: `src/candidate/scan.py` `clear_rectangle_centres` at `a48c77cc0d46a81b96aa56f54197b8837c4c735c` (candidate ancestor `e4ee8febfc8ff89e927eba436e81e2ae7759af1c`)
- Disposition: **FIX** (implementation). Not model-family. Not a Red Team finding. Not selection.

## Git identity (this review)

- Lead worktree `E:/pycharm/projects/pythonProject18/题目/B题`, branch `main`, HEAD `3b4d6dad99872add52bd2e7c249052c0ff7d64a1`, clean, equal `origin/main`.
- Executor `E:/pycharm/projects/pythonProject18/题目/B题-executor`, branch `feat/WI-017-p1b-c0-practice`, HEAD `a48c77cc0d46a81b96aa56f54197b8837c4c735c`, clean.
- Cited commits exist; `e4ee8fe` is an ancestor of `a48c77c`.

## Independent reproduction (not copied as a finished review)

Plan §5.2: centres `S+(10+20i)t+(-20+20j)t_⊥`, snake **in the rectangle**, adjacent submitted ≤22 m; `V_src` uses `224·22`.

Current code builds the 225 points in global coordinates then `snake_order` by **global y**. At `θ=0` that equals local order (G16). At `θ=π/4`, `S=(0,0)`:

| Sequence | n | segs >22 m | max adjacent (m) | path length (m) |
|---|---:|---:|---:|---:|
| current `clear_rectangle_centres` | 225 | **150** | **72.111** (`√(40²+60²)`) | 5782.90 |
| snake in local `(i,j)` then rotate | 225 | **0** | **20.000** | 4480.00 |

Same point **set**, different **order**. First five current 45° steps: 20, 28.284 (`20√2`), 20, 28.284, 28.284.

G16 (`theta_deg=0`) checks set equality and a **fixture pair** `[10,0]–[30,0]` plus the constant 22; it does not walk the emitted sequence after rotation. Evaluator `clear_centres` lists `i,j` without snake; G16 compares sets.

Live logs at `a48c77c`, consecutive `/clear` on the **same channel** only:

| Log | same-ch segs | segs >22 m | max (m) | reconstructed move time / `exit` vt |
|---|---:|---:|---:|---:|
| `q3_practice_requests.json` | 1833 | **1235** | **382.099** | 31872.9 / 38510.9 ≈ **82.76%** |
| `q4_practice_requests.json` | 1685 | **885** | **501.597** | 37064.4 / 51898.4 ≈ **71.42%** |

## Boundaries

- 225-point **cover** of `G(S,θ)` is unchanged. Practice `K=N=12` (operator UI) is unchanged.
- Plan `V_src≤(10000+224·22)/5+225·3+2` **does not apply** to this sequence. That is not a proof that the two practices exceeded 63000/81000 s (observed vt 38511 and 51898 s).
- Grade: **implementation**. No C0 family failure.

## Affected claims (scope only)

- Historical G16 / WI-016 P1-A PASS: still valid for **set** cover and `θ=0` adjacency; **not** a check of rotated snake or of `224·22` path cost on this binary.
- Plan §5.3 virtual-cost bound: not instantiated by `a48c77c` clear order.
- Practice completeness (`K=N=12`) not reversed.

## Follow-up

Repair WI-018 issued after user authorization 2026-09-13. Independent Red Team of `a48c77c` and of the later repair commit. No C1, no 81→49 / 225→150 in that WI.

## Progress briefing

- Established: implementation snake ≠ plan local snake for non-axis-aligned `θ`; live same-channel clears exceed 22 m.
- Unverified: whether a repaired order would change practice wall-clock or `K` (no new rehearsal in this WI).
- Red Team: **after** the repair commit, target `a48c77c` (defect present) and the new hash (defect gone, set unchanged).
- Strategist: **not** for this repair. Optional later: 81→49 / 225→150 / fuse positives — not in WI-018.
- Next: Executor runs WI-018. Execution Start is in the assignment prompt.
