# WI-024 — Q2 wrap-up: certified inner domain, 6-point rule, local comparators, paper draft

- Status: `COMPLETE` — TR-029 PASS of `882ba6fe3fd84459c64e2121e872a51cee35304a`. Draft Q2 answer; not CLAIMS. Not on `main`.
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: finish Q2 as an independent, citable answer. **Does not wait** for WI-023 150/49 selection. **Does not** start C1.
- Comparison Base Commit (full hash): `7cbeda3723e43d40623e0d0eaff5b634fb2ae36a`
- Execution Start Commit: supplied after this WI is committed and the Q2 worktree is prepared
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-q2`
- Assigned Branch: `feat/WI-024-q2-wrapup`
- Local Commit Permission and Allowed Paths: permitted; only
  - `src/candidate/q2.py`
  - `tests/candidate/test_q2.py`
  - `tests/q2/`
  - `evidence/experiments/EXP-006/`
  - `paper/02_Q2.md`
- Remote Push: not authorized
- Work Type: Q2 wrap-up. **Forbidden:** `src/candidate/scan.py`, `variants.py`, `model.py` (except imports), `tests/p1a_run`, live sim, 150/49, C1, raising time caps, `paper/CLAIMS.md` (Lead coordinates later)

## Inventory (do not redo)

Reuse `q2.py` / `test_q2.py` / G09–G14 fixtures as read-only inputs. Fill gaps: isolated timing, full-resolution comparator table (or reduced with **labeled upper bounds**), paper draft, wrap-up report.

## Required content

1. Feasible A1 after first direction (plan §4.1).
2. C_in (three-disk / inequalities); six pool points certified in C_in; not claiming C_sig equality.
3. Quality: \(\bar J_2\) is an **upper bound** on J2, not realized error. Selection: min \(\bar J_2\), then move, then a, then b. QUALITY_UNCERTIFIED when no bound.
4. Successors A2 near/dir; no_signal inside C_in is conflict.
5. Local compare: same first observation; pool vs lateral (500,500) vs forward (500,0). Use G09–G14 **catalog numbers**. Certification for all allowed feedbacks; sampling only as examples/counterexamples. Report **all** fails, UNRESOLVED, timeouts, and no-gain cases. Do **not** claim global optimality or two-measure clear precision.
6. Timing: wall-clock of `certifiable_candidates` **alone** at production 360×4096 and at test 24×256; record CPU load, machine, whether WI-023 tests were running. Do **not** run `unittest discover -s tests/p1a_run` in this WI (load + cap). Isolated select time is the Q2 number; harness 10 s is not geometry.
7. `paper/02_Q2.md` Chinese draft + `evidence/experiments/EXP-006/Q2_WRAPUP.md` with claim→code→test links. No CLAIMS.md.

## G09/G11

Timeouts are harness load, not geometry bugs. Isolated PASS is not “stable harness PASS”. Do not raise 10 s.

## Budget / stop

Prefer finish in one session. Stop if: worktree is `B题-executor`; any 150/49 path; C1; live sim; git mismatch. Conclusion `Q2_WRAPUP_READY` / `OPEN`.

## Independence

This author must not Red-Team WI-024. WI-023 author may be the same person **only if** they use this worktree and do not mix commits.
