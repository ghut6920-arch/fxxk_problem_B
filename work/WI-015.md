# WI-015 — EXP-002 C0 P1-A Candidate Implementation

- Status: `COMPLETE` — TR-014 PASS of `e4ee8febfc8ff89e927eba436e81e2ae7759af1c` (Execution Start `9c60400d63943322ff37b0b5bc4a4262cb940544`). Implementation only; D-004 same-model residual retained. Not `P1A_PROPERTIES_*`, not merged, not pushed.
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: implement C0 geometry/state/queue/certificate code under `src/candidate/` so a later **run** WI can score G01–G16 / T01–T10 `candidate_later` items against the independent evaluator
- Comparison Base Commit (full hash): `61465c5513c5bdc3d593d3fd389f6866d8339721`
- Execution Start Commit: supplied by Technical Lead after this WI is committed and the assigned branch/worktree is prepared; the assignment prompt records the full hash
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-executor`
- Assigned Branch: `feat/WI-015-p1a-c0-candidate`
- Local Commit Permission and Allowed Paths: permitted; result commit(s) may add or modify only
  - `src/candidate/`
  - `tests/candidate/`
  - `evidence/experiments/EXP-002/C0_CANDIDATE_IMPL.md`
- Remote Push: not authorized; intended future remote/branch/payload is none
- Work Type: P1-A **candidate implementation** of frozen C0 under `experiments/EXP-002/SPEC.md`; not the accepted 4 min 20 s property conclusion, not P1-B, not simulator, not model selection, not evaluator edits

## Independence bind (non-negotiable)

User D-004 allows the WI-014 model `deepseek-flash` to write `src/candidate/` in a **new** session. This does **not** satisfy `modeling/EXPERIMENT_DESIGN.md` §4.5 isolation; it is a recorded residual. Do not copy or import `src/evaluator/`. Derive only from the plan and SPEC. Do not own the later challenge-manifest WI.

Record the actual author identity in the report.

## Objective

Implement the C0 **candidate-under-test** interfaces frozen in `experiments/EXP-002/SPEC.md` (plan pin `modeling/COMPLETE_MODEL_PLAN.md` blob `407b5e9f0bf001663bee4f9b9d4b21e601c4bbe4`). Provide candidate-side tests and a closed-set implementation report. Do not edit the evaluator, fixtures, SPEC, or catalog. Do not conclude `P1A_PROPERTIES_PASS` / `FAIL` / `UNRESOLVED`.

## Inputs

- `AGENTS.md`, `docs/roles/executor.md`, this WI
- `experiments/EXP-002/SPEC.md` (candidate interfaces and path split)
- `experiments/EXP-002/FIXTURE_CATALOG.md` (`candidate_later` property statements; do not rewrite numbers)
- `modeling/COMPLETE_MODEL_PLAN.md` blob `407b5e9f0bf001663bee4f9b9d4b21e601c4bbe4` §§2–3, §4.1–4.2, §5.1–5.3, §6, §7.1–7.3
- `modeling/EXPERIMENT_DESIGN.md` blob `9a2b46e89687bd90318036d48785e57ab9e4476b` §4.2, §4.5
- Frozen evaluator on Comparison Base: `src/evaluator/`, `tests/p1a/fixtures/` (read-only)
- `ASSUMPTIONS.md` O-03
- `STATUS.md`, `NEXT_ACTION.md`, `DECISIONS.md` D-003

## Allowed Scope

- Pre-task Git checks; stop on mismatch. Do not pull, fetch, switch, merge, rebase, reset, or repair Git.
- Create a Python 3 **stdlib-only** candidate under `src/candidate/` implementing at least the SPEC “Candidate under test” interfaces:
  - wedge / half-plane intersection classification (plan §3): `CONFLICT` / `UNBOUNDED` / `BOUNDED`; vertices, diameter, diameter-circle, min enclosing radius as required by G01–G07
  - G06 unit equilateral covering-circle submodule
  - `C_in` membership and Q2 6-point / lateral / forward comparators (plan §4.1; design §4.2); successor regions `A_2^{near}` / `A_2^{dir}`
  - `P_3` / `P_4` generation (plan §5.1) and C0 scan order (row-major snake, channels 1–20) as needed for G15 `candidate_later`
  - 225 clear centres and 20 m / 1 m Euclidean submission bound (plan §5.2; G16)
  - finite state: per-channel existence, cells ≤4096 leaves and depth ≤8, type tags, first positive feedback, raw reading log, success log, queue; outward rounding; do not delete a true cell to meet the cap (plan §6; T05)
  - `L=2` not reset by ordinary progress; one clear list per discovered source; keep an older fallback certificate when a newer crude bound is larger (plan §7.1–7.3; T08/T09)
  - virtual remainder accept/reject against the all-feedback certificate (T10)
  - candidate-side virtual ledger from plan §2 (independent copy; do not import evaluator)
- Create `tests/candidate/` unittests for the above using catalog **numbers** and plan predicates. Do not copy `src/evaluator/` source. Do not edit `tests/p1a/fixtures/` or `expected_evaluator`.
- A thin harness that **imports evaluator only from tests** to compare verdicts is allowed if `src/candidate/` itself contains none of the evaluator modules and no candidate module imports `evaluator`. Prefer candidate-only tests when an analytic catalog number already exists (G01–G07, G13, G16 centres).
- Write `evidence/experiments/EXP-002/C0_CANDIDATE_IMPL.md` with the required report fields and one closed-set conclusion.
- One or more local commits containing only the authorized paths.

## Forbidden Scope

- Do not edit `src/evaluator/`, `tests/p1a/fixtures/`, `tests/p1a/test_evaluator_fixtures.py`, EXP-002 SPEC/catalog, the mathematical plan, `MODEL_SPEC.md`, or `SELECTED_MODEL.md`.
- Do not `import` `evaluator` from any file under `src/candidate/`.
- Do not run or conclude the official 4 min 20 s `P1A_PROPERTIES_*` suite. A later run WI does that after Technical Review of this implementation.
- Do not implement C1 adaptive scoring except the already-named G09–G14 Q2 local comparators. Do not implement C2.
- Do not implement T11–T12, HTTP, JSON protocol adapters, or inspect simulator archives/executables.
- Do not install third-party packages, use the network, close `RT-002`, or push.
- Do not invent an official answer for O-03. Do not treat component worlds as 10–16 source performance samples.
- Do not claim model selection or formal readiness.

## Required Review Commands

Run each in the assigned worktree. Record output and exit status.

1. `git rev-parse --show-toplevel`
2. `git branch --show-current`
3. `git rev-parse HEAD`
4. `git status --short --branch`
5. `git cat-file -e "<Comparison Base>^{commit}"` using the hash in this WI
6. `git merge-base --is-ancestor <Comparison Base> HEAD`
7. `git cat-file -e "HEAD:work/WI-015.md"`
8. `git cat-file -e "HEAD:src/evaluator/predicates.py"`
9. `git rev-parse HEAD:modeling/COMPLETE_MODEL_PLAN.md` equals `407b5e9f0bf001663bee4f9b9d4b21e601c4bbe4`
10. Confirm no file under `src/candidate/` imports `evaluator` (AST or equivalent)
11. `PYTHONPATH=src python -m unittest discover -s tests/candidate -v` (Windows: set `PYTHONPATH=src` then the same python command)
12. The evaluator suite must still pass: `PYTHONPATH=src python -m unittest discover -s tests/p1a -q`
13. `git diff --check` on the result commit(s)

## Required report fields

`evidence/experiments/EXP-002/C0_CANDIDATE_IMPL.md` must contain:

- Date, role, **author identity**, worktree, branch, Comparison Base, Execution Start, result-commit placeholder per `AGENTS.md`
- Independence statement: this author did not write `src/evaluator/` and will not own the challenge-manifest WI
- Interpreter version (`python --version`)
- Every required command with output and exit status
- Module list, mapping to plan sections, and a statement that no third-party package was installed
- Which G/T `candidate_later` interfaces are implemented vs still stubbed (stubs must be named; do not silently omit T05/T08/T09/T10)
- One conclusion: `CANDIDATE_IMPL_READY` or `CANDIDATE_IMPL_OPEN`
- Limitations; local commit status; `NOT PUSHED`

## Acceptance Criteria

- Pre-task contract matches this WI and the assignment Execution Start.
- `src/candidate/` exists and does not import `evaluator`.
- Candidate unittests pass, or a failed item is the reason for `CANDIDATE_IMPL_OPEN`.
- Evaluator `tests/p1a` suite still passes.
- Fixtures, evaluator code, SPEC, and catalog bytes are unchanged.
- Conclusion is one of the two allowed values and matches the evidence.
- Result commit(s) touch only authorized paths; `git diff --check` passes.

## Stop and Escalation Conditions

- Stop without editing on Git mismatch or unexplained worktree changes.
- Do not treat D-004 as permission to copy the evaluator.
- Escalate if a catalog number contradicts the plan or would require a new official interpretation or a C1/C2/P1-B expansion.
- Stop after the report commit. Do not start a property-run WI or P1-B.

## Completion Boundary

Completion is a C0 P1-A **implementation** plus candidate tests. It is not `P1A_PROPERTIES_*`, not P1-B, not `RT-002` closure, not model selection, not a push, and not authorization to reuse the evaluator author.

## Technical acceptance (2026-09-13)

Accepted result: `e4ee8febfc8ff89e927eba436e81e2ae7759af1c` on `feat/WI-015-p1a-c0-candidate`. Author conclusion `CANDIDATE_IMPL_READY` is retained as implementation-only. D-004 isolation waiver remains. Red Team of this commit is the next gate before any property-run WI.
