# WI-023 — Implement BASE/CLEAR150/SCAN49/COMBINED and offline same-world compare

- Status: `READY`
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: turn RT-006 KEEP math into four **run tags** on the repaired-C0 tree; compare on frozen synthetic worlds; **keep BASE** as default
- Comparison Base Commit (full hash): `7cbeda3723e43d40623e0d0eaff5b634fb2ae36a`
- Execution Start Commit: supplied in the assignment prompt after this WI is committed and the worktree is prepared
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-executor`
- Assigned Branch: `feat/WI-023-cover-variants`
- Local Commit Permission and Allowed Paths: permitted; only
  - `src/candidate/`
  - `tests/candidate/`
  - `tests/c0_baseline/`
  - `scripts/`
  - `evidence/experiments/EXP-005/`
- Remote Push: not authorized
- Work Type: C0 **model-variant** implementation + offline compare. Not C1. Not live sim. Not formal.

## Clock

Research wall for **this WI**: **90 minutes** from first Executor command after Execution Start. 36-trajectory cap 24 min inside that. If 90 min hits, stop with `COVER_VARIANTS_OPEN`, keep BASE, do not cut tests to force PASS.

Beijing new-test **start** cutoff 2026-09-13 17:30 remains outside this WI (no live `/enter`).

## Frozen BASE

Geometry: `d675831` / `scan.py` `32dd26f7fccf8b2d50f2af349251848cea1c39fc` until CLEAR150/SCAN49 **intentionally** change scan/clear generators. Inherit protocol/session/logging from Comparison Base (WI-020 tree). Do not drop adapter work.

## Four tags only

| Tag | Scan | Clears |
|---|---|---|
| BASE | 9 / 81 | 225 |
| CLEAR150 | 9 / 81 | 150: \(x=10+20i\), \(y\in\{-15,15\}\), **20×30** tiles, local snake then rotate |
| SCAN49 | Q4 `P_4'=\{700(i,j):i,j=-3..3\}` keep exterior points; still 20 channels | 225 |
| COMBINED | SCAN49 scan | CLEAR150 clears |

Q3: BASE vs CLEAR150. Q4: all four. Same protocol, ledger, no truth reads. `near` unchanged. No 130-grid, no channel skip, no C1, no COMPLETE change, no raising P1-A time caps.

SCAN49: 980 measures, 979 switches if all 20 channels; count origin→first and 48 grid steps.

CLEAR150: 148×20+30=2990 m ideal; submitted bound 148×22+32=3288 m; update loop/request/clear budgets to 150.

Envelope 62946.57 s / 3380 requests is **not** a score.

## Required tests (actual sequences, not constants)

- 150-set, 20×30 cover \(\sqrt{325}+1<20\), 30 m row wrap, consecutive distances after rotation.
- 49-set includes \(\|p\|>1800\) prescribed points; counterexample if they are dropped; grid/circle/line samples.
- N=16 max action counts per tag; unique-accept ledger; fail-closed unknown-accept.
- `tests/p1a` and `tests/p1a_run`: record load-sensitive 10 s cap; do **not** raise it. Isolated PASS ≠ stable PASS.

## Synthetic compare (if time)

12 legal worlds, 6/6 split, 3 Q3+3 Q4 each half. Freeze files+hashes before run. Q4 both types. Do not use “225th success” pressure scripts as physics worlds.

36 tracks: Q3×6×2 + Q4×6×4. 40 s/track, 24 min stage. Dev first, freeze, then holdout. Max 2 substantive repairs; holdout used for repair becomes regression.

Per track: K/N, complete/false-complete/safety, Tv, Tv/K, scan/intra-clear/inter-channel/origin→first, measure/switch/clear ok/fail, wall, protocol, ledger residual. Keep failures in the table.

If 90 min remains only for tests, skip holdout and say so.

## Report

`evidence/experiments/EXP-005/COVER_VARIANTS.md`: blob hashes per tag, tests, compare table or NOT_RUN, `g_i` vs BASE, C1-necessity **note only** (which stage still costs; failed clears; inter-channel; existing bearings). Conclusion `COVER_VARIANTS_READY` / `OPEN` / `FAIL`. Recommend retain BASE or a named tag using pre-registered screens (all complete, holdout median save ≥10%, no Tv>1.10×BASE, no timeout) — screens are not selection.

## Forbidden

Live sim; formal; push; C1/C2; editing evaluator/fixtures; pairing with practice logs as speedup.

## Stop

Git mismatch; extra grids; 90 min; two failed repairs; holdout contamination. Stop after the report commit.
