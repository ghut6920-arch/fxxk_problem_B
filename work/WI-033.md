# WI-033 — Q1 figure production from approved execution packet

- Status: `READY`
- Owner Role: Technical Lead (user-authorized bounded figure-production maintenance)
- Coordinator / Review Recipient: Technical Lead
- Related Goal: render the user-supplied Q1 final figure plan as reproducible repository artifacts without changing the mathematical model, official evidence, or Q2 work
- Comparison Base Commit (full hash): `c3e78884f142d5ada222e8fa3935dc2fb307099c`
- Execution Start Commit: supplied after this WI is committed
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题`
- Assigned Branch: `main`
- Local Commit Permission and Allowed Paths: permitted; only
  - `work/WI-033.md`
  - `paper/figures/q1/**`
  - `evidence/figures/Q1_FIGURE_BUILD.md`
- Remote Push: not authorized
- Work Type: exact-data ingestion, deterministic plotting, visual/geometric verification, and publication-format export

## User authorization and fixed inputs

The user directly requested Q1 figure production on 2026-09-13 from:

- `C:/Users/15006/.codex/visualizations/2026/09/12/01a0971f-f903-7e51-9e11-5f135af36a96/问题一图表方案_最终执行包.zip`
- `C:/Users/15006/.codex/visualizations/2026/09/12/01a0971f-f903-7e51-9e11-5f135af36a96/q1_final_figure_plan/问题一图表方案_最终执行版.md`

The execution plan SHA-256 is `6030ba66ee03a9a818f65b04c991238079c0540b945d3696b97615514265a53e`.
Use only the packet's named Fig. 1 and Fig. 2 JSON inputs. Treat them as derived plotting input, not official data or formal experimental evidence.

## Required outputs

1. A deterministic plotting source that reads the copied JSON inputs rather than manually entered display coordinates.
2. Figure 1 in PDF, SVG, and 600 dpi PNG: two panels for bearing wedges and the six-vertex localization region/diameter.
3. Figure 2 in PDF, SVG, and 600 dpi PNG: three equal-scale analytic panels for diameter-circle coverage success, failure, and the minimum enclosing circle.
4. Appendix Table A1 as editable Markdown.
5. A build and verification record containing input hashes, environment, commands, geometric checks, output hashes, visual-review scope, and known limitations.

## Acceptance criteria

- Figure geometry, labels, colors, line styles, panel structure, axis limits, and captions follow the supplied V1.0 execution plan.
- Fig. 1 uses the six packet vertices, identifies the unique farthest pair `(v1,v4)`, and displays `27.68 m`; wedges remain true ±1° forward wedges.
- Fig. 2 uses `D_P=1`, a common axis range and equal aspect; the reference circles have radius `1/2`; panel (c) uses `J=(0,sqrt(3)/6)` and radius `1/sqrt(3)`.
- All analytic/geometric checks pass before export; generated images are visually inspected at rendered size.
- PDF and SVG are vector outputs and PNG exports are 600 dpi. No JPEG, 3-D effect, generated-image model, inferred official data, or unsupported program-correctness claim.
- The working diff contains only the authorized paths plus pre-existing unrelated user files, which remain unstaged and unchanged.

## Required checks

- Verify packet files against `manifest.json`.
- Run the plotter's internal validation.
- Confirm output files exist and record SHA-256 hashes.
- Inspect both PNGs for clipping, overlap, false geometry, and readable labels.
- `git diff --check` over authorized changed paths.

## Stopping conditions

Stop and report if packet hashes fail, the fixed geometry is inconsistent, required fonts/exports cannot be produced, or unrelated repository changes would have to be modified.

## Completion report

Return the worktree, branch, Comparison Base, Execution Start, result commit if created, changed/staged paths, build command and environment, check results, visual-review result, remaining limitations, local commit status, and remote push status. Conclusion: `Q1_FIGURES_READY` or `Q1_FIGURES_OPEN`.
