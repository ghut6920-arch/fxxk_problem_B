# WI-018 — Restore C0 local-rectangle snake for 225 clear centres

- Status: `READY` — user authorized issuance 2026-09-13. Execution Start is supplied in the assignment prompt after this WI is committed and the assigned branch/worktree is prepared.
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: make `clear_rectangle_centres` visit centres in **local** `(i,j)` snake then rotate/translate, matching `modeling/COMPLETE_MODEL_PLAN.md` §5.2–5.3, so adjacent submitted distance ≤22 m applies to the **emitted sequence**
- Comparison Base Commit (full hash): `a48c77cc0d46a81b96aa56f54197b8837c4c735c`
- Execution Start Commit: supplied by Technical Lead after this WI is committed and the assigned branch/worktree is prepared; the assignment prompt records the full hash
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-executor`
- Assigned Branch: `feat/WI-018-c0-clear-snake`
- Local Commit Permission and Allowed Paths: permitted after issuance; result commit(s) may add or modify only
  - `src/candidate/scan.py`
  - `tests/candidate/` (add/extend sequence tests only)
  - `tests/p1a_run/checks.py` (G16: check **all** consecutive distances on at least one rotated rectangle, without editing fixtures)
  - `evidence/experiments/EXP-002/C0_CLEAR_SNAKE_REPAIR.md`
- Remote Push: not authorized
- Work Type: **implementation repair** of frozen C0 contract. Not C1/C2, not 81→49 or 225→150, not new simulator/formal runs, not selection

## Defect (already independently reproduced)

At `a48c77c`, `clear_rectangle_centres` rotates into global XY then `snake_order` by global `y`. Counterexample: `S=(0,0)`, `θ=π/4`, 225 points, 150 of 224 consecutive steps >22 m, max `√(40²+60²)≈72.111` m; local-index snake then rotate has max 20 m and the **same set**. G16 uses `theta_deg=0` and a fixture pair plus the constant 22, so it did not catch this. Live same-channel `/clear` at that commit: Q3 max 382.099 m (1235 segs >22), Q4 max 501.597 m (885 segs >22). Coverage set and practice `K=N=12` are **not** in dispute.

## Objective

1. Generate the 75×3 centres in local `(i,j)` **row-major snake** (`j=0,1,2` as local rows, even `j` increasing `i`, odd `j` decreasing `i`, or equivalent that keeps every consecutive local step 20 m along an axis or one 20 m row wrap of 20 m). Then apply `S + x t + y t_⊥`.
2. Keep the 225-point **set**, `P_3`/`P_4` scan, channel policy, 20 m cells, 1 m submission bound, and plan mathematics. Do not delete points; do not implement later optimization candidates.
3. Tests that **fail on `a48c77c` and pass after repair**:
   - `θ=45°` (and at least one other non-axis `θ`): all 224 consecutive Euclidean distances ≤20+ε (submitted bound 22 after ±1 m).
   - Set equality with the formula / current set.
   - `θ=0` still matches G16.
   - Path length at `θ=45°` equals 224×20 m (not ~5783 m).
4. Do not rerun the simulator. Optional: **offline** recompute same-channel path length on stored logs vs a **new** order is out of scope unless done without claiming a new live `K`.
5. Report `evidence/experiments/EXP-002/C0_CLEAR_SNAKE_REPAIR.md` with commands, the 45° table, and conclusion `CLEAR_SNAKE_RESTORED` or `OPEN`.

## Forbidden

- C1/C2; changing 81/225 counts; P3/P4 spacing; new practice/formal; editing `src/evaluator/` or `tests/p1a/fixtures/`; rewriting historical PASS; claiming `V_src` bound on unrepaired logs; push.

## Required commands

1. Git identity; Comparison Base ancestor; `HEAD:work/WI-018.md` exists
2. A test that fails if the global-y snake is restored (document by applying the new test mentally to `a48c77c`, or a one-off check against the Comparison Base tree without modifying it)
3. `PYTHONPATH=src python -m unittest discover -s tests/candidate -q`
4. `PYTHONPATH=src python -m unittest discover -s tests/p1a -q`
5. `PYTHONPATH=src python -m unittest discover -s tests/p1a_run -q` if `checks.py` changed
6. `git diff --check` on the result commit

## Stop

Git mismatch; any edit outside allowed paths; proposal to shrink 225 points or skip channels; desire to open C1 or a live rehearsal. Stop after the report commit.

## Independence

Repair author must not later Red-Team this WI. Red Team targets: (1) `a48c77c` exhibits the long-step defect; (2) the WI-018 result commit does not, set unchanged, G16 still passes.

## Execution Start

Technical Lead supplies the full hash after this WI is committed and `feat/WI-018-c0-clear-snake` is created from Comparison Base with this WI present.
