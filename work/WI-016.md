# WI-016 — EXP-002 C0 P1-A property run

- Status: `READY` — TR-015 disposed RT-004; SR-002 default is a **full** C0 P1-A run. Execution Start is supplied in the assignment prompt after this WI is committed and the assigned branch/worktree is prepared.
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: score G01–G16 and T01–T10 against frozen evaluator + frozen C0 candidate; conclude exactly one of `P1A_PROPERTIES_PASS` / `FAIL` / `UNRESOLVED`
- Comparison Base Commit (full hash): `e4ee8febfc8ff89e927eba436e81e2ae7759af1c`
- Execution Start Commit: supplied by Technical Lead after this WI is committed and the assigned branch/worktree is prepared
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-executor`
- Assigned Branch: `feat/WI-016-p1a-c0-run`
- Local Commit Permission and Allowed Paths: permitted; result commit(s) may add or modify only
  - `tests/p1a_run/`
  - `scripts/run_p1a.py` (optional)
  - `evidence/experiments/EXP-002/P1A_PROPERTIES.md`
- Remote Push: not authorized
- Work Type: P1-A **property run** of frozen C0. Not implementation of new geometry, not P1-B, not simulator, not selection, not independent evaluation

## Frozen versions (do not change)

- Candidate: `e4ee8febfc8ff89e927eba436e81e2ae7759af1c`
- Evaluator (integrated ancestor): `1ae791b8055690fd9d1239d0e864819865b58f6b`
- Plan blob: `407b5e9f0bf001663bee4f9b9d4b21e601c4bbe4`
- SPEC/catalog: as at Comparison Base / Execution Start; do not edit them

## Independence and wording (D-004 / SR-002)

Same-model `deepseek-flash` wrote evaluator and candidate. This run is a **same-model internal consistency check** on preregistered fixtures. It is **not** independent evaluation. The report must use SR-002 allowed wording and must not use the forbidden phrases. Do not conclude that P1-A “independently verified” the model.

D-004 still applies: do not copy evaluator into candidate; do not edit `src/candidate/` or `src/evaluator/` in this WI.

## Objective

Run every G01–G16 and T01–T10 item (full suite, no convenient subset) under `experiments/EXP-002/SPEC.md` budgets: 10 s per item, 4 min 20 s stage cap. Thin harness may import `evaluator` **only from tests/scripts**, not from `src/candidate/`. Score `candidate_later` by calling candidate modules and checking with evaluator predicates / catalog numbers. Score `evaluator_now` regressions with the existing `tests/p1a` suite (must still pass).

T08: score `AdaptiveGate` / clear-list uniqueness as implemented. Record RT4-F3: `C0Runner` does not consult the gate; C0 has no adaptive actions. Do not fail C0 analytic cover solely for unused runner wiring. T05: use `OuterState.apply`, not the dead `build_outer` helper (RT4-F2).

## Forbidden

- Do not edit `src/candidate/`, `src/evaluator/`, `tests/p1a/fixtures/`, SPEC, catalog, or the plan.
- Do not skip named items. Do not open C1/C2/P1-B/simulator/formal slots.
- Do not treat 195 candidate unittests as this run. Do not close `RT-002`. Do not merge or push.
- Do not move thresholds to fit scores. At most this one run in this WI (repairs are a later WI; SR-002 cap is two substantive retests per round).

## Required commands

Record output and exit status from the assigned worktree:

1. Git identity: toplevel, branch, `HEAD`, `status --short --branch`
2. `git cat-file -e` Comparison Base and `merge-base --is-ancestor` vs HEAD
3. `git cat-file -e HEAD:work/WI-016.md`
4. `PYTHONPATH=src python -m unittest discover -s tests/p1a -q`
5. The property-run command actually used (must cover G01–G16 and T01–T10, enforce 10 s / 4 min 20 s or record a cap stop as `UNRESOLVED`)
6. `git diff --check` on the result commit

## Report fields

`evidence/experiments/EXP-002/P1A_PROPERTIES.md` must include: date, author identity, worktree, branch, Comparison Base, Execution Start, result-commit placeholder, candidate hash, evaluator hash, interpreter, D-004/SR-002 wording disclaimer, per-item result and wall-clock, truth-exclusion / false-completion / unsafe-cancel / ledger residual, T05 peak leaves/depth, G16 submission error if exercised, one closed-set conclusion (`P1A_PROPERTIES_PASS` or `FAIL` or `UNRESOLVED`), local commit status, `NOT PUSHED`.

If PASS, quote SR-002: finite fixtures found no conflict; does not replace analytic cover, independent challenge, or official slots.

## Acceptance

- Full suite attempted; no silent skip
- Conclusion matches SPEC closed set and the recorded item table
- Evaluator/candidate/fixture bytes unchanged
- Wording does not claim independent evaluation
- Authorized paths only

## Stop

Git mismatch; any edit outside allowed paths; need to change C0 mathematics; desire to skip items or open C1. Stop after the report commit.
