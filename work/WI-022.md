# WI-022 — Diagnose Q3 practice 14/16 COMPLETE (false no-source / miss)

- Status: `READY`
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: explain and, if it is implementation, **fix** why frozen B reported `COMPLETE` with `K=14` when the operator UI had **N=16 omni** on the WI-020 Q3 practice
- Comparison Base Commit (full hash): `7f4dfba66a1467da865e411c2aa03bbec2203843`
- Execution Start Commit: supplied after this WI is committed and the worktree is prepared
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-executor`
- Assigned Branch: `feat/WI-022-q3-miss-diagnosis`
- Local Commit Permission and Allowed Paths: permitted; only
  - `src/candidate/` (only if a located implementation defect requires a patch; record the blob change)
  - `tests/candidate/`
  - `tests/c0_baseline/`
  - `evidence/experiments/EXP-004/Q3_MISS.md`
  - `evidence/experiments/EXP-004/q3_practice.json` (read; do not rewrite the frozen live log)
- Remote Push: not authorized
- Work Type: diagnosis / bounded implementation repair. **Not** 150/49, not C1, not Phase 5 pack

## Frozen facts (do not reopen)

- B geometry commit `d675831`; WI-020 result `7f4dfba`; `scan.py` blob `32dd26f7fccf8b2d50f2af349251848cea1c39fc` unless this WI documents a necessary patch.
- Live log: `evidence/experiments/EXP-004/q3_practice.json` — K=14, six `no_source` channels, 180 measures, COMPLETE.
- Operator UI: N=16 all omni ⇒ **two misses**. Grade default: implementation or no-source-certificate **configuration**, not C0 model-family, unless you produce a theoretical P3 counterexample (then **stop** and escalate).

## Objective

1. From the frozen Q3 log: list cleared vs `no_source` channels; whether all 9 `P_3` points were measured per remaining channel; observation labels; whether exit happened before all channels were scanned or after a false empty certificate.
2. Offline test that **fails on current B** if the same control flow would mark an omni still in range as `no_source` / `COMPLETE` with K<N.
3. If implementation: one patch, tests, do **not** silently add 150/49. If configuration-only (illegal world, operator mode): say so and do not change `src/candidate/`.
4. **No live run in this WI** unless Technical Lead later amends it. Do not auto-open the simulator.
5. Report `Q3_MISS.md`: mechanism, evidence from the log, conclusion `Q3_MISS_EXPLAINED_OPEN` / `Q3_MISS_PATCHED` / `Q3_MISS_ESCALATE`.

## Forbidden

81→49, 225→150, C1, formal, push, pairing with other practices as speedup, reading simulator hidden truth, raising P1-A time caps.

## Required commands

Git identity; Comparison Base ancestor; `HEAD:work/WI-022.md`; `git rev-parse HEAD:src/candidate/scan.py`; `PYTHONPATH=src python -m unittest discover -s tests/candidate -q`; `tests/c0_baseline -q`; `git diff --check`.

## Stop

Git mismatch; 150/49; theoretical P3 hole (escalate); third geometry family. Stop after the report commit.
