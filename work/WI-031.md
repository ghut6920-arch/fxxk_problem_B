# WI-031 — Q2 finite-construction chapter closeout

- Status: `COMPLETE` — result `53285abb737291b34ccfc7a5f5e64f396a1c9540`; TR-036 PASS for bounded authoring/evidence scope. Five registered claims remain `PENDING_RED_TEAM`; chapter freeze waits for WI-032 and final Technical Review.
- Owner Role: Executor
- Coordinator / Review Recipient: Technical Lead
- Related Goal: produce one evidence-consistent Q2 chapter closeout under SR-003 / D-008; completion is not conditioned on finding a better point
- Comparison Base Commit (full hash): `067f4ca057857840b24444f5ff50c9604db696d2`
- Execution Start Commit: supplied by Technical Lead after this WI is committed and the assigned branch/worktree is prepared
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-q2`
- Assigned Branch: `feat/WI-031-q2-finite-closeout`
- Local Commit Permission and Allowed Paths: permitted; only
  - `paper/02_Q2.md`
  - `paper/CLAIMS.md`
  - `evidence/experiments/EXP-006/Q2_WRAPUP.md`
  - `evidence/experiments/EXP-006/Q2_ANALYTIC_CLOSEOUT.md` (new)
- Remote Push: not authorized
- Work Type: statement fidelity, bounded analytic proof record, and pending/evidence-linked claim registration. No algorithm search.

## Fixed inputs

- Q2 repaired target: `067f4ca057857840b24444f5ff50c9604db696d2`
- RT8-F1 recheck: `b13daeb6bd12ab0a3b9c6be4f359fee38b09e371`
- `q2.py` blob: `00320497e0936d570d4a164b729e43543714ff07`
- `modeling/COMPLETE_MODEL_PLAN.md` §4
- `problem/RULES.md`
- `audits/strategic/SR-003.md`, `DECISIONS.md` D-008
- `audits/technical/TR-029.md`, `TR-033.md`, `TR-035.md`

The fixed commits are review inputs, not this task's Execution Start.

## Required corrections

1. Replace any statement that an omni channel is guaranteed receivable within 1500 m. State that the source-specific actual `R_c` is unknown in `[1000,1500]`; reception depends on actual distance and that fixed per-world radius. Explain why a successful first direction permits the existential range used in `A_1`.
2. Define `near` by current measurement point-to-source distance `≤5 m` plus applicable visibility, not by distance between measurement points.
3. Define `epsilon_d=10^-6 m` only as the experiment-design fixture boundary perturbation/test resolution. It is not official position error, official bearing error, or an expansion of an official threshold.
4. Separate these objects without changing them:
   - mathematical paper pool: six points `{250,500,750}×{-500,500}`;
   - default comparison program: six pool records + a duplicate `lateral=(500,500)` tag + extra `forward=(500,0)` record;
   - seven unique coordinates, eight tagged records;
   - `certifiable_candidates(..., with_lateral=True, with_forward=True)` ranks every valid tagged record and its `selected` is the comparison-list selected;
   - the six-point paper rule ranks only the six pool points. For the fixed evidence both select `(750,-500)`; this coincidence does not redefine the pool.

If accurate description cannot reconcile the contract and fixed program, stop and report the exact conflict. Do not edit code or the selection rule.

## Required bounded analytic record

Create `Q2_ANALYTIC_CLOSEOUT.md`. Use official contracts and exact/integer geometry before any supporting arithmetic. Do not present the following as already independently reviewed.

### Proposition A — lower bound for the first feasible region

Fixed first observation: `S=(0,0)`, `theta_hat=0°`, omni. Check `g1=(6,0)` and `g2=(1500,0)` separately as admissible worlds: inside `D`, distance `>5`, one admissible fixed `R_c` per world that explains the received first direction, true bearing error inside `[-1°,1°]`. Different worlds may have different radii; within each world the radius is fixed.

If both are feasible, prove `r(A_1) ≥ ||g2-g1||/2 = 747 m`.

### Proposition B — lower bound for fixed forward

Use `p_f=(500,0)` and one second response `direction, theta_hat_2=0°`. Check `g3=(506,0)` and `g4=(1500,0)` as separate admissible worlds consistent with the same named first and second observations. For each world record both distances, an admissible single radius held fixed across both observations, bearing errors, source domain/type, and that no truth is passed to the policy.

If both are feasible, prove that the corresponding exact successor contains two points 994 m apart and hence `J_2(p_f) ≥ 497 m`.

### Fixed upper bound and combinations

Trace `88.93905919223569 m` to `067f4ca:evidence/experiments/EXP-006/q2_wrapup.json`, production caps 360 intervals / 4096 leaves / depth 8, selected pool point `p*=(750,-500)`, the fixed module pins including `q2.py` blob `00320497…`, and the covering-interval argument establishing `J_2(p*) ≤ J_bar_2(p*)`. Only if that trace is complete may the record round outward to `J_2(p*) ≤ 89 m` and derive:

- `J_2(p*) / r(A_1) ≤ 89/747 < 0.12`;
- `J_2(p*) ≤ 89 < 497 ≤ J_2(p_f)`.

State that the first is a ratio of worst-case covering radii for this fixed first observation, not actual error reduction. State that the second is an upper-bound/lower-bound comparison against fixed forward, not a comparison against lateral and not a global-optimality result.

If any premise or upper-bound trace fails, mark the affected combination `UNRESOLVED` and preserve the reason. Do not search for replacement points, radii, caps, or observations.

## CLAIMS registration

Update `paper/CLAIMS.md` with separate entries for the retained `C_in` claim, Proposition A, the fixed `p*` upper bound, the ratio combination, and the fixed-forward separation if supported. Every entry must name its status (`PENDING_RED_TEAM` at handoff unless unsupported), paper location, rule, assumption/design choice, fixed code/blob/configuration, evidence record/result commit placeholder, and reviews still required.

Registration is not formal approval. Unsupported claims remain `UNRESOLVED`; do not delete the template or unrelated entries.

## Required checks

- Read the fixed blobs directly, not only the briefing.
- `git rev-parse HEAD:src/candidate/q2.py` must remain `00320497e0936d570d4a164b729e43543714ff07`.
- Confirm the working diff contains only the four authorized paths.
- Search the revised chapter/evidence for the three incorrect meanings and for candidate-count ambiguity.
- Check exact arithmetic `1500-6=1494`, `1494/2=747`, `1500-506=994`, `994/2=497`, and record `89/747` without claiming measured precision.
- `git diff --check` over all changed paths.
- No production 360×4096 rerun, no large sampling, no `tests/p1a_run`, no simulator.

## Bounded budget

One statement/proof pass, the four named witnesses only, the fixed `p*` only, and one local result commit. No parameter, resolution, candidate, or observation search. Existing data may be read and mechanically checked; do not rerun performance work to improve numbers.

## Forbidden

- Any edit to `src/`, tests, fixtures, rules, model plan, EXP specs, STATUS/NEXT_ACTION, audit records, or JSON numeric evidence.
- OPEN-2 changes; new candidates/caps/parameters; C1/C2; raised time caps.
- Q3/Q4 simulator work, formal `/enter`, formal results, final selection, integration, or push.
- Describing `J_bar_2` as actual error; claiming lateral superiority, continuous global optimality, arbitrary-`S/theta` generality, equal actual-error reduction, two-measure clearing, stable 10 s execution, or RT-002 closure.

## Completion report

Return worktree, branch, Comparison Base, supplied Execution Start, full result commit, actual changed/staged paths, commands and results, failures, unsupported claims, remaining OPEN items, local commit status, and remote push status. Conclusion: `Q2_CLOSEOUT_READY` or `Q2_CLOSEOUT_OPEN`.

## Stop and escalation

Stop on Git mismatch, extra paths, a required code/selection-rule change, inability to bind 89 m to the fixed evidence, or a witness/official-contract conflict. Report to Technical Lead without broadening the WI.
