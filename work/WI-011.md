# WI-011 — Inventory Locally Reachable P0 Inputs

- Status: `COMPLETE` — Technical Review `audits/technical/TR-010.md`: `PASS`; Executor result `49db3ae1c57c4aa0fd4c321569c9fcbd748fd528`; conclusion `INPUTS_PRESENT_NOT_APPROVED`
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: Record whether locally reachable experiment-design and complete-model-plan artifacts are an approved immutable P0 contract; no change to the competition goal
- Comparison Base Commit (full hash): `a562fdd4756bcd67114a1ee1b7b54cd589b6b969`
- Execution Start Commit: `c423b9a48e94f0c640dc0554cf677f9888846217`
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-executor-wi011`
- Assigned Branch: `feat/WI-011-p0-input-audit`
- Local Commit Permission and Allowed Paths: permitted; the result commit must contain exactly `evidence/prerequisites/P0_INPUT_AUDIT.md`
- Remote Push: not authorized; intended future remote/branch/payload is none
- Work Type: evidence-only input inventory; not an experiment, EXP SPEC, implementation, or model selection

## Issuance Note

This WI supersedes the unreachable remote issuance that called itself `WI-006` in `prompts/TECH_LEAD_HANDOFF_WI-006.md`. That document used another host's worktree, commit `e05f199c5c9eebe1a288539c784cecd640ab5e8a` (not in this repository), and treated missing design files as the expected state. It does **not** reopen or replace the completed local `WI-006` (Red Team SMOKE-001 integration).

User-authorized substitute issuance because the Technical Lead agent had no remaining quota. The issuer remains Independent Red Team after this bounded coordination. This issuance is not an independent Red Team review of WI-011.

## Objective

Search the locally reachable repository state for experiment-design and complete-model-plan artifacts, record exact provenance, and decide whether those artifacts are an approved immutable contract from which a later P0 EXP SPEC may be issued. Do not implement P0, create an EXP SPEC, or run an experiment.

## Inputs

- `AGENTS.md`
- `docs/roles/executor.md`
- This WI
- `STATUS.md`
- `NEXT_ACTION.md`
- `experiments/SPEC_TEMPLATE.md`
- `work/README.md`
- `work/WI-010.md`
- `audits/technical/TR-009.md`
- `audits/redteam/RT-002.md`
- `prompts/TECH_LEAD_HANDOFF_WI-006.md` (stale remote issuance; provenance only)
- All locally reachable worktrees and refs, read-only

## Allowed Scope

- Perform the Task Git Protocol pre-task checks in this WI and stop on mismatch.
- Run every Required Audit Command against locally reachable refs and sibling worktrees listed by `git worktree list`.
- Read files and commits; record path, ref, full commit, git blob SHA-1, and SHA-256 of blob bytes.
- Create exactly `evidence/prerequisites/P0_INPUT_AUDIT.md` with every required field, then one local commit containing only that path.

## Forbidden Scope

- Do not create `experiments/EXP-NNN/SPEC.md`, experiment code, configuration, fixtures, data, metrics, plots, or results.
- Do not infer or invent P0 scientific content, inspect or interpret competition attachments for modeling, install dependencies, run a simulator, browse externally, or begin P1-A or any later step.
- Do not modify `main`, other worktrees, `work/WI-006.md`, `work/WI-010.md`, design documents, reviews, `STATUS.md`, `NEXT_ACTION.md`, or any path other than the one authorized output.
- Do not pull, fetch, switch, merge, rebase, reset, repair Git state, rewrite history, or push.
- Do not treat presence of design files as `STALE_WI_STOP`. Presence is expected on this host and is the thing to inventory.
- Do not treat `WI-010` `FIX_REQUIRED`, an unreviewed design-branch revision, or an unintegrated file as an approved immutable contract.

## Required Audit Commands

Run each command separately in the assigned worktree. Preserve the actual output and exit status. Do not rewrite `git grep` / search exit status 1 as success; record it as a no-match observation.

1. `git rev-parse --show-toplevel`
2. `git branch --show-current`
3. `git rev-parse HEAD`
4. `git status --short --branch`
5. `git cat-file -e "a562fdd4756bcd67114a1ee1b7b54cd589b6b969^{commit}"`
6. `git merge-base --is-ancestor a562fdd4756bcd67114a1ee1b7b54cd589b6b969 HEAD`; then `echo $LASTEXITCODE` (PowerShell) or equivalent
7. `git cat-file -e "HEAD:work/WI-011.md"`
8. `git worktree list --porcelain`
9. `git branch -a --list`
10. `git log --all --oneline --decorate -- modeling/EXPERIMENT_DESIGN.md modeling/COMPLETE_MODEL_PLAN.md`
11. `git rev-list --all -- modeling/EXPERIMENT_DESIGN.md modeling/COMPLETE_MODEL_PLAN.md`
12. For every worktree path from command 8, test whether `modeling/EXPERIMENT_DESIGN.md` and `modeling/COMPLETE_MODEL_PLAN.md` exist on disk and record yes/no.
13. For every distinct commit from commands 10–11, record `git rev-parse <commit>:modeling/EXPERIMENT_DESIGN.md` and/or `git rev-parse <commit>:modeling/COMPLETE_MODEL_PLAN.md` when that path exists at the commit.
14. SHA-256 of each distinct blob from command 13 using Python so encoding cannot alter bytes:

```python
python -c "import hashlib,sys,subprocess; c,p=sys.argv[1],sys.argv[2]; d=subprocess.check_output(['git','show','%s:%s'%(c,p)]); print(hashlib.sha256(d).hexdigest())" <commit> <path>
```

15. Search reachable text for the staged contract. Record output and exit status:

- `git grep -n -I -e "P0" -e "P1-A" -e "P1-B" -e "P2" -e "P3" main -- modeling experiments work STATUS.md NEXT_ACTION.md`
- `git grep -n -I -e "P0" -e "P1-A" -e "P1-B" -e "P2" -e "P3" design/NA-003-experiment-design -- modeling/EXPERIMENT_DESIGN.md modeling/COMPLETE_MODEL_PLAN.md`
- `git grep -n -I -e "EXPERIMENT_DESIGN" -e "COMPLETE_MODEL_PLAN" $(git rev-parse --all)` is not required if it is too large; the explicit refs `main`, `design/NA-002-complete-model-plan`, `design/NA-003-experiment-design`, `origin/main`, and `review/RV-001-smoke-audit` are required.

16. Record the current text of these Lead/review facts from the assigned HEAD and, when different, from `main` and `design/NA-003-experiment-design`:

- `work/WI-010.md` status line
- `audits/technical/TR-009.md` disposition and `Rechecked Version and Evidence`
- `audits/redteam/RT-002.md` highest finding severity
- `STATUS.md` WI-010 collaboration bullet
- `NEXT_ACTION.md` status line
- `work/README.md` next identifier

17. `git diff --check` on the assigned HEAD, then again on the staged result before commit.

## Required Report Fields

Create `evidence/prerequisites/P0_INPUT_AUDIT.md` containing at least:

- Date, role, assigned worktree, branch, Comparison Base, Execution Start, and result commit placeholder filled after the local commit
- Every Required Audit Command with actual output and exit status
- Table of found `EXPERIMENT_DESIGN*.md` and `COMPLETE_MODEL_PLAN.md` rows: path, ref, full commit, git blob SHA-1, SHA-256
- Whether a repository-backed `P0 -> P1-A -> P1-B -> P2 -> P3` dependency contract exists, and the exact path/commit of the definition
- Whether each found artifact is on `main` or only on an unintegrated design branch
- WI-010 status, TR-009 disposition, TR-009 recheck field, RT-002 highest severity
- One conclusion from the closed set below, with evidence
- Limitations and remaining issues
- Local commit status and remote push status `NOT PUSHED`

## Conclusion Set

Choose exactly one:

- `INPUTS_ABSENT_BLOCKED` — no locally reachable `EXPERIMENT_DESIGN*.md`, `COMPLETE_MODEL_PLAN.md`, or repository-backed `P0/P1-A/P1-B/P2/P3` contract.
- `INPUTS_PRESENT_NOT_APPROVED` — one or more such artifacts exist, but they are not an approved immutable contract. Treat as not approved if any of these hold: `WI-010` is not `COMPLETE`; `TR-009` is not `PASS` on the exact file hash/commit being considered; the file exists only on an unintegrated design branch; or an open `CRITICAL` finding still applies to the proposed use of that artifact.
- `READY_FOR_P0_SPEC` — approved, immutable experiment-design and complete-model-plan artifacts are identified by full commit and hash, reconciled with `STATUS.md` / `NEXT_ACTION.md`, and sufficient for a later Technical Lead to issue a P0 EXP SPEC without inventing scientific content.

Presence of design files is not `STALE_WI_STOP` and is not execution authority.

## Acceptance Criteria

- Pre-task: assigned worktree, branch, `HEAD` equals the assignment Execution Start Commit, Comparison Base is an ancestor of `HEAD`, `work/WI-011.md` exists at `HEAD`, and the worktree has no unexplained change.
- Every Required Audit Command was run and recorded with output and exit status.
- The report names every found artifact with path, ref, full commit, blob SHA-1, and SHA-256, or records a negative search with the actual no-match exit status.
- The conclusion is one of the three allowed values and matches the recorded evidence, including the approval tests above.
- The local result commit changes exactly `evidence/prerequisites/P0_INPUT_AUDIT.md`; commit-range `git diff --check` passes.
- No EXP SPEC, experiment, implementation, attachment interpretation, or remote push occurs.

## Stop Conditions

- Stop without editing if the worktree `HEAD` differs from the assignment Execution Start Commit, the Comparison Base is not an ancestor, this WI is absent at `HEAD`, another unexplained change exists, or a required Git object is missing.
- Stop after the one-path local commit and completion report. Do not issue P0, create an EXP SPEC, or start P1-A.
- Stop with `STALE_WI_STOP` only if a newer committed WI already inventories these inputs or already authorizes P0 from a named approved hash; return that path, ref, commit, and SHA-256.

## Escalation Conditions

- Escalate contradictory hashes, a worktree/ref that appears and then changes during the audit, or any request to implement P0 or interpret official attachments.
- Route any newly discovered `CRITICAL` validity issue to the Strategist per `AGENTS.md`; otherwise hand the fixed result to the Technical Lead.

## Completion Boundary

Completion produces an evidence inventory of locally reachable P0 inputs. It does not accept `WI-010`, close `RT-002`, authorize a P0 EXP SPEC, run an experiment, select a model, integrate a design branch, or authorize a push.
