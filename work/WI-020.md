# WI-020 — Phase 1: repaired C0 (B) offline e2e + two practice observations

- Status: `COMPLETE` — TR-023 PASS of `7f4dfba66a1467da865e411c2aa03bbec2203843`. `B_BASELINE_READY` with residuals (p1a_run cap flake; live N not UI-reported). Not Phase 5.
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: establish a **trusted repaired-C0 baseline** at frozen `d675831` without 81→49 or 225→150
- Comparison Base Commit (full hash): `d675831695d705ea8c4ffcb74fe8c83277847daf`
- Execution Start Commit: supplied in the assignment prompt after this WI is committed and the worktree is prepared
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-executor`
- Assigned Branch: `feat/WI-020-c0-repaired-baseline`
- Local Commit Permission and Allowed Paths: permitted; only
  - `tests/c0_baseline/`
  - `scripts/run_c0_baseline_mock.py`
  - `scripts/run_c0_practice.py` (logging/session confirm only; no geometry change)
  - `src/protocol/` (session/mode confirm and cost-log fields only)
  - `evidence/experiments/EXP-004/`
- Remote Push: not authorized
- Work Type: verification of frozen B. **Do not edit** `src/candidate/` unless you stop and report a new implementation defect (do not mix a silent repair into this WI)

## Frozen B

- Commit `d675831695d705ea8c4ffcb74fe8c83277847daf`
- `scan.py` blob `32dd26f7fccf8b2d50f2af349251848cea1c39fc`
- Keep 9/81 scan, 225 clears, 20 channels, plan mathematics. Distance argument: local 20 m steps + rigid motion preserves distances + 1 m submission. Finite-angle tests check the implementation only.
- Do not cite \(224\cdot22\) on `a48c77c` logs (RT-005). Old practices are historical, not paired speedup.

## A. Offline (required)

1. Re-run, do not inherit without command: `PYTHONPATH=src python -m unittest discover -s tests/p1a -q`; `tests/candidate -q`; `tests/p1b -q`; `tests/p1a_run -q`. Record that WI-016 PASS was on `761c0f7` (pre-snake-repair); this WI **re-runs** on B.
2. Add `tests/c0_baseline/` + `scripts/run_c0_baseline_mock.py`: mock Q3 and Q4 full flow (scan, first observation saved, clear list, completion certificate, simulated exit). Include **N=16** worst-path and a late-protocol/unknown-accept **fail-closed** case. Do not only test light successes.
3. Cost certificate vs plan: name **origin→first measure** move; scan moves; intra-clear consecutive (must be 20 m local / ≤22 submitted); **inter-channel** connects; switch counts; successful vs failed clears; unique accepted actions; early stop on success. Map each term to §5.3. Rigid-motion invariance of local step lengths.
4. Confirm `git rev-parse HEAD:src/candidate/scan.py` equals `32dd26f7…` unless you stopped for a defect.

## B. Live (only after operator)

If and only if the user confirms a **current** practice session (problem number, practice not 正式, **current** `robot_id` / base URL — do not reuse `202619007369` or old mode flags without checking):

- One Q3 and one Q4 on B. **Max two lives.** Do not auto-rerun on failure.
- No formal `/enter`. No simulator internals / hidden truth as policy input.
- Record: K; N and types **only if operator reports UI after exit** (label source); omit/false-complete/unsafe-cancel; Tv; Tv/K (undefined if K=0); scan move, intra-clear move, inter-channel connect, **origin→first**; measure/switch/clear success/fail counts; wall clock, remaining window, slack; retry/reject/unknown-accept; ledger residual.
- If no session is confirmed, conclude offline only: `B_BASELINE_OFFLINE_READY` or `OPEN`, `PRACTICE_NOT_RUN`.

Do not treat these two as Phase 5 qualification. Do not difference them against `a48c77c` as “snake repair gain.”

## Report

`evidence/experiments/EXP-004/B_BASELINE.md`: versions, blob hashes, commands, mock table, live table or NOT_RUN, conclusion exactly one of `B_BASELINE_READY` / `B_BASELINE_OFFLINE_READY` / `B_BASELINE_OPEN` / `B_BASELINE_FAIL`.

## Gate to Phase 2 (TL uses this; you do not start Phase 2)

Both mock flows pass; no false-complete / truth-exclusion / unsafe-cancel / unexplained ledger; no unresolved unknown-accept or deadline fail; itemized costs. Failures: locate configuration vs implementation; stop — TL may issue a repair WI (max 2). Do not implement 150/49.

## Forbidden

C1/C2; 81→49; 225→150; formal; push; editing evaluator/fixtures/plan; pairing with old practices as causal speedup; reading `JammersSimulatorData` as strategy.

## Required git checks

Toplevel, branch, HEAD = Execution Start, status, Comparison Base ancestor, `HEAD:work/WI-020.md`, `HEAD:src/candidate/scan.py` blob, `git diff --check`.

## Stop

Git mismatch; any `src/candidate/` edit without stopping; formal mode; unknown accept live; desire to add 150/49; third live run. Stop after the report commit.

## Technical acceptance (2026-09-13)

Accepted result: `7f4dfba66a1467da865e411c2aa03bbec2203843`. Operator UI: Q3 N=16 all omni, K=14 (two misses); Q4 N=16 (5 omni, 11 dir), K=16. Live Q3 completeness gate **fails**. Offline + Q4 16/16 stand. Phase 2 is math-only; no 150/49 implementation from this COMPLETE.
