# WI-012 — EXP-001 P0 Static Source and Premise Review

- Status: `READY` — previous Execution Start `bf0b6aa344723df84718606f32219aae3cf0d983` is retired by repository consolidation; do not execute until Technical Lead supplies a new Execution Start on the rebuilt Executor worktree
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: pin versions and locate P0 proof-chain premises so a later P1-A WI can be drafted without inventing mathematics
- Comparison Base Commit (full hash): `ae9e1d5390c889ebb5689face763409f4c397018`
- Execution Start Commit: supplied by Technical Lead after this WI is committed and the assigned branch/worktree is prepared; the assignment prompt records the full hash
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-executor`
- Assigned Branch: `feat/WI-012-p0-static-review`
- Local Commit Permission and Allowed Paths: permitted; the result commit must contain exactly `evidence/experiments/EXP-001/P0_STATIC_REVIEW.md`
- Remote Push: not authorized; intended future remote/branch/payload is none
- Work Type: P0 static review under `experiments/EXP-001/SPEC.md`; not implementation, simulator rehearsal, P1-A, or formal test

## Objective

Run EXP-001: verify official hashes, pin the frozen plan and experiment-design commits, record missing implementation/evaluator commits, locate the four proof-chain premises by section, define independent ledger fields from the frozen plan, and conclude `P0_PREMISES_CLEAR` or `P0_PREMISES_OPEN`.

## Inputs

- `AGENTS.md`, `docs/roles/executor.md`, this WI, `experiments/EXP-001/SPEC.md`
- `STATUS.md`, `NEXT_ACTION.md`
- `problem/official/MANIFEST.md`, `problem/RULES.md`, `problem/DATA_AUDIT.md`
- `git show 44bf45ab43fbba6d14461b13c485890db437dd30:modeling/COMPLETE_MODEL_PLAN.md`
- `git show e3ca1e2a12b9f27bee95d03dc31591f19c61fac8:modeling/EXPERIMENT_DESIGN.md` section 4.1
- `audits/technical/TR-008.md`, `audits/technical/TR-009.md` including the recheck addendum
- Executor inventory `49db3ae1c57c4aa0fd4c321569c9fcbd748fd528:evidence/prerequisites/P0_INPUT_AUDIT.md`

## Allowed Scope

- Pre-task Git checks; stop on mismatch. Do not pull, fetch, switch, merge, rebase, reset, or repair Git.
- Run every Required Review Command. Read frozen commits with `git show` / `git cat-file`; do not check out other branches.
- Hash the three local official files with Python SHA-256 of file bytes and compare to MANIFEST.
- Locate, without re-proving, these four chains in the frozen plan (suggested sections from the experiment-design P0 note): continuous disk → scan coverage; first direction → rectangle coverage; Euclidean output error → clear/move bounds; unique accepted action → virtual ledger.
- List independent ledger fields the plan already requires (move, actual receive-channel switch, measure, clear fail/success, enter/exit/reject). Do not invent a new timing theory.
- Write exactly `evidence/experiments/EXP-001/P0_STATIC_REVIEW.md` and one local commit containing only that path.

## Forbidden Scope

- Do not implement models, create fixtures, run or inspect the simulator/executable/archives, install dependencies, or browse externally.
- Do not create P1-A/P1-B/P2/P3 SPECs, `MODEL_SPEC.md`, or `SELECTED_MODEL.md`.
- Do not close `RT-002`, change `WI-010`, interpret competition attachments beyond the already audited `RULES.md`/`MANIFEST.md` hash check, or push.
- Do not treat `SMOKE-001` or Spikes as model evidence.

## Required Review Commands

Run each separately in the assigned worktree. Record output and exit status.

1. `git rev-parse --show-toplevel`
2. `git branch --show-current`
3. `git rev-parse HEAD`
4. `git status --short --branch`
5. `git cat-file -e "ae9e1d5390c889ebb5689face763409f4c397018^{commit}"`
6. `git merge-base --is-ancestor ae9e1d5390c889ebb5689face763409f4c397018 HEAD`
7. `git cat-file -e "HEAD:work/WI-012.md"`
8. `git cat-file -e "HEAD:experiments/EXP-001/SPEC.md"`
9. `git cat-file -e "44bf45ab43fbba6d14461b13c485890db437dd30^{commit}"`
10. `git cat-file -e "e3ca1e2a12b9f27bee95d03dc31591f19c61fac8^{commit}"`
11. `git cat-file -e "49db3ae1c57c4aa0fd4c321569c9fcbd748fd528^{commit}"`
12. SHA-256 of `problem/official/B题.pdf`, `problem/official/附件1.docx`, and `problem/official/附件2.docx` using Python file bytes; compare to MANIFEST
13. `git rev-list --all -- src scripts modeling` limited to naming candidate implementation or evaluator code. Record absence if none. Do not search outside the repository.
14. `git show 44bf45ab43fbba6d14461b13c485890db437dd30:modeling/COMPLETE_MODEL_PLAN.md` used only to cite sections for the four chains and ledger fields
15. `git diff --check` before and after the single result commit

## Required Report Fields

`evidence/experiments/EXP-001/P0_STATIC_REVIEW.md` must contain:

- Date, role, worktree, branch, Comparison Base, Execution Start, result-commit placeholder per AGENTS.md
- Start and end timestamps and elapsed minutes
- Every command with output and exit status
- Official hash table: path, MANIFEST SHA-256, measured SHA-256, match yes/no
- Frozen pins: plan commit, experiment-design commit, implementation commit (`ABSENT` or hash), evaluator commit (`ABSENT` or hash)
- Four-chain table: chain name, plan section, quoted premise pointer, explicit/TODO
- Independent ledger field list taken from the plan
- One conclusion: `P0_PREMISES_CLEAR` or `P0_PREMISES_OPEN`
- Limitations; local commit status; `NOT PUSHED`

## Acceptance Criteria

- Pre-task contract matches this WI and the assignment Execution Start.
- Official hashes match MANIFEST or a mismatch is the reason for `P0_PREMISES_OPEN`.
- Implementation and evaluator commits are explicitly `ABSENT` or a real hash.
- Each of the four chains has a section citation or an explicit inability to locate it.
- Conclusion is one of the two allowed values and matches the evidence.
- Result commit changes exactly `evidence/experiments/EXP-001/P0_STATIC_REVIEW.md`; `git diff --check` passes.
- No implementation, simulator run, attachment reinterpretation, or push.

## Stop and Escalation Conditions

- Stop without editing on Git mismatch, missing frozen objects, or unexplained worktree changes.
- Escalate hash mismatch on a VERIFIED official file, or a plan contradiction that would change constraint meaning, to the Technical Lead / Strategist.
- Stop after the one-path commit and completion report. Do not draft P1-A.

## Completion Boundary

Completion is a P0 static pin-and-premise record. It does not implement C0, validate numerics, authorize P1-A execution, close `RT-002`, select a model, integrate design branches, or push.
