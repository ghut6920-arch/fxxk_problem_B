# WI-017 — P1-B protocol adapter and C0 practice rehearsal

- Status: `READY` — user D-006 authorized P1-B plus C0 **practice** against a user-started local simulator; origin audit waived for this WI only. Execution Start is supplied in the assignment prompt after this WI is committed and the assigned branch/worktree is prepared.
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: connect frozen C0 to official HTTP/JSON; mock T11–T12; then at most one Q3 and one Q4 **practice** run for wall-clock observation
- Comparison Base Commit (full hash): `761c0f7788ed773d2a132b92caaa9e1378b22c88`
- Execution Start Commit: supplied by Technical Lead after this WI is committed
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-executor`
- Assigned Branch: `feat/WI-017-p1b-c0-practice`
- Local Commit Permission and Allowed Paths: permitted; result commit(s) may add or modify only
  - `src/protocol/`
  - `tests/p1b/`
  - `scripts/run_c0_practice.py`
  - `evidence/experiments/EXP-003/`
- Remote Push: not authorized
- Work Type: P1-B adapter + P3-style **practice** observation. Not C1, not formal test, not origin hashing, not selection

## Frozen versions

- Candidate: `e4ee8febfc8ff89e927eba436e81e2ae7759af1c` (present on Comparison Base)
- Evaluator: `1ae791b8055690fd9d1239d0e864819865b58f6b` (do not edit)
- Plan blob: `407b5e9f0bf001663bee4f9b9d4b21e601c4bbe4`
- Rules: `problem/RULES.md`; protocol: OFFICIAL-002 / OFFICIAL-003

## D-006 / SR-002 constraints

User guaranteed the local simulator; **do not** spend this WI hashing archives. Record in the report: origin was user-guaranteed, not independently verified; this does **not** close SR-002 formal-slot origin gate or `RT-002`.

Base URL default `http://127.0.0.1:2026`. **Practice / 演练 only.** If the UI or API indicates formal/正式, **stop** without sending `/enter`. Do not consume a formal slot.

Do not read `JammersSimulatorData/**` logs, sqlite, or `.jlog` as strategy input. After `/exit`, use only official revealed counts if the interface provides them.

C1 stays closed (`UNKNOWN` real-time bound). No adaptive actions.

Same-model D-004 still applies: live results are not independent evaluation.

## Objective

1. **P1-B adapter** (`src/protocol/`): stdlib-only HTTP/JSON for `/enter`, `/measure`, `/clear`, `/exit` per OFFICIAL-003. Unique `request_id`; same id + same body on retry; never new id while accept is unknown; `accepted=false` does not reset the virtual clock from `virtual_time_s=0`; HTTP error / missing JSON is unknown — stop affected interaction. Map responses into the candidate observation/clear/ledger types **without** importing evaluator from `src/protocol/` or `src/candidate/`.
2. **T11–T12 mock** in `tests/p1b/` (no live server required): T11 unknown duration bound ⇒ adaptive calls = 0; late `/enter` / past deadline fail honestly. T12 same-id retry recovers; `accepted=false`; HTTP error; lost response; unknown-accept stop. Wall budget: 10 s each + 20 s adapter mapping of saved T01–T10 event shapes; stage 40 s.
3. **C0 practice client** `scripts/run_c0_practice.py`: drive frozen C0 scan/clear (Q3 uses `P3`, Q4 uses `P4`) through the adapter. Serial requests only. Log send/receive monotonic time, HTTP status, `accepted`, id, retry, raw JSON, local state. Stop on unknown accept, unexpected truth-deletion analogue (cannot know truth live — stop on protocol/ledger inconsistency), or user deadline.
4. **Live runs** only if `GET`/`enter` against the base URL shows a practice session: at most **one Q3 and one Q4** practice. If the simulator is down, report `P1B_ADAPTER_READY` or `OPEN` and `PRACTICE_NOT_RUN`; do not invent live metrics.
5. Report `evidence/experiments/EXP-003/P1B_AND_PRACTICE.md` with one closed conclusion from: `P1B_FAIL` / `P1B_PASS_PRACTICE_NOT_RUN` / `P1B_PASS_PRACTICE_OBSERVED` / `P1B_PASS_PRACTICE_INCOMPLETE`. Do not write `P1A_PROPERTIES_*` or formal qualification.

## Forbidden

- Formal/正式 tests; C1/C2; editing `src/evaluator/`, fixtures, SPEC, catalog, plan, or C0 geometry except via protocol wiring under allowed paths
- Installing packages; pushing; closing `RT-002`; claiming independent evaluation
- Parameter scans; extra practices to make figures; reading simulator internals

## Required commands

Record output and exit status:

1. Git identity (toplevel, branch, HEAD, status)
2. Comparison Base ancestor check; `HEAD:work/WI-017.md` exists
3. `PYTHONPATH=src python -m unittest discover -s tests/p1b -q`
4. `PYTHONPATH=src python -m unittest discover -s tests/p1a -q` (must still pass)
5. If live: the exact `run_c0_practice.py` command(s) and whether Q3/Q4 practice was used
6. `git diff --check` on the result commit

## Report fields

Date, author, worktree, branch, Comparison Base, Execution Start, result-commit placeholder, D-006 origin-waiver sentence, interpreter, T11/T12 results, live base URL, mode (practice/formal-abort), per-request log path, for each live problem: enter remaining time, K, N if revealed, virtual time, wall time, unknown-accept count, whether completed, `NOT PUSHED`.

## Stop

Git mismatch; formal mode detected; unknown accept on live; need C1; desire to hash-skip while claiming origin verified. Stop after the report commit.
