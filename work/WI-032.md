# WI-032 — Independent Red Team review of Q2 finite-construction closeout

- Status: `READY`
- Owner Role: Independent Red Team; the WI-031 author is ineligible
- Coordinator / Review Recipient: Technical Lead
- Related Goal: independently challenge the fixed WI-031 result before the finite-construction chapter may be frozen
- Comparison Base Commit (full hash): `b13daeb6bd12ab0a3b9c6be4f359fee38b09e371`
- Execution Start Commit: supplied by Technical Lead after this WI is committed and the assigned branch/worktree is prepared
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-redteam-q2`
- Assigned Branch: `review/RV-011-q2-finite-closeout`
- Local Commit Permission and Allowed Paths: permitted; only
  - `audits/redteam/RT-009.md` (new independent review)
  - `audits/redteam/RT-008.md` (append-only F2/F3 recheck reference; preserve all prior findings and WI-030 text)
- Remote Push: not authorized

## Fixed review target

Review exact commit `53285abb737291b34ccfc7a5f5e64f396a1c9540`, not a moving branch tip. Validate it with `git cat-file -e` and read these target blobs directly:

- `paper/02_Q2.md`
- `paper/CLAIMS.md`
- `evidence/experiments/EXP-006/Q2_WRAPUP.md`
- `evidence/experiments/EXP-006/Q2_ANALYTIC_CLOSEOUT.md`
- fixed supporting source/tests/JSON as needed, especially `src/candidate/q2.py` blob `00320497e0936d570d4a164b729e43543714ff07`

Review inputs also include `AGENTS.md`, `docs/roles/red_team.md`, `problem/RULES.md`, `modeling/COMPLETE_MODEL_PLAN.md` §4, `audits/strategic/SR-003.md`, `audits/technical/TR-029.md`, `TR-033.md`, `TR-036.md`, and the full historical `audits/redteam/RT-008.md`.

The target, q2.py blob, JSON, and earlier reviews are evidence inputs, not this task's Execution Start. Do not modify the target branch or target artifacts.

## Required independent challenges

### 1. Official semantics and candidate accounting

Check the target text directly against official/rule evidence and fixed code:

- actual source-specific `R_c` is unknown in `[1000,1500]`, fixed within each world, reception depends on actual distance/visibility, and 1500 m is not a uniform guaranteed-receive radius;
- `near` depends on current point-to-source distance `<=5 m` plus applicable visibility, not the distance between two measurement points;
- `epsilon_d=10^-6 m` is a fixture/test boundary perturbation, not official position or bearing error;
- six mathematical pool points, one duplicate lateral tag, one extra forward point, eight tagged records and seven unique coordinates;
- default `certifiable_candidates` ranks all valid tagged records, while the paper six-point rule ranks only the six pool points; the shared fixed selection `(750,-500)` must not silently redefine either rule.

Report any residual ambiguity with exact target location and affected claim.

### 2. Proposition A

For fixed first observation `S=(0,0)`, `theta_hat=0°`, omni, independently check `g1=(6,0)` and `g2=(1500,0)`:

- source-domain membership, non-`near` distance, permitted source type/visibility, true bearing and allowed error;
- an admissible radius for each world and why different worlds may choose different radii;
- each world's radius is a fixed hidden property, not an observation input;
- both points genuinely belong to the exact `A_1` for the named observation.

Then check whether the two-point covering-radius argument supports only `r(A_1)>=747 m`, not equality or arbitrary-observation generality.

### 3. Proposition B

For `p_f=(500,0)` and named second response `direction, theta_hat_2=0°`, independently check `g3=(506,0)` and `g4=(1500,0)`:

- both named observations, both point-to-source distances, source domain/type/visibility, bearings/errors, and non-`near` status;
- one admissible `R_c` held fixed across both observations within each world;
- no hidden truth enters the selection policy;
- both points lie in the same exact successor corresponding to the named response.

Then check whether the separation supports only `J_2(p_f)>=497 m` for the fixed first observation and fixed forward point.

### 4. Fixed upper bound and combinations

Without rerunning production computation:

- bind `88.93905919223569 m` to `067f4ca:evidence/experiments/EXP-006/q2_wrapup.json`, its blob, row `(750,-500)`, and caps 360 / 4096 / depth 8;
- verify the target still cites the correct `q2.py` blob and supporting module pins;
- independently inspect the interval-covering and outward-bounding-box argument enough to decide whether the fixed evidence supports `J_2(p*)<=88.93905919223569<=89 m`;
- check the inequality directions in `J_2(p*)/r(A_1)<=89/747<0.12` and `J_2(p*)<=89<497<=J_2(p_f)`;
- challenge every prohibited overreach: actual-error reduction, lateral superiority, two-measure clearing, stable 10 s, continuous/global optimality, other `S/theta_hat`, or C1/formal readiness.

If the upper-bound applicability cannot be established from fixed evidence, mark dependent claims unresolved. Do not round by trust and do not search for replacement evidence.

### 5. Targeted RT8-F2/F3 recheck

Do not redo all of RT-008. At target `53285abb`, determine only whether:

- F2's two missing guard checks are now actually exercised by fixed tests and the text does not overclaim them;
- F3's provenance ambiguity is repaired by explicit git blob identities versus working-tree CRLF hashes, without implying that the dirty-parent historical generation was clean.

Record the independent determination in `RT-009.md` and append a concise cross-reference/recheck disposition to `RT-008.md`. Preserve the original F2/F3 findings. Do not close RT8-F1 again and do not close RT-002.

## Required output

Create `audits/redteam/RT-009.md` with:

- fixed target, Comparison Base, supplied Execution Start, worktree/branch/status checks, changed paths, and reviewer participation history;
- attack method and exact evidence for every required section above;
- one disposition per registered claim: supported within scope, unresolved, or affected by a finding;
- findings using AGENTS.md severities, or exactly `No findings within the reviewed scope` if none;
- explicit status of RT8-F2/F3 at the fixed target;
- exact allowed and forbidden paper claims;
- remaining OPEN-1/2/3, RT-002, implementation-purpose, freeze and formal boundaries;
- required handoff to Technical Lead and reopen conditions.

An absence of findings is not proof of correctness and is not formal approval. Red Team does not edit `paper/CLAIMS.md`, freeze the chapter, select a model, or authorize integration.

## Checks and bounded budget

- Pre-task Git protocol in full; stop on any mismatch or unexplained dirty state.
- Static fixed-blob inspection and exact integer/rational arithmetic only. Small read-only calculations are allowed; no production 360x4096 rerun, performance retest, simulator, `tests/p1a_run`, large sampling, new candidates, caps, parameters, observations, or proofs by search.
- Confirm only the two authorized audit paths are changed/staged.
- `git diff --check` over the complete changed diff.
- One local review commit. No push.

## Stop conditions

Stop and report to Technical Lead on Git/independence mismatch, missing fixed commit/blob, target movement, required edit outside the two audit paths, official-contract conflict, or an upper-bound/witness failure. Preserve failed checks as findings; do not repair the target or broaden the task.

## Completion report

Return worktree, branch, Comparison Base, supplied Execution Start, full result commit, reviewed target, actual changed/staged paths, checks and results, findings/dispositions, remaining issues, local commit status, and remote push status. Conclusion: `Q2_CLOSEOUT_REVIEWED` or `Q2_CLOSEOUT_BLOCKED`.
