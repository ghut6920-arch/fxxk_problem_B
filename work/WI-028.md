# WI-028 — Q2 wording repair (RT8-F1; optional F2/F3)

- Status: `READY`
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: make the Q2 wrap-up / chapter match RT-008: bound tables are **not** a certified \(J_2\) ranking
- Comparison Base Commit (full hash): `882ba6fe3fd84459c64e2121e872a51cee35304a`
- Execution Start Commit: supplied after this WI is committed and the Q2 worktree is prepared
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-q2`
- Assigned Branch: `feat/WI-028-q2-wording`
- Local Commit Permission and Allowed Paths: permitted; only
  - `paper/02_Q2.md`
  - `evidence/experiments/EXP-006/Q2_WRAPUP.md`
  - `evidence/experiments/EXP-006/q2_wrapup.json` (optional F3 pin)
  - `tests/candidate/test_q2.py` (optional F2)
  - `tests/q2/` (optional F2 / rank-reversal sentence in tests)
- Remote Push: not authorized
- Work Type: **wording / test hygiene**. **Do not** change selection rule, caps, or `src/candidate/q2.py` unless F2 needs a test-only helper (prefer tests only).

## Required (F1)

1. Retract or rewrite “三种分辨率下排序均确定” → repeats of **one** frozen discretisation, not three resolutions.
2. Publish the independent fact: at 24×256, \(a=250\) bound **beats** lateral \(a=500\); at 360×4096 the order reverses; selected family stays \(a=750\).
3. “优于侧移” / −23.3% only as **smaller \(\bar J_2\) under named caps**, not smaller \(J_2\), not certified relative improvement.
4. Do not register `paper/CLAIMS.md`.

Optional F2/F3 in the same commit: empty-`A_1` actually CONFLICT; lex ranking includes move/\(a\)/\(b\); JSON hashes from `git cat-file blob` or a clean HEAD.

## Forbidden

`B题-executor`; `scan.py` / `variants.py`; `tests/p1a_run`; raising 10 s; C1; 150/49; OPEN-2 model change; live sim; push.

## Stop

Git mismatch; extra paths; treating F1 as a solver rewrite. Conclusion `Q2_WORDING_READY` / `OPEN`.
