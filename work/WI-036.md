# WI-036 — Close exact C0 entry verification and fail-closed reporting

- Status: READY
- Owner Role: Executor
- Coordinator / Review Recipient: Technical Lead
- Related Goal: complete the bounded WI-034 entry repair before Red Team review or named-version rehearsal
- Comparison Base Commit (full hash): b205b024d6a828bc7352de877d6ef8ea8cda8ada
- Execution Start Commit: supplied by Technical Lead after this WI is committed and the assigned branch/worktree is prepared
- Assigned Worktree: E:/pycharm/projects/pythonProject18/题目/B题-executor
- Assigned Branch: feat/WI-036-c0-entry-verification
- Local Commit Permission and Allowed Paths: permitted; only
  - scripts/run_c0_practice.py
  - tests/p1b/test_practice_configuration.py
  - evidence/experiments/EXP-005/C0_ENTRY_INTEGRATION.md
- Remote Push: not authorized
- Work Type: one substantive repair to action-evidence verification and failure reporting. No strategy, coverage, candidate or protocol change.

## Fixed inputs and retained configuration

- WI-034 result: b205b024d6a828bc7352de877d6ef8ea8cda8ada.
- Technical finding: fixed local commit 8a1ccae06a59c435c1104ca7034aea3ba8b872b6, path audits/technical/TR-038.md (read that fixed object; the task branch need not contain unrelated main status files).
- Strategy and named configuration remain SR-004 / D-009: Q3 BASE, Q4 SCAN49, explicit Q4 BASE fallback.
- Fixed variant implementation remains 9de8b67d32382e5c3767823e468894e436a7a3eb.

Retain the current plan resolution, same-plan propagation, channel order, complete SCAN49 point set, clear generator, budgets, protocol/session behavior, completion rules and BASE default. CLEAR150, COMBINED, C1/C2, additional search and a general variant selector remain forbidden.

## Defects to repair

1. The current verifier reduces emitted measure actions to cardinalities and switch count. A Q4 set formed by shifting every planned x coordinate by one metre still returns matches=true because it has 49 distinct points, 980 measurements and 979 switches.
2. A false post-run emitted verification does not override COMPLETED or the zero exit status.
3. A pre-enter named-invariant mismatch returns before cleanup/report finalization; ordinary output then indexes a missing session field and crashes.
4. The evidence statement that the wire log reproduced the plan exactly is unsupported until the first defect is repaired.

## Required implementation

1. Preserve and verify the full ordered measure **business-action** sequence from the request records. Compare every emitted (x, y, channel) tuple with the sequence produced from the resolved plan's scan points and the fixed channel order. HTTP retry attempts remain metadata on one business-action record and must not be counted as new actions.
2. The exact comparison must reject a different 49-point set, a shifted coordinate, reordered points, reordered channels, missing/extra actions, and duplicate substitutions even when all aggregate counts match. Keep the aggregate counts for readable reporting, but do not use them as the exact proof.
3. Record a deterministic compact identity for expected and emitted sequences plus a bounded first-mismatch diagnostic. Do not rely on a plan label or digest alone; the code must compare the sequences themselves.
4. After scan completion and before clear actions, verify the exact emitted scan sequence. A mismatch must stop without running clears and produce a named non-success outcome.
5. Verify that emitted clear business actions equal the clear actions recorded by the runner and that, for each discovered channel, the executed sequence is a prefix of the same resolved plan's clear plan for that channel's fixed first positive observation. Early success is valid; do not require 225 emitted clears. A mismatch must override COMPLETED.
6. Any configuration/action mismatch must be visible in the report, produce a non-success outcome and make main return exit status 1. Preserve the attempted exit/cleanup behavior appropriate to an entered practice session.
7. Resolve/check pre-enter named invariants before constructing resources where practicable, or otherwise guarantee cleanup. Both ordinary and JSON output must return a complete bounded diagnostic and status 1 without KeyError and without sending /enter.
8. Update C0_ENTRY_INTEGRATION.md to preserve the discovered b205b02 failure, state exactly what the repaired verifier proves, and record the repaired commands/results. Do not erase or relabel the original mock evidence.

No change under src/ is authorized. If the repair cannot be completed through the three paths above, stop and report the exact interface conflict.

## Required tests

Add focused positive and negative tests proving:

- exact Q3 BASE, Q4 SCAN49 and Q4 BASE fallback measure sequences pass;
- the 1-metre-shifted 49-point counterexample fails despite unchanged aggregates;
- a 49-point order or channel-order mismatch fails;
- emitted clear actions are an exact valid prefix of the runner/plan-derived clear sequence, and a wrong clear coordinate/order fails;
- post-scan or post-run verification failure cannot retain COMPLETED and returns status 1;
- a pre-enter invariant mismatch sends no /enter, returns status 1, and renders safely in ordinary and JSON modes;
- existing forbidden-selection, same-plan, budget, protocol/deadline, BASE-default and mock-flow behavior remains unchanged.

Run serially, without the live simulator:

- tests/p1b/test_practice_configuration.py;
- tests/candidate/test_variants.py;
- tests/c0_baseline/test_variants_flow.py;
- all tests/p1b;
- the three mock practice entries for named Q3, named Q4 and Q4 BASE fallback.

Do not run tests/p1a_run, the 36-track comparison, RT-006 computations, a live /enter, simulator session or formal mode.

## Budget and stop conditions

One substantive repair pass, using WI-034's reserved repair budget. No additional algorithm or search iteration.

Stop and report if any source/protocol path is required, if the exact fixed sequence cannot be reconstructed from the business-action records without changing semantics, if the repair changes accepted actions, clear policy, budgets, completion criteria, deadlines or retry behavior, or if any required regression fails. Preserve every failure; do not weaken an assertion, margin, cap or acceptance criterion.

## Completion report and next gate

Return the worktree, branch, Comparison Base, supplied Execution Start, full result commit, actual changed/staged paths, commands/environment/results, the exact-sequence negative controls, failure-path results, mock counts, repair count, remaining gaps, local commit status and push status. Conclusion: C0_NAMED_ENTRY_READY or C0_NAMED_ENTRY_OPEN.

Technical Lead reviews the fixed commit first. If and only if it passes, a separate Red Team WI will inspect the repaired exact-action evidence and failure-closed paths. No live rehearsal, integration, freeze, formal work or push is implied.
