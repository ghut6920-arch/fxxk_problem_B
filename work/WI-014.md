# WI-014 — EXP-002 Independent Evaluator and Frozen Fixtures

- Status: `FIX_REQUIRED` — TR-012 reviewed fixed result `a49cc64827cc398c44a41d4094776edddb41f79f`; self-checks pass but evaluator defects and G07/G15 interpretation issues prevent acceptance. Original execution contract below is retained.
- Owner Role: Implementation Engineer
- Coordinator / Review Recipient: Technical Lead
- Related Goal: build the independent evaluator and freeze G01–G16 / T01–T10 fixture files so a later **different author** can implement `src/candidate/` without sharing oracle logic
- Comparison Base Commit (full hash): `f69f2c670827fc807a124945bef280fb0907775d`
- Execution Start Commit: supplied by Technical Lead after this WI is committed and the assigned branch/worktree is prepared; the assignment prompt records the full hash
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-executor`
- Assigned Branch: `feat/WI-014-p1a-evaluator`
- Local Commit Permission and Allowed Paths: permitted; result commit(s) may add or modify only
  - `src/evaluator/`
  - `tests/p1a/`
  - `evidence/experiments/EXP-002/EVALUATOR_AND_FIXTURES.md`
- Remote Push: not authorized; intended future remote/branch/payload is none
- Work Type: P1-A evaluator and fixture construction under `experiments/EXP-002/SPEC.md`; not candidate implementation, not the 4 min 20 s property run, not P1-B, not simulator, not model selection

## Independence bind (non-negotiable)

The person/agent who authors `src/evaluator/` **must not** later author `src/candidate/`. Relabelling the role is not independence (`AGENTS.md`; `modeling/EXPERIMENT_DESIGN.md` §4.5). Record the author identity in the report.

## Objective

Implement the evaluator predicates from EXP-002 and instantiate every G01–G16 and T01–T10 fixture from `experiments/EXP-002/FIXTURE_CATALOG.md`. Self-check only `evaluator_now` items. Do not write `src/candidate/`. Do not conclude `P1A_PROPERTIES_PASS`.

## Inputs

- `AGENTS.md`, `docs/roles/executor.md`, this WI
- `experiments/EXP-002/SPEC.md`
- `experiments/EXP-002/FIXTURE_CATALOG.md`
- `modeling/COMPLETE_MODEL_PLAN.md` blob `407b5e9f0bf001663bee4f9b9d4b21e601c4bbe4` (especially §2, §4.1, §5.1, §5.2)
- `modeling/EXPERIMENT_DESIGN.md` blob `9a2b46e89687bd90318036d48785e57ab9e4476b` §4.2, §4.3, §4.5
- `ASSUMPTIONS.md` O-03
- `evidence/experiments/EXP-001/P0_STATIC_REVIEW.md` ledger field list
- `STATUS.md`, `NEXT_ACTION.md`

## Allowed Scope

- Pre-task Git checks; stop on mismatch. Do not pull, fetch, switch, merge, rebase, reset, or repair Git.
- Create a Python 3 stdlib-only evaluator under `src/evaluator/` implementing at least:
  - observation (`near` / `direction` / `no_signal` / `O03_OPEN`)
  - clear success (`u_c=1` and `||p-g||≤20`)
  - virtual ledger `ΔT` and totals from plan §2
  - `C_in` membership from plan §4.1 (evaluator copy)
  - `P_3` and `P_4` from plan §5.1
  - 225 clear centres from plan §5.2
  - `validate_world` for Q3/Q4 legal vs illegal type counts
- Create `tests/p1a/fixtures/` JSON files exactly for G01–G16 and T01–T10 plus `manifest.json` with SHA-256 of each fixture file (Python file bytes).
- Copy numeric instances from the catalog; do not replace them with “similar” points.
- Unittest for `evaluator_now` checks only. Command: `PYTHONPATH=src python -m unittest discover -s tests/p1a -v` (Windows: set `PYTHONPATH=src` then the same python command).
- Write `evidence/experiments/EXP-002/EVALUATOR_AND_FIXTURES.md` with the required report fields and one closed-set conclusion.
- One or more local commits containing only the authorized paths.

## Forbidden Scope

- Do not create or edit `src/candidate/` or any module imported by a future candidate as shared geometry/clock/completion logic.
- Do not run the 4 min 20 s G/T candidate property suite, do not conclude `P1A_PROPERTIES_PASS` / `FAIL` / `UNRESOLVED`.
- Do not implement T11–T12, HTTP, JSON protocol adapters, or inspect simulator archives/executables.
- Do not install third-party packages, use the network, close `RT-002`, edit `MODEL_SPEC.md` / `SELECTED_MODEL.md` / the mathematical plan / EXP-002 SPEC / the fixture catalog, or push.
- Do not invent an official answer for O-03 (T02). Do not treat component worlds as 10–16 source performance samples.
- Do not implement the candidate quadtree, `L=2` scheduler, or fallback-certificate manager (T05/T08/T09/T10 `candidate_later`).

## Required Review Commands

Run each in the assigned worktree. Record output and exit status.

1. `git rev-parse --show-toplevel`
2. `git branch --show-current`
3. `git rev-parse HEAD`
4. `git status --short --branch`
5. `git cat-file -e "<Comparison Base>^{commit}"` using the hash in this WI
6. `git merge-base --is-ancestor <Comparison Base> HEAD`
7. `git cat-file -e "HEAD:work/WI-014.md"`
8. `git cat-file -e "HEAD:experiments/EXP-002/SPEC.md"`
9. `git cat-file -e "HEAD:experiments/EXP-002/FIXTURE_CATALOG.md"`
10. `git rev-parse HEAD:modeling/COMPLETE_MODEL_PLAN.md` equals `407b5e9f0bf001663bee4f9b9d4b21e601c4bbe4`
11. After implementation: the unittest command above
12. SHA-256 of every file under `tests/p1a/fixtures/` using Python file bytes
13. Confirm `git ls-files src/candidate` is empty
14. `git diff --check` on the result commit(s)

## Required report fields

`evidence/experiments/EXP-002/EVALUATOR_AND_FIXTURES.md` must contain:

- Date, role, author identity, worktree, branch, Comparison Base, Execution Start, result-commit placeholder per `AGENTS.md`
- Independence statement: this author will not write `src/candidate/`
- Interpreter version (`python --version`)
- Every required command with output and exit status
- Fixture table: ID, JSON path, SHA-256, `evaluator_now` self-check pass/fail, `candidate_later` items listed not run
- Module list and a statement that no third-party package was installed
- One conclusion: `EVALUATOR_FIXTURES_READY` or `EVALUATOR_FIXTURES_OPEN`
- Limitations; local commit status; `NOT PUSHED`

## Acceptance Criteria

- Pre-task contract matches this WI and the assignment Execution Start.
- All 26 fixture files exist and match the catalog numbers.
- Unittest of `evaluator_now` items passes, or a failed item is the reason for `EVALUATOR_FIXTURES_OPEN`.
- `src/candidate/` is absent. No simulator, no P1-B, no push.
- Conclusion is one of the two allowed values and matches the evidence.
- Result commit(s) touch only authorized paths; `git diff --check` passes.

## Stop and Escalation Conditions

- Stop without editing on Git mismatch or unexplained worktree changes.
- Escalate if a catalog number contradicts the plan or would require a new official interpretation.
- Stop after the report commit. Do not start a candidate WI.

## Completion Boundary

Completion is an independent evaluator plus frozen fixtures. It is not P1-A property passage, not C0 implementation, not P1-B, not `RT-002` closure, not model selection, and not a push.
