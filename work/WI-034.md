# WI-034 — Bind the real C0 practice entry to Q3 BASE / Q4 SCAN49

- Status: `READY`
- Owner Role: Executor
- Coordinator / Review Recipient: Technical Lead
- Related Goal: minimal named-configuration entry required by SR-004 / D-009 before any new practice validation
- Comparison Base Commit (full hash): `9de8b67d32382e5c3767823e468894e436a7a3eb`
- Execution Start Commit: supplied by Technical Lead after this WI is committed and the assigned branch/worktree is prepared
- Assigned Worktree: `E:/pycharm/projects/pythonProject18/题目/B题-executor`
- Assigned Branch: `feat/WI-034-c0-named-config-entry`
- Local Commit Permission and Allowed Paths: permitted; only
  - `scripts/run_c0_practice.py`
  - `tests/p1b/test_practice_configuration.py` (new)
  - `evidence/experiments/EXP-005/C0_ENTRY_INTEGRATION.md` (new)
- Remote Push: not authorized
- Work Type: minimal entry/configuration binding, regression tests, and fixed evidence record. No coverage search or candidate algorithm change.

## Fixed inputs and configuration

- Strategy: `audits/strategic/SR-004.md`, `DECISIONS.md` D-009.
- Variant implementation/evidence: `9de8b67d32382e5c3767823e468894e436a7a3eb`.
- Reviews: RT-006; RT-007 recheck `3a573c323e733ce2e181f1dce64803cddf14ce23`; TR-034.
- Protocol/practice lineage: repaired BASE `7f4dfba66a1467da865e411c2aa03bbec2203843`; `modeling/EXPERIMENT_DESIGN.md` §10.

The named default is exactly Q3 `BASE` (9 scan, 225 clear) and Q4 `SCAN49` (the complete 49-point `700(i,j), i,j=-3..3` scan including all 28 exterior points, 225 clear). COMBINED, CLEAR150 and C1 are forbidden. Do not make a general variant selector. Preserve an explicit Q4 BASE fallback.

## Required implementation

1. Use the existing fixed `candidate.variants` plan objects; do not duplicate point coordinates.
2. The named entry resolves Q3 to `variants.plan_for("BASE")` and Q4 to `variants.plan_for("SCAN49")`, passes that plan to `C0Runner`, and obtains scan points from the same plan.
3. Provide one explicit fail-closed way to start Q4 repaired BASE fallback. It may select only BASE; do not expose CLEAR150 or COMBINED.
4. Log requested mode, resolved tag, source/blob identity available at runtime, planned scan/clear counts, planned measure/switch/request bounds and budget, plus actual action-derived counts. A label alone is not evidence.
5. Make these invariants mechanically checkable: Q3 named 9 scan / 180 measure / 179 switch / 225 clear; Q4 named 49 / 980 / 979 / 225; Q4 BASE fallback 81 / 1620 / 1619 / 225.
6. Preserve practice confirmation, serial requests, same-ID retry, unknown-accept fail closed, deadline protection, enter/exit, coordinate mapping/error contract, near/direction branches, success stop, completion certificate, ledger and independent decomposition.
7. Preserve `C0Runner(plan=None)` and BASE default outside this entry. Do not edit `src/candidate/`, `src/protocol/`, coverage definitions, channel order, clear policy, completion criteria, cost constants, time caps, or screens.

If existing interfaces cannot do this without a `src/` change, stop and report the exact conflict. Do not expand scope.

## Required tests

Add focused tests proving the real entry resolution and emitted plan for named Q3, named Q4 and Q4 BASE fallback; the same plan controls clear generation and budget; CLEAR150/COMBINED/unknown values cannot be selected; and logged resolved configuration agrees with mock action counts rather than echoing a label.

Run serially, without the live simulator:

- new `tests/p1b/test_practice_configuration.py`;
- `tests/candidate/test_variants.py`;
- `tests/c0_baseline/test_variants_flow.py`;
- all `tests/p1b`;
- mock practice entry for named Q3, named Q4 and Q4 BASE fallback, recording actual configuration/action counts in `C0_ENTRY_INTEGRATION.md`.

Do not run `tests/p1a_run`, the 36-track comparison, RT-006 cover computations, a live `/enter`, simulator, or formal mode.

## Evidence, budget, and stop

`C0_ENTRY_INTEGRATION.md` records full fixed/result versions, relevant blobs, commands/environment, test/mock results, resolved configuration and actual counts, failures, and remaining qualification gaps. State that mock integration is not a live SCAN49 rehearsal, independent review, or formal qualification.

Budget: one implementation pass and at most one substantive local repair; targeted tests and three mock checks only. Stop on Git mismatch, authorized-path conflict, source-interface insufficiency, inability to bind actions to the plan, protocol/deadline behavior change, missing SCAN49 point, failing BASE equivalence, or need for another path. Preserve failures; do not weaken tests, coverage, margins, caps, or acceptance criteria.

## Completion report and next gate

Return worktree, branch, Comparison Base, supplied Execution Start, full result commit, changed/staged paths, commands/environment/results, mock counts, failures/repair count, remaining gaps, local commit and push status. Conclusion: `C0_NAMED_ENTRY_READY` or `C0_NAMED_ENTRY_OPEN`.

Technical Lead reviews the fixed commit and briefs the user before any next instruction. A PASS then receives a separate Red Team gate; no rehearsal, integration, `/enter`, or push is implied.
